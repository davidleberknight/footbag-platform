{
  "_comment": "Copy this file to .secrets.local.json (gitignored) and fill in real values for local dev. Each key matches the SSM parameter basename under /footbag/<env>/secrets/<key>. SECRETS_ADAPTER=local is the dev default; the live SecretsAdapter ignores this file entirely.",
  "safe_browsing_api_key": ""
}
