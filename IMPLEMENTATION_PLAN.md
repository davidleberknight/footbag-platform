# IMPLEMENTATION_PLAN.md

This is the single plan of record for the footbag.org modernization: every open work item and every open question we still need to resolve, grouped by who owns it. It is the clean baseline we will load into the go-live kanban board. Long-term design and rationale live under `docs/`; this file tracks what is left to do. The migration's governing principle: one coordinated production go-live, with no required function dependent on legacy infrastructure afterward (the requirement of record in `docs/MIGRATION_PLAN.md`).

How to read it. Sections are ordered by owner, from the people we are coordinating with (Steve, then Julie) to the maintainers doing the build (Dave, then James, whose pipeline work carries the most detail). Each item is written to stand on its own: you should not need to open another document to understand what the task is. Every item must be pedantic for a human reader: state the task in full and unambiguously, name exact identifiers (file paths, route strings, schema objects, env vars) rather than vague gestures, give an explicit done-condition, and use no internal shorthand, finding ids, or labels a reader has not been given. An item a human cannot act on without opening another document or guessing what it means is not done. A reference to `docs/MIGRATION_PLAN.md` (the detailed migration design) or a file path appears only where it points at the actual work or saves copying a large table here. Finished items are deleted from this file rather than marked done.

Owners. Steve is the current footbag.org webmaster (he holds the legacy-site, DNS, email, and data-export facts). Julie is the IFPA secretary (she decides IFPA governance, groups, and votes). Dave is the project maintainer and manager (platform code, and he drives every open question to a decision). James is the historical-data and freestyle maintainer (the data pipeline and all freestyle content).

Tags. Each Dave and James item carries a category so this plan can seed the go-live kanban board directly: **[KANBAN]** a forward feature, test, gate, verified defect, or guarded deviation that becomes a board card (a defect or deviation states its current-versus-target state inline); **[BLOCKED]** waiting on external input (Steve, Julie, the IFPA, or rules expert Red). Steve's and Julie's items, and the final MVP scope, are open questions, marked **[Q]**.

What is and isn't an item here. Documented future work — a capability a canonical doc describes as design intent — is not a gap or a defect; it becomes an item only as a **[KANBAN]** card once scoped for build, and the plan never builds it unsolicited. A verified defect or a guarded, intentional difference from the design with an unblock condition is likewise a **[KANBAN]** card, its current-versus-target state stated inline. Current-versus-target status lives here, never in the canonical docs, which stay pure design intent.

---

## Steve — legacy-site coordination

Steve runs the current footbag.org. The migration is one coordinated production go-live with no hard dependency on legacy tech afterward; that requirement was put to Steve on 2026-07-13, and this section tracks his factual handover — the data, runtime facts, and registrar/provider actions only he can supply — plus his response naming anything that would stop working at shutdown and any service he believes must stay live (with its users, the evidence of current use, and why it cannot be migrated, replaced, archived, or retired). `docs/MIGRATION_PLAN.md` front matter and §19 hold the full background; this section tracks only the actionable status of what Steve owns.

- [Q] **Deliver the committee and board membership table** (`ifpa_committees`, `ifpa_committee_members`, delivered in `groups/backups/latest.sql`). It decides whether a member was on the board at cutover, which sets their starting tier; the board derivation ships inert until it arrives. Status: blocking go-live (data only). (MP §28.)
- [Q] **Supply the @footbag.org email and mailing-list usage facts.** Which addresses are actively read versus dead or spam-only, and any apex aliases configured in sendmail outside the codebase; every active address must exist on Google before the MX repoints, or its mail is silently lost. (The `list.footbag.org` Majordomo disposition moved to the subdomain ruling below.) A candidate inventory is already derived from the dump; this is the residual human judgment. Status: blocking go-live. (MP §19 items 21-22.)
- [Q] **DNS registrar facts and the zone move to Route 53.** Confirm which registrar holds `footbag.org` and where the zone is served today, get the registrar account under IFPA control, and make the name-server change pointing the domain at Route 53. The zone move is go-live preparation done with Dave; after it, DNS changes are operator-made and Steve's bind9 service retires once nothing resolves through it. (MP §19 item 15, §29.12.)
  - Low priority, not a cutover dependency: confirm whether IFPA still owns `footbag.sport` and `footbag.us`, and whether to hold them defensively or let them lapse.
- [Q] **Disposition the legacy subdomains, one ruling per name — none stays on legacy.** Still resolving as of July 2026: the mailing-list hosts `list.` and `lists.` (both exist), the IFPA group-mail host, the WordPress event-sites host, the media hosts `photo.` and `video.`, `v.` (an alias to `www` that must be retired or re-pointed at the switch or it silently breaks), and two old names on the media server (`quebec.`, `ifc.`). Each is replaced, captured into the read-only archive, or retired with notice; every one is plain HTTP, so none could ever safely share the platform's `.footbag.org` login cookie. The wiki is a path on the main site, not a subdomain, and goes dark with the main switch. (MP front-matter question 3, §19 item 16.)
- [Q] **Confirm the forum's status.** The delivered repository proves a phpBB forum once ran on the production host (a charset-migration tool names the `phpbb` database and its tables; a saved shell history opens it and reads a neighbouring forum config). Confirm whether it is still installed, long dead, or deleted, and whether its database survives; surviving content is archived read-only or given an explicit accepted-loss decision. (MP front-matter question 19.)
- [Q] **Write down what the tournament tool does today.** The written description (the functions organizers actually use) is the requirements baseline for the native replacement built before Worlds 2027; the legacy software is never carried forward, and no registration export is requested unless the organizer confirmation in Julie's section establishes a need. (MP §28 Tournament in a box.)

---

## Julie — IFPA governance

Julie is the IFPA secretary. She leads the active group and email requirements discovery — gathering current-use facts from real users: group owners, moderators, committee members, the people sending or receiving through each address, event organizers, and the Board — and supplies the governance decisions only she (with the Board) can make; Dave chases each one. The sensitive specifics (actual ballots and tallies) stay out of this public file. `docs/MIGRATION_PLAN.md` front matter (the discovery section) and §20a (with §19 items 35-37) hold the full background; this section tracks the actionable status of what Julie owns. Discovery gates go-live: every kept function needs a named replacement, every retired one an approved decision, and no active communication need left unresolved at the switch.

- [Q] **Which legacy groups, committees, and lists are still alive** — established from real users (who owns, posts, receives, or moderates each; when it last carried non-spam traffic; what breaks if it disappears), not from database flags or generated aliases (the alias-generation job was suspended around 2024, on Steve's report). The output is one ruling per group and address: Google Workspace, Google Groups, a platform feature, another approved service, archive, or retire. (MP front-matter discovery section, §20a item 1, §19 item 35.)
- [Q] **Confirm the outbound mail-sender list**: every legitimate `@footbag.org` sender (Steve's sending host, any others) that the apex SPF record must authorize before the policy tightens. (MP §29.12 hand-applied record inventory.)
- [Q] **Disposition of the old IFPA vote records** (elections, issues, and the individually-attributable ballots): keep, archive, seal-and-encrypt, keep tallies only, or delete. A records-retention decision that is hers, not the operator's; the recommended path is encrypted-and-sealed and never published. (MP §20a item 2.)
- [Q] **Confirm the group-message archive scope with the group owners and the Board, before any transfer.** Which groups are included; approved messages only or everything; spam and moderation residue excluded or kept; how private and public groups differ; who may read Directors and other private messages; where the encrypted archive lives; who holds decryption authority; how long it is retained; and whether any portion may ever be published. Steve has substantially cleaned the archive and can transfer it once a secure destination exists, but Dave provides no destination and requests no dump until this scope is ruled; Julie's wish to preserve group messages (especially Directors) is recorded as input, not authorization. (MP §29.12a sealed archive.)
- [Q] **How sanctioning actually works** (a deliberating committee or simple admin approval, and whether `sanctioning@footbag.org` is still read), which decides whether the platform's current simple-admin-approval design needs to become a committee workflow. (Legacy code shows sanctioning was ad hoc: one form emailed a single hardcoded mailbox, with no persisted approval state, no committee, and no `sanctioning@` address, so the platform's tracked admin-approval already exceeds it.) (MP §20a item 3, §19 item 37.)
- [Q] **Where each group's conversation should live** (new platform, Google Groups, external chat, or retired); no in-app group feature is built until this is decided. (MP §20a item 4, §19 item 36.)
- [Q] **Whether kept groups need real moderators** with power over membership and posts, or whether informal admin and community order suffices. (MP §20a item 5.)
- [Q] **Who to notify** when a group or address is retired or archived, and how. (MP §20a item 6.)
- [Q] **Verify and take over the Google Workspace account, then help run it.** Julie has held a Workspace account since 2009 with possibly stale contact details: with Steve, restore access through a secure channel; verify the edition, the legal and billing owner, the covered domains, the current super administrators, the recovery contacts, domain-verification control, and whether a nonprofit conversion is needed; the Board approves the administration and recovery model, and two IFPA-authorized administrators hold tested recoverable access before go-live. Then help run groups, aliases, and spam settings once the kept groups and addresses are known. No permanent admin role is promised until governance approves it. (MP front-matter question 9, §20a item 7.)
- [Q] **Set the vote-validity policy: quorum and supermajority certification.** The IFPA bylaws bind vote validity: an online membership vote requires at least 10% of members participating with a majority of those voting (bylaws Article VIII), and a bylaw amendment requires a two-thirds affirmative vote with the same 10% participation (Article IX). The platform's voting design (`A_Create_Vote` and `A_Publish_Vote_Results` in `docs/USER_STORIES.md`, not yet built) says nothing about these thresholds; it already states that admins apply vote outcomes manually outside the vote system. Decide: should the platform enforce or display quorum and supermajority checks when voting is built, or is certifying a vote against the bylaws a manual board/secretary step outside the system? Recommended: the manual-certification model, stated explicitly in the stories, matching the existing manual-outcomes pattern. The story edit lands once ruled.
- [Q] **Confirm with the Worlds 2026 organizers what they need from the legacy registration tool.** Which functions they actually use and what data they need after the event; this feeds the native replacement's requirements and the shutdown checklist, and gates any registration-export request to Steve. (MP §28 Tournament in a box; decision 21.)
- [Q] **Ratify IFPA governance for platform-secret custody and administrative authority:** a System Administrator role with the Secretary's oversight; custody, separation of duties, and rotation for the operational secrets; who authorizes granting and revoking platform admin; bootstrap provisioning and total-admin-loss recovery; and audit and reporting to the Secretary. The technical mechanisms already exist (the admin lifecycle and secrets management in DESIGN_DECISIONS); this adds the IFPA authority layer, and per bylaws Article IV it also governs the Stripe-to-IFPA-bank authorization that Dave's Stripe activation depends on. (MP §20a; `GOVERNANCE.md`.)

---

## Dave — platform and project management

Dave owns the platform code and coordinates every open question to a decision: he keeps the action tracker and the shared stakeholder documents, writes up user stories for the facts Steve and Julie supply, and routes governance questions to Julie. The platform work below runs roughly in go-live order.

### Platform build and go-live items

Each item is tagged by category; the tag legend is in the header above.

- **[KANBAN] Run the human legacy-claim strata walk on staging before cutover.** The automated real-claim crawl and the read-only whole-population invariant gate cover the machine-checkable real-data behavior; the messy-data judgement calls (near-duplicate names, shared addresses, no-email stubs, collision clusters) still need a human pass across every claim risk class. `legacy_data/member_data_scripts/sample_legacy_claim_strata.py` emits a git-ignored per-stratum sample (linked-with-email, paid-tier evidence, honoree, same-name disambiguated only by birth date, no-email, bare collision stub, historical-person-without-account) naming who to register as and the expected wizard behavior; for each sampled row the tester registers on staging, reads the captured mail at `GET /dev/outbox`, and confirms the claim wizard does exactly what the row predicts, including the enumeration-safe outcomes where an absent record and an already-claimed record are indistinguishable from outside. The sample carries real names and emails, so it stays a local-only input the tester reads on their own machine and is never committed; report any defect by record id and a structural description, never by a real name, email, or address. Done when every stratum is walked end to end on staging at least once and the pass is recorded in the data-review sign-off.
- **[BLOCKED] Activate real Stripe payments (needs Julie, the Board, and the IFPA bank account).** The live payment adapter is built (`src/adapters/paymentAdapter.ts` reads the API key lazily from Parameter Store), so going live is activation, not code. Blocked on the IFPA governance decision in Julie's section and, per bylaws Article IV, Board and Treasurer authorization to link the IFPA bank account to a Stripe account. The activation and professional end-to-end verification are the migration plan's live-payments gates (EX5: live keys and webhook secret in Parameter Store with one webhook delivery confirmed; EX8: the full go-live procedure). The Parameter Store plumbing for the webhook secret needs no external input and is split out as the `STRIPE_WEBHOOK_SECRET` item below.
- **[KANBAN] Source `STRIPE_WEBHOOK_SECRET` from Parameter Store.** Today the operator pastes the webhook secret into the server's env file by hand because no Parameter Store entry exists for it. Add the secret to Parameter Store in both staging and production Terraform, grant the server permission to read it, and have the deploy pull it from there, updating the DevOps guide to match. Done when the deploy sources the secret from Parameter Store in both environments and the hand-pasted env-file step is gone from the DevOps guide.
- **[KANBAN] Execute the DNS zone move to Route 53 (go-live preparation; replaces the retired apex-redirector build).** Stand up the `footbag.org` hosted zone in Terraform, mirror every existing record from Steve's zone (fresh zone snapshot first; verify with `dig` against the Route 53 name servers that the answers match the snapshot), coordinate Steve's registrar name-server change, and verify resolution end-to-end. `enable_apex_alias_records` stays off through the move (Route 53 must first serve the existing legacy-pointing records faithfully) and flips at T-0 as the go-live switch; the bare domain is served as a Route 53 ALIAS, so the previously planned Lightsail apex redirector is never built. Done when Route 53 is authoritative, its answers match the pre-move snapshot, and `scripts/dns-ttl-preflight.sh` works against the zone. (MP §19 item 15, §29.12; DEVOPS_GUIDE §6.8 zone-authority handoff checklist.)
- **[KANBAN] Build the legacy shutdown checklist and the recovery package.** With Steve, enumerate every legacy production service, machine, and provider account (web, mail, the group-mail list host, media hosts, DNS, host-only scripts and cron jobs, and anything the legacy-feature disclosure surfaces); define the encrypted recovery and reference artifacts (the final member export, the pre-flip snapshot, and the approved archive captures), their custody under IFPA-controlled access, and recorded destruction dates for working copies. At go-live: capture and verify the artifacts, then Steve executes the shutdown and Dave verifies it with him, recording evidence and timestamps in the cutover log. Done when the checklist exists and is agreed with Steve, every artifact has custody and a destruction date, and the go-live runbook carries the shutdown-and-verify step. (MP §19 items 30-31, §29.12a.)
- **[KANBAN] Build the archive.footbag.org serving stack.** No archive resource exists in any Terraform tree while live code already redirects to the hostname (`src/controllers/clubController.ts`; the member-facing `legacyRedirectController` sends a resolved legacy member to their live profile, not to the archive). Build in `terraform/production/`: the private archive S3 bucket behind Origin Access Control; the CloudFront public key + key group for signed cookies (private half in Secrets Manager or SSM SecureString scoped to the app runtime role); the archive CloudFront distribution (OAC origin, `trusted_key_groups` on the cache behavior, 1-year edge TTL, custom 403 redirect to the login page); and the `archive.footbag.org` ACM certificate in us-east-1. The serving contract and end-to-end auth test are MP §29.15. Done when `terraform plan` shows the stack and the end-to-end check (unauthenticated 403 → login redirect; authenticated 200) passes.
- **[KANBAN] Prove the restore chain, then implement `scripts/deploy-migrate.sh`, in that order, before cutover-readiness signoff.** The rollback design rests on two unproven things: no database restore has ever been rehearsed (the backup producer `scripts/backup-db.sh` is verified good, but no drill record exists anywhere in the repo), and `scripts/deploy-migrate.sh` — the data-preserving code-plus-migration deploy the post-cutover fix-forward path assumes — is an explicit stub whose own header forbids implementing it until a restore has been rehearsed. Four steps, in order: (1) confirm the snapshot restore runbook and drill cadence already in `docs/DEVOPS_GUIDE.md` (§16.5, §16.7) cover a staging restore drill, extending them only if a gap is found (the migration plan requires the runbook to exist before the drill runs); (2) execute one full restore drill on staging from a real `backup-db.sh` artifact, recording date, duration, and outcome — that dated record is the migration plan's restore-drill gate evidence, and the plan's own note says to schedule the drill with weeks of lead time, so book the calendar slot now; (3) implement `scripts/deploy-migrate.sh` per the 10-step plan in its own header (the working `deploy-code.sh`/`deploy-rebuild.sh` orchestration minus the DB rebuild, plus migration SQL application); (4) run the durability tests against it on staging: curator media and galleries, `email_templates` rows, and member rows all survive a deploy through it with no seeder run, and admin-UI authoring (curator media create/edit/delete, email-template edits) is the sole post-go-live path and durable without seeder replay — the durability leg of the curator and email-template cutover gates, whose seeder-fail-fast leg is already built and regression-tested via the shared in-database post-cutover guard. Sequenced before cutover-readiness signoff because the 48-hour post-flip window is exactly when a migration-shaped fix is most likely, and without this script such a fix escalates to a full DNS revert plus snapshot restore. Done when the dated drill record exists, the script deploys staging without data loss, and the durability tests pass.
- **[KANBAN] Harden lazily-resolved secrets and document production operations.** The live Stripe, Turnstile, and Safe Browsing adapters resolve their Parameter Store secret on first use by design, so a placeholder or wrong key boots clean and fails only under the first real request; optionally add a boot-time probe so a misconfigured secret fails at startup instead. (The KMS JWT-signing adapter is not in this set: it reads its KMS key ID from the `JWT_KMS_KEY_ID` env var at boot and calls KMS directly, so it has no Parameter Store secret to resolve or probe.) Separately, document in `docs/DEVOPS_GUIDE.md` the production-versus-staging cost deltas (the larger Lightsail bundle, longer log retention, dual-region replication, CloudFront) and the rotation runbook for the session and origin-verify secrets.
- **[KANBAN] Alert the remaining administrators to recruit when an administrator is lost.** New behavioral requirement supporting administrator continuity: when an administrator is lost, the remaining administrators receive a work-queue item and an email prompting them to recruit a new admin volunteer as soon as possible. This is a new behavioral surface, so it is specced in `docs/USER_STORIES.md` (extending `A_Manage_Admin_Role` or a new story) before coding. The death and resignation cases have clear triggers (a revoke through `/admin/admin-roles`, or a deceased mark on the member); the "stops working" case needs a defined inactivity signal (for example, no login within a configured window) plus a scheduled check to detect it, which is the open design question the spec settles. Done when the behavior is specced in USER_STORIES, built, and covered by a firing test; then delete this note.
- **[KANBAN] Give operators an in-app way to monitor operational state, and stop the metric-feed log spam.** Today an operator can only watch system health (email-outbox backlog, scheduled-job success and failure, the dead-letter queue, webhook health, payment volumes, raised alarms) by reading the worker's raw structured-log stream, where high-frequency metric-feed lines can bury the signal. The designed views are specified but unbuilt: `A_View_System_Health` and `A_View_Audit_Logs` in `docs/USER_STORIES.md`, the operational-metrics cards on `A_View_Dashboard`, and the `src/internal-admin/**` admin tooling reserved in `docs/DESIGN_DECISIONS.md`. Build the operator-facing monitoring views so health is read in the app, not the log: outbox backlog and the dead-letter queue (the `email_outbox_paused` kill switch exists as a config key read in `communicationService`, with no admin view over it yet), scheduled-job run history with last-success age (the `system_job_runs` table already records each run's status, so this is a read view over existing data), and the alarm-acknowledge and audit surfaces those stories define (`A_View_Dashboard` already specifies the outbox-failure and unacknowledged-alarm summary, `A_Acknowledge_Alarm` the alarm-acknowledge view, and `A_View_Audit_Logs` is already built at `/admin/audit-log`, so only the system-health views remain).
  - Developer ergonomics, same effort: make `./run_dev.sh` startup easy for a human to scan (a concise readiness banner, and/or a development-only human-readable log format), with a dev default `LOG_LEVEL` that hides the high-frequency `debug` lines.
  - Done when an admin can read outbox backlog, job-run health, the dead-letter queue, and recent alarms from an in-app view; and the dev startup output is human-scannable. Trace any view not already covered by `A_View_System_Health` or `A_View_Audit_Logs` to a `docs/USER_STORIES.md` spec before building.
- **[KANBAN] Resolve the last staging/production Terraform parity items.** The two-tree parity audit is the migration plan's OR18 gate, and every divergence is addressed in HCL except the tracked items here: (1) the staging `dr` bucket (with its versioning/encryption/public-access-block resources and the `dr` output in `terraform/staging/outputs.tf`) is marked a removal candidate but not yet removed, so either remove it or wire snapshot replication to it and record reset-tolerance; and (2) the staging SES send grant is deliberately widened to the account-wide `identity/*` resource in `terraform/staging/iam.tf` because SES sandbox mode IAM-checks both the sender and the recipient identity on every send, while production scopes `ses:SendEmail` to the single sender-identity ARN in `terraform/production/iam.tf` — an accepted, in-code-documented divergence; tighten staging to the sender-identity ARN once staging leaves the SES sandbox and sends from a verified domain identity. Accepted divergence, no action: the backup bucket names differ by environment (staging `-snapshots` / `-dr`; production `-db-snapshots` / `-db-snapshots-dr`); each tree is internally consistent, scripts consume the name via the `BACKUP_S3_BUCKET` env var rather than a literal, and the operator docs name both variants. Done when the `dr`-bucket and SES items are resolved and `terraform plan` shows the intended state for both trees, satisfying OR18.
- **[KANBAN] Build and test the recurring-donation subscription handlers (pre-launch).** Recurring donations open at launch, so the subscription path is pre-launch, not deferred. Today `startDonation` and `cancelRecurringDonation` (`src/services/paymentService.ts`) throw `ServiceUnavailableError`, and the webhook dispatcher has no `invoice.payment_succeeded` / `invoice.payment_failed` / `customer.subscription.deleted` cases. Build the donation entry points and the subscription/invoice handlers to the state machine already specified in `docs/USER_STORIES.md` (`M_Donate`, `SYS_Process_Recurring_Donations`: active on `customer.subscription.created`, new payment row on `invoice.payment_succeeded`, `past_due` on `invoice.payment_failed`, `canceled` on `customer.subscription.deleted`), under the same professional-payments standard and production-only verification as one-time payments. Activation still depends on the IFPA bank/Stripe authorization tracked in the Stripe-activation item. Done when donations can be started and canceled, all four subscription/invoice events are handled idempotently, and firing tests cover each transition.
- **[KANBAN] Build the pre-launch payment-readiness pieces the live-payments gate requires.** The professional live-payments go-live gate (the live-payments row in the Migration Plan external-dependencies block) requires capabilities not yet built: a nightly reconciliation job comparing the Stripe ledger to the platform `payments` rows, writing `reconciliation_issues` rows for mismatches and missed webhooks, plus an admin view to resolve them (the `reconciliation_issues` table and config are seeded but have no producer or consumer; `docs/USER_STORIES.md` `SYS_Reconcile_Payments_Nightly`, `A_Reconcile_Payments`); and webhook-delivery-failure alerting (a CloudWatch metric filter + alarm in production and staging `cloudwatch.tf`, because recoverable webhook failures log at `warn`, below the error-only alarm, so a bad secret rotation would not alert until Stripe disabled the endpoint). Done when the reconciliation job and admin view and the webhook-failure alarm are built and tested, satisfying the live-payments gate.
- **[KANBAN] Add the no-gratuitous-concurrency prevention layer (Claude harness-config, approval-gated).** Prevent unapproved concurrency, or a second writer to a shared store, from being introduced silently. Two layers, because neither covers the other. (1) A rule that the app is synchronous by default and that any concurrency, async background work, worker thread, or second writer to a shared store is a deliberate architecture decision requiring explicit human approval; candidate home is the "Transactions" section of `.claude/rules/db-layer.md` (which already documents the single-writer, synchronous model) or a short new `.claude/rules/concurrency.md`. (2) A mechanical gate in `scripts/ci/assert_conventions.sh` that fails the build on `setInterval`, `new Worker(`, `worker_threads`, `child_process`, and fire-and-forget `void <call>` in `src/` outside a named allowlist of the declared worker entrypoints (`src/imageWorker.ts`, `src/transcodeWorker.ts`, `src/worker.ts`), the ffmpeg `spawn` in `src/lib/videoProcessing.ts`, and the `setTimeout(() => controller.abort(), ...)` fetch-timeout idiom in the adapters; a blanket `setTimeout` ban is wrong and must not be used. Add two deterministic unit tests that pin the decidable roots: that every database primary-key id generator derives from `randomUUID()` entropy rather than a resettable counter, and that every `INSERT OR IGNORE` / `INSERT OR REPLACE` in `src/db/db.ts` is annotated as an intentional idempotency primitive so a silent drop can never be accidental. The allowlist must also cover two idioms already present in `src/`: the SSE-heartbeat `setInterval` in `src/controllers/adminCuratorController.ts`, cleared unconditionally on connection close; and the response-owning `void handle().catch(...)` idiom in the upload controllers (`src/controllers/memberMediaUploadController.ts`, `src/controllers/memberGalleryController.ts`, `src/controllers/adminCuratorController.ts`), where the async promise owns the HTTP response by ending in a render or redirect with every error routed to the response or `next`. The gate therefore targets only detached `void` calls that do not own the response. This prevention layer edits `.claude/` and CI, so it needs explicit approval and, per `.claude/rules/claude-harness-governance.md`, Plan Mode and a fixture test; it is not to be applied silently.
- **[KANBAN] Decide the six residual Hall-of-Fame / Big-Add-Posse honor-name identities (delegated from the pipeline linking).** The honor-name linking resolved 24 of the 30 unlinked worklist names to a historical person and handed six to a curator decision before the final member load. Four are ambiguous single-candidate matches (a variant match with no honor flag to corroborate): Garry Gaman → Garry H. Gaman, Honza Weber → Weber Honza, Rene Ruhr → Ren Ruhr, and Arek Dzudzinski → Arek Dudzinski. Two have no person in the corpus and need an identity confirmed from the public roster or an accepted honor-only stub: Maxell Smith Jr. and Tina Aberli. The candidates, person_ids, and evidence are in `legacy_data/reports/honor_name_linking_adjudication.md`. Kept separate from the five possible-duplicate-person findings surfaced during linking, which stay in the person-QC lane and are not part of this decision. Done when each of the six has a recorded disposition (a confirmed person_id or an honor-only-stub decision) that the cutover backfill can consume to grant their tier-2.
- **[KANBAN] Build the admin-to-member in-app message channel (`A_Message_Member`).** The behavior is specced in `docs/USER_STORIES.md` as `A_Message_Member`: an admin sends a member a message or question from the admin member-detail view or in one click from a work-queue item; the member hits an unmissable non-blocking login prompt plus a persistent banner on every member page until answered, and answers with a structured control (acknowledge, or confirm/correct date of birth, a correction writing back through the owning service) plus an optional free-text note; the answer returns to the admin as a work-queue item, reactivating the originating item when there was one. Build to that story: a new `member_messages` table with purgeable body and note columns, the admin composer and work-queue deep-link, the member login prompt/banner and owner-only answer surface, the content-free email nudge, and purge of the content columns on account erasure and the deceased contact-scrub. No such channel exists today; the current follow-up to a member is out-of-band email to the verified login address. First use case is asking a member to confirm their real date of birth from a birth-date-conflict work-queue item, where the content must stay in-app because date of birth is owner-and-admin private. Done when the story's success criteria are implemented and covered by firing tests.

### Gaps between what the site promises and what it does

Each item is a place where a shipped user story is not fully delivered. Delete it when the fix ships with a test. The name in parentheses is the user story it belongs to.

- **[KANBAN] Screen new passwords against a breached-password corpus.** Password validation enforces only length (8–128, no complexity), which matches the passphrase-friendly standard, but new passwords are not checked against known-compromised-password lists, which the same standard recommends. Add a breach check at registration and password change that rejects a known-compromised password with a clear message. This is new behavior, so spec the criterion into `V_Register_Account` / `M_Change_Password` first. Use the HaveIBeenPwned Pwned-Passwords k-anonymity range API (privacy-preserving: only a 5-character SHA-1 hash prefix leaves the server, never the password), with a short timeout and fail-open on outage (a breach check is defense-in-depth, not an availability gate); and include a bundled offline floor (the top-1000 breached list) so the most common passwords stay blocked during an API outage. Done when new and changed passwords are screened and a firing test covers a known-compromised rejection. (V_Register_Account, M_Change_Password)
- **[KANBAN] Populate net media and open the Net topic gallery.** Net footage is not yet part of the browseable gallery/tag collection: no curated media carries the `#net` tag and no `gallery_net` sidecar exists, so the media hub's Net card renders with no link. That empty state is correct today, matching how the Related Sports card stays unlinked until the chinlone gallery is seeded; it is not a defect. Curate net footage into the collection tagged `#net`, add a `curated/galleries/net.json` sidecar mirroring `curated/galleries/chinlone.json` (id `gallery_net`, a single `#net` criteria tag, plus name and description) so the seeder builds the gallery, and wire the media hub Net card in `src/services/mediaService.ts` to link to it conditionally — present only when `gallery_net` is seeded, exactly as the Related Sports card links to `gallery_chinlone` — so the card stays unlinked until net content exists. The `/net` landing page is untouched: its demo video and championship clips are fixed page content by design and are not moved into a gallery. Sideline stays static chrome: the `.webm` demos remain fixed site content and no `#sideline` gallery is built. Done when net media is curated and tagged `#net`, `gallery_net` is seeded from its sidecar, and the media hub Net card links to the populated gallery. (V_View_Gallery)
- **[KANBAN] Add a `mime_type` column to `media_items`.** The table records `media_type` (photo/video) and, for locally-stored videos, `source_filename` plus `video_platform='s3'`, but carries no explicit content type: a stored video's format is only inferable from the `source_filename` extension, so the curated-video MP4-only rule cannot be asserted or served against a real column. Add a `mime_type` column to `media_items`, populate it in the curator seed (`scripts/seed_fh_curator.py`) and the upload/transcode path, and read it where the content type is needed. Done when `media_items.mime_type` exists, is populated for seeded and uploaded rows, and the MP4-only curated-video invariant can be checked against it.

---

## James — data pipeline and freestyle

James owns the data pipeline (turning the old site's data into the new database) and all freestyle content, and he runs the onboarding QC pass below. This section carries the most detail. The QC pass and the cutover-critical pipeline work come first, then the freestyle work.

### Onboarding QC pass (platform code; moved from Dave's lane)

James runs the QC pass over the legacy member load and the onboarding surfaces. The items below moved from Dave's lane with the pass.

- **[BLOCKED] Confusable-skeleton display-name collision check: production enforcement is blocked on obtaining the official Unicode 17.0.0 confusables data file (Unicode UTS #39 follow-on).** Three dependency-free display-name checks already ship at registration in `identityAccessService`: NFC normalization of the real and display names, rejection of invisible / bidi-control / other `\p{C}` code points, and the single-script restriction (a name's letters must resolve to one script, with the Japanese and Korean CJK augmentations allowed), all covered by adversarial registration tests. Registration is the sole guard point: the real and display names are immutable after it (profile edit exposes no name field, and no `UPDATE members` statement sets `real_name` or `display_name`), so no other surface needs the check, and a member re-storing its own unchanged name cannot self-collide. Partially implemented (mechanism only): `src/lib/confusableSkeleton.ts` carries the UTS #39 skeleton transform (with a documented lowercase pre-fold for consistency with the stored lowercased display-name identity), a strict parser for the Unicode confusables data line format that fails loudly on a missing or unrecognized header, a missing version, or a malformed line, and a collision helper; unit tests exercise the parser, the transform, a whole-script and a fullwidth collision, NFC-equivalence, case-folding, determinism, and the loud-failure paths, all against a clearly-labeled hand-authored test fixture at `tests/fixtures/confusables.test-fixture.txt` that is explicitly NOT official Unicode data and NOT complete threat coverage. The target criterion is spec'd in the register-account user story. Deliberately NOT done, and must not be done from a hand-built map: no production registration rejection is wired in, no persisted skeleton column exists, no uniqueness index is added, and no migration or backfill is applied. The reason is that Unicode does not guarantee confusable mappings or the skeletons derived from them are stable across versions, so a persisted skeleton column must be recomputed whenever the data version changes; starting from a partial hand-built map would create an immediate migration liability and an inaccurately named security guarantee. Unblock: obtain the official Unicode 17.0.0 confusables data file. Then, in order: (1) vendor it unchanged with its Unicode version, license notice, source provenance, and checksum; (2) validate its header and format and fail loudly on malformed or unexpected data; (3) compute the existing-member collision audit using that exact table and report every collision cohort, merging, renaming, or discarding no existing account; (4) decide the grandfathering and uniqueness strategy from the real audit (a plain unique index if the audit is clean, or a grandfathering flag plus a partial unique index over non-grandfathered rows if pre-existing collisions block a plain constraint), with enforcement made race-safe through the database constraint plus the existing insert-retry loop rather than an application preflight alone; (5) only then add the indexed skeleton column via a real migration, backfill existing members deterministically, and enable the registration rejection with a clear message that discloses no account information. Done when the official file is vendored, the audit and chosen strategy are reported, the column and enforcement land, and a firing adversarial homoglyph-collision registration test passes; then delete this note. (V_Register_Account)
- **[KANBAN] Retire the internal QC subsystem when James judges it no longer needed (hard go-live gate).** The operator QC surfaces under `src/internal-qc/` — the Net adjudication pages (`/internal/net/*`, `netQcController.ts` / `netQcService.ts`) and the persons name-likeness pages (`/internal/persons/*`, `personsQcController.ts` / `personsQcService.ts` / `personsQcChecks.ts`) — exist to adjudicate pipeline data quality and are deleted before cutover, not shipped. The QC-subsystem retirement gate in `docs/MIGRATION_PLAN.md` (§30, gate R1) blocks the State 3 → State 4 transition and carries the canonical deletion inventory: those controllers, services, and views; the QC-only `db.ts` statement groups under the `// ---- QC-only (delete with pipeline-qc subsystem) ----` banner (the eight `net*` groups plus `personsQc`); the `net_review_queue` table; the QC route tests; the `/internal` mount in `src/app.ts`; and the `dist/internal-qc` strip in `docker/web/Dockerfile`. James decides the moment: as soon as the QC surfaces have finished their adjudication work, and in any case before cutover-readiness sign-off. Until retirement, two interim safeguards keep QC out of production — `src/app.ts` mounts `/internal` only when the environment is not production, and the production image strips `dist/internal-qc` and stubs the router module — and a standing test pins the production 404. The retirement adds the two permanent enforcement layers the migration plan specifies (the positive-assertion test that the deleted files stay gone, and the CI grep for QC entry points).

  One item remains before retirement: resolve whether the `net_candidate_match`, `net_curated_match`, and `net_raw_fragment` tables are QC-only (deleted with the subsystem) or pipeline-load tables (kept). The emerging-vocabulary keeper carve-out and the club `r1`..`r10` column drop are both done, and the migration-plan inventory now reflects them: the workbench is relocated onto the production admin router at `/admin/freestyle/emerging-vocabulary` and still reachable, and the columns with their runtime readers (the `db.ts` SELECT, the club-viability test, and the club-candidate factory) are removed with the cleanup predicate keyed off `classification` alone. The migration-plan test list now names every net route test plus persons QC rather than a subset.

  Done when the net and persons QC inventory is deleted, both enforcement layers are green in CI, and R1 is signed off.

### Data pipeline

#### Legacy-site dump intake (the member data from Steve)

The one intake that reads the live legacy site's database export and lands it in the platform database, unified here so the whole task is visible in one place. Shared contract for every sub-item: the dump is the per-app MariaDB `mysqldump` set (`<app>/backups/latest.sql` in the webmaster's private repository), reached read-only through the git-ignored repo-root `footbag_legacy_repo` symlink or a dump-root environment variable — never a path committed anywhere in this repo. Processing follows the established pipeline shape: extract to PII-bearing intermediate CSVs in the git-ignored `legacy_data/member_data_scripts/out/`, validate, dry-run load, then `--apply` load, with credential columns stripped at extraction and a header abort backstop in the loader. Completed claim state (`claimed_by_member_id`, `claimed_at`) is never disturbed by a re-import (covered by a test). The real-data load runs only on a maintainer machine as a cutover-migration step — never in CI, never against a persistent production database (a code guard enforces this: the loader refuses to run when `NODE_ENV` is production, `FOOTBAG_ENV` is production or staging, or the target database path is a deployed-host path); a build with no dump present warns and skips, which is the correct state for local development and CI. Local development still needs `legacy_members` populated for the `historical_persons.legacy_member_id` foreign key to resolve, so `scripts/reset-local-db.sh` seeds it from a mirror-derived extract (`legacy_data/scripts/load_legacy_members_seed.py`); that mirror-derived seed is a temporary substitute the authoritative legacy-site export replaces at cutover. A second dev machine without the dump bootstraps from one gitignored artifact, `legacy_data/member_data_scripts/out/legacy_members_final.csv` (the deploy-local-data preflight falls back to it); it is the most PII-dense artifact in the project (names, three emails, birth dates, street addresses for every dump account), so it is transferred encrypted and privately only, and re-distributed after every new dump load or the two machines' builds silently diverge. Scale, measured against the delivered test dump: 33,665 member rows (25,655 with `MemberValid=1`), full date of birth on 20,607, primary email on 33,267 (`MemberEmail2`/`MemberEmail3` are empty across the whole dump), 520 Tier-1 and 318 Tier-2 statuses. The end state is a unified production database ready for onboarding matching, legacy claims, and historical-person pages: every dump account in `legacy_members`, every real dump-account-to-historical-person correspondence expressed as `historical_persons.legacy_member_id`, no duplicate historical persons, and the claim and auto-link surfaces reading all of it. What remains below is the externally-blocked board sub-item, the data-remainder adjudications, and the final post-freeze load.

- **[BLOCKED] (5) Board / Tier-3 grant (needs Steve's committee table).** Granting Tier 3 to a member on the IFPA board at cutover: the two board columns on `legacy_members` (`legacy_was_board_at_cutover`, `legacy_board_underlying_paid_tier`) with their consistency CHECK, the board-to-`tier3` grant and `new_underlying_tier_status` derivation (including how a migrated Director's `tier3` relates to the governance ledger path `setGovernanceTier3`, `change_type 'governance_set'`), and the committee-table extractor with `BOARD_IFPA_TIER_CODES` kept empty until the codes are confirmed (do not guess). The derivation scaffold in `extract_legacy_members.py` is in place and inert; the committee table arrives in `groups/backups/latest.sql` (see Steve's section). The legacy admin PHP confirms `MemberIFPATier` is only 0/1/2 and does not encode a board tier, so that committee delivery is the sole unblock. Do not build a single board-member fixture to exercise the grant path early: the board columns are not in the schema and the member record carries no board signal, so the fixture would require building the blocked scaffolding itself; the governance-path grant (`setGovernanceTier3`) is already covered by its own tests. Scope on its own once delivered.
- **[KANBAN] (6) Data remainder before the final production load.** Read-only member-load validation is complete: the extract, full validation, Stage A and Stage B reconciliation, and the read-only dry-run load all ran against the test dump, the two hard gates passed, and no application database rows were written. Separate the state cleanly, because the passing hard gates mean only that the proposed set is internally consistent and safe to apply mechanically, and are not cutover approval or apply readiness. Mechanically validated (all pass): export schema and required-field thresholds, `legacy_user_id` and `legacy_member_id` uniqueness, the `historical_persons.legacy_member_id` referential reconcile, tier and admin derivation, Stage-B proposed-link consistency (unambiguous and non-duplicating), and the dry-run import. Not yet adjudicated: 97 duplicate-account groups (Stage A), 33 ambiguous person-link groups (64 rows) on the current read-only run (Stage B), the 311 email-conflict exclusions held out of the import, 17 proposed links whose account is not importable, and the unresolved honor-account cohorts. Not yet configured: the real cutover date (annual-active Tier-1 derivation stays inert until it is set) and the board-at-cutover source (board grants stay inert). Environment prerequisite: refresh the production site mirror before any canonical event-results regeneration, so a rebuild does not silently re-drop recently-completed events. Owners for the remaining work: Dave holds the unresolved honor-name dispositions and the frozen-dump and cutover-date inputs; Steve or the board-data owner holds the board-at-cutover source; James and curation hold the account and person-link reconciliation queues unless delegated; the cutover operation itself runs the roster-drift check, final extraction, reconciliation rerun, and honor backfill once every prerequisite is available. Detail below. The Hall-of-Fame and Big-Add-Posse honor-name linking is complete on the pipeline side: of the 30-name worklist that had no person link (16 Hall-of-Fame rows with a blank `person_id` in the roster snapshot plus 14 unmatched Big-Add-Posse names, surfaced by the read-only `report_legacy_member_honors.py`), 24 are resolved to a historical-person identity, recorded in `legacy_data/reports/honor_name_linking_adjudication.md`; the six residual identity decisions (four ambiguous single-candidate matches and two true unmatched names) are delegated to Dave and tracked in his lane. James's only remaining honor actions are to re-verify the public rosters for drift shortly before go-live (`diff_live_honor_rosters.py`, refreshing the snapshots if it reports real change) and to run the cutover backfill: that backfill (`extract_legacy_honors.py`) runs against the final dump, fed by the resolved name-to-person map and Dave's six dispositions, or the honorees miss their tier-2 grant; rerun the member-ID cross-check (`crosscheck_member_profile_ids.py`) against the frozen export; load the final post-freeze dump through the orchestrator, running the member extract with the confirmed go-live write-freeze date (the `--cutover-date` / `FOOTBAG_CUTOVER_DATE` input; Dave supplies it) so the Tier-1-annual flag derives against the real cutover moment — on the current test dump the flag is vacuously zero because every ever-paid Tier 1 is lifetime. Also adjudicate the shared-email / shared-user-id collision cohort before the final load: accounts that share an email address or user id with another valid account (311 on the test dump; the count is test-dump-specific and will move on the final dump, listed with their partners in `legacy_data/member_data_scripts/out/reconciliation_excluded_accounts.csv`) are held out of the import entirely — the ratified rule is that no member of a collision group is imported until the group is adjudicated, and both `load_legacy_export.py` and the reconciliation step now enforce the same hold-the-whole-group rule (regression-tested). As part of the final-dump sign-off, re-run the read-only accent-folded duplicate scan over `historical_persons` (NFKD accent-strip plus hyphen/space folding on names, pairs with more than one person per folded name, test fixtures excluded): zero accent-variant pairs on the current data, and the final production data must be explicitly re-verified before go-live; if the final dump introduces real collisions, the person-id normalizer unification tracked as a board task below is run as its own scoped slice before that data loads. Also adjudicate the reconciliation review queues: the 66 Stage-B link-review rows (`legacy_data/member_data_scripts/out/stage_b_link_review.csv`) and the Stage-A duplicate-account groups (`stage_a_duplicate_accounts_review.csv`): ruled to auto-merge at load any accounts sharing an exact name and exact full birth date into one claimable record (they are one person, which keeps onboarding fully automated with no human in the claim path), routing to human review only the residue where other fields (email, join year, country) actively contradict. This auto-merge is built and runs only in the final-load path (`run_legacy_members.sh --final-export`): it collapses each clean name+full-DOB group to the lowest-id survivor, unions the emails into the three columns, OR-merges the entitlement flags, takes the earliest join date, and remaps the build-time pipeline references (`historical_persons`, club affiliations, club bootstrap leaders) loser-to-survivor inside the loader's single transaction, hard-aborting before any write on any live-entity reference (a member link, a claim token, an auto-link candidate, or an existing or claimed `legacy_members` row) and verifying no loser id remains in any referencing table afterward. The contradiction residue (conflicting non-empty country, an unpreservable claim handle, more than three emails, or a link to a different canonical person) stays in the Stage A review CSV for human adjudication; two country-conflict groups remain on the current test dump. Done when the final load passes the reconciliation gate and the data-review sign-off records it.
#### Other pipeline work

Pipeline defects, deviations, and cleanup the cutover load and onboarding matching depend on.

- **[KANBAN] The two person-id generators normalize names with local rules weaker than the canonical resolver, risking duplicate or mismatched historical persons (accents, and spaced/doubled hyphens).** Both id generators reimplement name normalization rather than using the canonical `legacy_data/pipeline/identity/alias_resolver.py:52-80` `normalize_name` (which applies NFKD accent-folding plus a defined punctuation/artifact strip), against the one-canonical-normalizer invariant: `legacy_data/clubs/scripts/01_build_club_person_universe.py:24-28` (`normalize_name`) and `legacy_data/persons/scripts/05_build_persons_master.py:30-31` (`norm_name`) both only lowercase, replace hyphens with spaces, and collapse whitespace, with no NFKD. Two consequences. (1) Accent-duplicate risk: neither id generator folds accents, while the canonical `AliasResolver.resolve` (used in `persons/05` at line 203) does, so accent variants of one name (for example `Bélanger` vs `Belanger`) hash to distinct `membership_only::` / `master_person::` ids and can surface as two historical persons the resolver would merge into one; whether this actually produces duplicates depends on the source data and must be checked against it. (2) Spaced-hyphen mismatch (verified): the two generators order whitespace-collapse and hyphen-replacement differently (`clubs/01` collapses after replacing, `persons/05` does not), so `"Jean - Pierre"` normalizes to `jean pierre` in `clubs/01` but `jean   pierre` in `persons/05`, and the load-time bridge `translate_membership_only_pid` in `legacy_data/event_results/scripts/09_load_enrichment_to_sqlite.py:116-164` (which keys on the normalized name) misses for that name, leaving the membership-only person's `historical_persons.historical_person_id` NULL. For ordinary names the two agree, so the bridge docstring's "consistent across both upstream scripts" holds in the common case and should be corrected to note these exceptions. Fix (owned by the data-and-freestyle maintainer; it changes some ids and needs a full pipeline rebuild): route name normalization in `clubs/01`, `persons/05`, and any other id generator through the canonical `alias_resolver.normalize_name`; correct the bridge docstring; and verify against the legacy data that an accent-variant pair collapses to one historical person and a spaced-hyphen name resolves to a non-NULL `historical_person_id`, with `legacy_data/pipeline/qc/run_qc.py` passing after the rebuild. Scheduling: this is a board task gated by the final-dump duplicate scan in the data-remainder item above. That scan is the checkpoint; if it surfaces real collisions the fix runs before the final production dump loads, so the production `historical_persons` universe is built on the canonical normalizer rather than loaded through a divergent one and repaired afterward; if the scan is clean, the task is board-sequenced like any other card. A read-only scan of the current data shows the defect is real at the source yet currently absorbed downstream: the legacy member export carries 765 accented names and 18 accent-fold same-person source pairs (one of them a spaced-hyphen-and-accent name that trips both defects at once), while the built `historical_persons` universe shows zero accent-variant duplicate pairs and only four spaced/doubled-hyphen names, because on this test dump the accent-variant members almost never reach the person universe. Those counts are test-dump-specific and will move on the final dump, and the zero is a property of this dump's membership-to-person overlap, not of the normalizer, which still diverges. The final-dump sign-off in the data-remainder item then re-verifies zero collisions on the production data.
- **[KANBAN] The persons-master id generator mints duplicate historical persons when one person arrives from two source types (a separate mechanism from the normalizer divergence above).** `stable_master_person_id(name_norm, source_types)` in `legacy_data/persons/scripts/05_build_persons_master.py:49-51` hashes the source-type string into the person id, and nothing merges same-normalized-name candidate rows across source types before hashing, so the same person arriving once via membership-derived and once via club-derived candidate rows mints two `master_person::` ids for byte-identical names. The one previously-confirmed instance ("Georg Waldispühl", one MEMBERSHIP-sourced and one CLUB-sourced provisional row) has been adjudicated and resolved: "Georg" was the formal name of the existing canonical competitor "Geri Waldispühl", who is recorded under that nickname, so the two stubs were merged into the canonical person through the alias registry rather than being a genuine cross-source split, and no confirmed instance remains in the current database. The reconciliation stage routes same-name multi-person cases to manual review, so member claims cannot silently attach to the wrong half; the exposure would be a split public person page. The mechanism fix (merge same-name candidates across source types before id minting) rides the same final-dump-gated slice as the normalizer unification above. The final-dump duplicate scan (data-remainder item) re-checks this class; if the final dump reveals multiple instances, revisit whether the pipeline fix moves forward.

- **[KANBAN] Remove the unused `contact_email` column from the committed club seed.** The `contact_email` column in `legacy_data/seed/clubs.csv` is empty in every row and is never populated or loaded into the database: `legacy_data/scripts/extract_clubs.py` writes it as `""` by design (club contact is leader-supplied, not carried from the mirror), and the cutover and enrichment loaders (`legacy_data/clubs/scripts/06_cutover_pre_populated_clubs.py`, `legacy_data/event_results/scripts/09_load_enrichment_to_sqlite.py`) intentionally never write it; the `has_contact_email` leader signal derived from it in `legacy_data/clubs/scripts/02_build_legacy_club_candidates.py` is therefore always false. Drop the column from `clubs.csv` and from the extractor, candidate-builder, and test references that carry it (`extract_clubs.py`, `02_build_legacy_club_candidates.py`, and the `contact_email` rows in `legacy_data/tests/test_loader_idempotency.py` and `test_clubs_url_verdict_stamping.py`), so the committed seed no longer carries a dead, email-shaped column. If a real club-contact email is ever wanted, it can be re-derived from the footbag.org dump. Done when `clubs.csv` and its producers and consumers no longer reference `contact_email`, the pipeline build runs clean, and the `legacy_data` tests pass. Mirror-blocked: clean removal needs `clubs.csv` regenerated from `extract_clubs.py`, but the local mirror's `clubs/show` tree holds only 1 of the 312 clubs, so a regeneration would lose 311 (the extractor's refuse-to-shrink guard blocks it). Do not hand-edit `clubs.csv`. Unblock when a complete club mirror is in the checkout; then the extractor edit plus a regen drops the column.

- **[KANBAN] Complete the archive mirror: crawler fixes, dump-derived seed lists, and gap investigations (prep for the final crawl).** The 2026-07-02 content-gap audit (its findings adjudicated and the report deleted 2026-07-16; the rulings live here) found the crawler's exclusion rules sound but its link handling and discovery incomplete. One card, three parts. (1) Fix the five crawler defects in `legacy_data/create_mirror_footbag_org.py`: the `forum-down.html` link mis-rewrite that breaks 18,987 references across all sections (the flat file is treated as a directory; the target content is present); ampersand double-escaping that breaks links to ~34 on-disk `.mp4` files and ~238 thumbnails (`Lisa&Andy` on disk, `Lisa&amp;amp;Andy` in links); the `events/ical` route never mapped in `url_to_filepath()`, so iCal export was captured for zero of 1,182 events; regenerate locally the 238 thumbnails whose full-size images are already in the mirror; and add the non-`2` `events/rm` and `events/edit` variants to `DESTRUCTIVE_ROUTES`. (2) Generate deterministic seed-URL lists from the dump databases for the content link-following never discovered, to feed the final crawl's seeded (non-link-following) fetch mode: the 367 Hall-of-Fame and historical-player photos under the old wiki's image store (first investigate why the crawl missed all 367 and whether the same cause hid any other content class); the 222 of 602 clubs absent from the mirror (investigate why; classify all 222 from `clubs/backups/latest.sql` into real, dormant, or junk; seed the real and dormant ones; route newly surfaced real clubs into the club-candidate classification, which also unblocks the `contact_email` removal card above); the 2,426 recoverable gallery items, including the 443 `SetVisible=0` sets that are index-hidden but directly servable, plus the 4 visible sets and 28 isolated images that failed fetch; the 7 of 10 rulebook chapters never fetched; the 9 of 11 missing polls; the `news/show/<NewsID>` permalinks (article text is already captured in the year-list pages; the permalinks preserve deep links); and the ranking pages as archive capture only (the platform-side rankings migration stays a deferred `docs/MIGRATION_PLAN.md` decision). Anything the live site no longer serves publicly falls back to a dump-derived rebuild (the photos have that fallback; gallery media does not, the dump holds no binaries). Done when the five fixes land with the crawler's test modes green, the seed lists are generated and committed as pipeline inputs, and both investigations record their root causes.
- **[BLOCKED] Run and audit the final FULL cutover mirror (James).** Ruled 2026-07-16: the final crawl is full, videos included, superseding the earlier videos-excluded plan for this run. Run `legacy_data/create_mirror_footbag_org.py <credentials> -fresh -log` against the frozen legacy site, seeded with the gap lists from the prep card above and with retention-on-failure enabled so the 969 magic-byte rejections from the previous crawl can be assessed once. The previous full crawl's multi-day duration was dominated by video download and re-encode, so start early enough that the run completes before the mirror must serve as `archive.footbag.org`, and the site freeze must hold for its whole duration. Then audit the run: review the generated manifests (sitemap, redirect map, failed-download log) and run the targeted reconciliation checks against the crawl outputs — classify failed downloads by area and extension, reconcile the dump's rendered-content seed rows (faq, news, events, clubs, from the local `*/backups/latest.sql`) against rendered mirror pages, and confirm each `v.footbag.org/media/...` reference localized. Package the mirror, logs, manifests, and checksums as the archive artifact. Subdomain scope facts already disclosed: `ifpa.footbag.org` and `list.footbag.org` are mailing-list mail, not web content. Unblock condition: production access plus confirmation that the legacy site is frozen and may be crawled at an approved rate.
- **[Q] Ratify the archive accepted-loss register (decide after the final crawl).** These exist in neither the dump nor the mirror, or are unrecoverable by policy; recording them as accepted prevents their being mistaken for crawl regressions later. The register: the discussion forum (down since ~2019; no thread content survives anywhere); the "Open de France 2005 - Finale Double Net" video, all three parts (the live site itself errored serving them; no bytes exist); the 7+ clubs already deleted from the live database before the snapshot (e.g. ClubID 1098109619, 1207008912, 1024950604, 1053011234, referenced from old news archives); the worlds2002 promo images and worlds2011-2017 WordPress theme assets (genuine 404s from abandoned installs); the 238 thumbnails whose full-size originals never existed; the 106 gallery audio tracks (excluded by the documented MP4-and-JPG-only malware-stripping media policy; reversing that requires a design-decision change); and whatever magic-byte rejections still fail under the final crawl's retention-on-failure assessment. IFPA election result pages are excluded from this register: they stay flagged-not-fixed under the vote-records disposition in Julie's section. Sequenced after the final crawl because the magic-byte assessment does not exist until then. Done when each register entry carries a recorded accepted or recovered disposition.

  Two audit findings need no card, recorded here so they are not re-derived: the old MediaWiki's article text is safely captured (606 pages in the mirror's `reference2/`; the dump holds no wiki page text, and its only unique wiki asset was the photo store now in the prep card); and no non-English localized pages ever existed to lose (the `localize` database is UI strings only, and locale was a session setting, not a crawlable URL). The group mailing-list and discussion-archive scope remains with the group-message archive item in Julie's section.

### Freestyle

**Surface propagation rule (definition of done for freestyle slices; a standing rule, not a task — do not delete when items close).** Any freestyle update — a promotion, a doctrine change, a classifier change, or a content backfill — must propagate to every affected surface before the slice is considered complete. A change is not complete merely because the source row was edited. Affected surfaces, as applicable: canonical trick data; aliases / duplicate archive; Emerging Vocabulary; the observational universe; tracked names; ADD analysis; trick detail pages; browse / search; operator / modifier pages; set pages; family surfaces; media / related-trick projections; metrics / counts / copy; generated content files (`src/content/freestyleObservationalUniverse.ts`, `freestyleTrackedNames.ts`, and their peers); and tests / QC gates. For every slice, explicitly verify one of: (1) all affected surfaces were updated and regenerated, or (2) a surface is intentionally unchanged, with the reason documented. This applies to human and AI work alike.
**Active freestyle work (single tracked authority).** This section is the
sole tracked source of truth for all remaining freestyle work; the retired
local planning records it replaced are gitignored, and nothing may cite them
as an active backlog. Every entry below is open; closed work is deleted,
never archived here.

#### James

- **[BLOCKED] Blank operational notation for the doctrine-blocked cohort.** Thirteen active canonical rows carry blank operational notation because their operator structure is unresolved: the blazing compounds (the blazing operator structure), the terraging chain (the terraging-chain arithmetic), and the flailing compound (`bill_ted_s_excellent_adventure`, the flailing confirm/deny). Each drops from the cohort as its Red ruling lands; none is an authoring task until then. The cohort is bounded and guarded by `legacy_data/tests/test_freestyle_incomplete_notation_cohort.py`, and every row's blank is intentional, not a gap. Unblocks with the corresponding rulings in the R1 scoring packet.
- **[KANBAN] Retire the legacy atomic-rotational computation path in `freestyleService.ts`.** Atomic is settled at +1 flat: canon is final, the public prose surfaces (operator reference, glossary, ADD-analysis, mechanical-delta, progressive-readings, and the atomic notation tooltip) are reframed to "+1 final plus conditional X-Dex," and the published data carries base+1 on every atomic-on-rotational compound (atomic-blender 5, atomic-drifter 4, atomic-torque 5 = atomic +1 + torque 4) with the single legitimate explicit-X-Dex exception atom-smasher (atomic mirage = 4, the named atomicX). The formula layer agrees. What remains is the computation code: `freestyleService.ts` still contains the legacy rotational-base branch (`ROTATIONAL_BASES` = whirl/mirage/torque/blender/swirl/drifter, and the atomic modifier's `add_bonus_rotational = 2`), which computes +2 for atomic on any rotational base. That now diverges from the published data for torque/blender/drifter, producing a diagnostic computed-versus-asserted mismatch, and is only still correct for atom-smasher (mirage). The open doctrine question is exact per-base X-Dex eligibility: read from the published data it is already implied (mirage eligible; blender/drifter/torque not), but whirl and swirl have no active atomic compounds yet, so their eligibility is undetermined. Done when the per-base X-Dex eligibility is settled and the `add_bonus_rotational = 2` plus `ROTATIONAL_BASES` branch is replaced by atomic = +1 plus an explicit per-base X-Dex eligibility table, with the computed-versus-asserted mismatch gone.
- **[KANBAN] Resolve the remaining guarded freestyle notation/rendering deviations.** Three of the four are resolved. Around-the-World direction: resolved by curator ruling (Around the World inward `SAME IN`, Orbit outward `SAME OUT`; corrected in the `CORE_TRICK_SPEC` render source, the tracked-name and resolved-formula echoes, and the curated atom/heel/double/triple notation, with regression coverage that the family carries no direction-neutral `SAME IN/OUT`). PassBack compound difficulty: resolved and staging-only (the rendered gallery was already clean); the six computed proposals lived only in the staging `curated/freestyle_media/video_snippet_candidates.csv`, the generator `legacy_data/tools/build_passback_intake.py` now emits `adds_proposed` blank, and a guard pins that no snippet carries a proposed difficulty. Butterfly default catch-side: the base atom already reads the far / opposite-side `OP OUT` in the stored data, the render source (`CORE_TRICK_SPEC`), and the `far_butterfly` alias, with `butterfly_same_side` the explicitly named `SAME OUT` variant; the side-either `SAME/OP` fold had already been superseded on the canonical row by the butterfly default-catch-side correction, so no atom was rewritten (the `SAME/OP` that remains is the legitimate symmetric leading-dex reading inside butterfly compounds, not the atom), and a DB-level guard now pins the stored atom to far-default. One remains: count and quantifier tokens (double, triple, surging, high) render like operators in the symbolic notation, carrying a Current/Target marker in `src/services/semanticNotationRendering.ts`; held until Red's count-quantifier ruling settles the classification, since rendering them distinctly would need a new symbolic token role, so the marker stays. Done when the count/quantifier ruling lands and its Current/Target marker is gone.

#### James + Dave

- **[KANBAN] FS-19. In-app curation cutover: staging rehearsals and launch
  approval.** The cutover feature is code-complete; what remains is the
  operator-run part. Dave (the authorized deploy operator) runs the two
  staging rehearsals that need AWS deploy and host access — a code-only
  deploy under which an in-app admin edit survives with the live database
  untouched, and a destructive-rebuild attempt refused before any mutation
  on a host carrying the post-cutover marker — then Dave and James record
  the shared launch approval. This is the actionable side of the
  persistent-database cutover-rehearsal gate in `docs/MIGRATION_PLAN.md`.
  Done when both evidence blocks are captured against the gate and the
  launch approval is recorded.
#### James + Red

- **[BLOCKED] R1. Scoring packet.** Urgency: doctrine blocker, the largest unlock.
  Status: sent — pending reply (James confirmed the send 2026-07-13). Its
  five ruling groups: blurry/paradox
  inheritance (61 gated audit rows; the 13 already-canonical blurry rows
  author independently but their blurry-named audit rows still gate on the
  alias-versus-distinct predicate); undefined or folk operators (9 tokens
  drafted covering 62 rows, plus a 19-token addendum covering 42 more:
  frootie, fyro, leaning, twisted, twisting, arctic, flailing, neutron,
  snapping, wonton, wrecking, flapper, floating, slicing, splicing, spyro,
  surfing, symp, zipper; the counts grew when the positional-equivalence
  ruling cleared its rows into these operator gates); double and
  count-quantifier scoring (22 rows); repeated same-operator use (4 audit
  rows plus the active canonical paradox_blur, a double-paradox whose empty
  notation waits on this answer); and terraging arithmetic (2 rows).
  Delivery map: three groups are drafted as paper sections;
  repeated-operator and terraging ride the queue letter's rider items; the
  19-token addendum was drafted after the paper's send and has since gone to
  Red as a follow-up message. Blocks: 193 gated
  rows plus the paradox_blur notation.
  Unlocks: the largest promotion band, plus the blurry
  registry-versus-reference consistency test Dave writes once the answer
  lands. Next: integrate Red's reply when it arrives. Done when Red's answers are
  integrated, the consistency test lands, and the audit tags clear.
- **[BLOCKED] R2. Notation packet.** Urgency: doctrine blocker. Status: sent —
  pending reply; the hygiene revisions were applied before the send (the settled butterfly question
  is struck; the cross-body question carries the clipper-terminal matrix
  evidence). Questions: the down-family embedded-base coordinate frame (21
  gated rows plus 3 shipped tension rows: shooting_star,
  tapping_double_over_down, venom); the cross-body rake base (12 rows now,
  a ~47-slug parser drain later); the osis spin-to-catch relationship (one
  audit row, Butterfly near Osis, now holds on it); the inward-gyro
  direction question (what "inward" denotes on a gyro: the three Inward
  Gyro rows plus Pixie Inward Gyro Mirage, withdrawn from author-now by
  the authoring-queue correction); the
  paradox-bracket record-versus-convention proposal; whether the cross-body
  bracket encodes opposite-side; and the path/catch terminal framing (103
  ambiguous-terminal rows plus 5 directional-syntax rows, framing only).
  Blocks: 40 promotion-relevant rows and the terminal-encoding standard for
  108 more. Unlocks: those rows and the standard. Next: integrate Red's reply
  when it arrives. Done
  when the answers are integrated.
- **[BLOCKED] R3. Rider list.** Urgency: doctrine blocker. Status: sent to Red as
  the rider list, pending replies; recorded in the
  Red queue's rider items. Riders: the
  implicit-paradox hypothesis (Blurrage, Predator, Schmoe; the sumo X-Dex
  value ships provisionally until ruled, and the same ruling unblocks
  Nuclear Ducking Mirage and its Sumo Thong twin, both held so neither
  ADD choice prejudges it); the Blink path-vocabulary
  exception; the jani-walker / sidewinder structure; the Motorfly memory
  check; the general osis-suffix rule; the twisting/twisted exemplar
  (counted in R1's token total); and the record names Double Dyno, Double
  Whip, Stepping Ducking Blurry Whirl, and Solestice (four of the five
  curator-review records; the fifth, Toe Spinning Toe, has an
  unavailable-footage disposition and rides here as a record-name
  question only); and the reported nine-ADD ceiling (an account relayed to
  James holds that a nine-ADD trick was landed, but the landing's
  cleanliness is disputed; the ask is the concrete record — which trick,
  roughly when, whether footage or witnesses exist, and why the landing is
  questioned — so the Frontier paper's ceiling note can move from
  anonymized testimony to a corroborated or retired entry; non-blocking,
  the documented ceiling stays at eight until the account is corroborated,
  and the public glossary already renders it only as anonymized qualified
  testimony that names no witness and claims no record). Blocks: the
  named rows and four record badges. Unlocks: those rows and badges. Next:
  integrate Red's answers as they arrive. Done
  when each rider is answered, ruled unrulable, or retired.
- **[BLOCKED] R4. History paper.** Urgency: external dependency. Status: sent to
  Red for the rules-era memory review; testimony and oral history only.
  Blocks: nothing (no promotion, doctrine, or launch item rides on it).
  Unlocks: the historical record only. Next: record any memory-review
  responses as they arrive. Done when the responses are recorded or the
  paper is closed as reviewed.

#### Other

- **[KANBAN] O2. Dave: mechanical test-comment de-epoch pass (~70 files).**
  Urgency: non-blocking cleanup; no Red dependency. Status: open. Strip the
  sprint, slice, and phase epoch labels from the test comments across the
  roughly seventy affected test files so each test comment states the
  long-term contract in plain words, per the code-comment standard in
  `.claude/rules/comments.md`. Repo-wide comment hygiene, not scoped to
  freestyle; the freestyle-surface code and test items that were bundled
  here have moved to the freestyle maintainer's freestyle code and test
  hygiene work. Blocks: nothing user-facing. Next: Dave
  picks up the pass. Done when no test comment carries an epoch label and
  `npm test` and `npm run build` pass.
- **[BLOCKED] O3. IFPA (Julie): Sick 3 competition-format rules wording.** Urgency:
  external dependency. Status: the freestyle-page rules buttons stay
  absent until the official wording lands. Remaining: the wording from
  IFPA. Blocks: the freestyle rules buttons. Unlocks: re-enabling them.
  Next: follow up with Julie, or James formally drops the buttons for V1.
  Done when the wording ships or the feature is formally dropped.
---

## Go-live MVP scope (Dave decides this last, after the questions above)

- [Q] **Decide what ships in version 1 and what waits.** This is the final scoping decision, and it cannot be made until every open question above is answered, because those answers determine what is even possible for launch. So it sits last in the plan.

---

## Blockers

Things we are waiting on from outside the immediate work.

- **[BLOCKED] The legacy member data, from Steve.** The final source for the member table; processed by the "Legacy-site dump intake" block in James's section, which carries the dump's scale, contract, and build behavior. Still open: the committee/board table (`ifpa_committees`, `ifpa_committee_members` — see Steve's section), the final dump taken after the site is frozen, and agreement that the member IDs line up. Several features wait on the real data: linking club-only members to accounts, claiming a legacy account at registration, re-seeding name variants, and showing the public member list (the public member directory is not yet built; the membership hub is at `/ifpa`).
