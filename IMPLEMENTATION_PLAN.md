# IMPLEMENTATION_PLAN.md

This is the single plan of record for the footbag.org modernization: every open work item and every open question we still need to resolve, grouped by who owns it. It is the clean baseline we will load into the go-live kanban board. Long-term design and rationale live under `docs/`; this file tracks what is left to do.

How to read it. Sections are ordered by owner, from the people we are coordinating with (Steve, then Julie) to the maintainers doing the build (Dave, then James, whose pipeline work carries the most detail). Each item is written to stand on its own: you should not need to open another document to understand what the task is. Every item must be pedantic for a human reader: state the task in full and unambiguously, name exact identifiers (file paths, route strings, schema objects, env vars) rather than vague gestures, give an explicit done-condition, and use no internal shorthand, finding ids, or labels a reader has not been given. An item a human cannot act on without opening another document or guessing what it means is not done. A reference to `docs/MIGRATION_PLAN.md` (the detailed migration design) or a file path appears only where it points at the actual work or saves copying a large table here. Finished items are deleted from this file rather than marked done.

Owners. Steve is the current footbag.org webmaster (he holds the legacy-site, DNS, email, and data-export facts). Julie is the IFPA secretary (she decides IFPA governance, groups, and votes). Dave is the project maintainer and manager (platform code, and he drives every open question to a decision). James is the historical-data and freestyle maintainer (the data pipeline and all freestyle content).

Tags. Each Dave and James item carries a category so this plan can seed the go-live kanban board directly: **[BUG]** a verified defect in running code; **[DEVIATION]** running code intentionally differs from the intended design, guarded, with a removal or unblock condition; **[KANBAN]** a forward feature, test, or gate that becomes a board card; **[PRE-KANBAN]** must be resolved before the kanban board launches and testers engage, to make the baseline trustworthy; **[BLOCKED]** waiting on external input (Steve, Julie, the IFPA, or rules expert Red). Steve's and Julie's items, and the final MVP scope, are open questions, marked **[Q]**. The Blockers, Release gates, and Deferred sections are context, not board cards.

What is and isn't an item here. Documented future work — a capability a canonical doc describes as design intent — is not a gap or a defect; it becomes an item only as a **[KANBAN]** card once scoped for build, and the plan never builds it unsolicited. Only a verified defect (**[BUG]**) or a guarded, intentional difference from the design with an unblock condition (**[DEVIATION]**) is tracked as drift. Current-versus-target status lives here, never in the canonical docs, which stay pure design intent.

---

## Steve — legacy-site coordination

Steve runs the current footbag.org and has agreed to the migration and cutover plan; what remains is the data and facts he still supplies. `docs/MIGRATION_PLAN.md` §19 (with the committee/board table in §28) holds the full background; this section tracks only the actionable status of what Steve owns.

- [Q] **Deliver the committee and board membership table** (`ifpa_committees`, `ifpa_committee_members`, delivered in `groups/backups/latest.sql`). It decides whether a member was on the board at cutover, which sets their starting tier; the board derivation ships inert until it arrives. Status: blocking go-live (data only). (MP §28.)
- [Q] **Supply the @footbag.org email and mailing-list usage facts.** Which addresses are actively read versus dead or spam-only, and any apex aliases configured in sendmail outside the codebase; every active address must exist on Google before the MX repoints, or its mail is silently lost. (The `list.footbag.org` Majordomo disposition moved to the subdomain ruling below.) A candidate inventory is already derived from the dump; this is the residual human judgment. Status: blocking go-live. (MP §19 items 21-22.)
- [Q] **DNS switch coordination facts.** Confirm the registrar and the authoritative DNS provider; whether keeping bind9 authoritative is a requirement only through go-live or durable (decides the optional later Route 53 move); and the reverse-DNS update path if the sending IP changes. The switch execution itself is the migration plan's cutover runbook. (MP §19 item 15, §29.12.)
  - Low priority, not a cutover dependency: confirm whether IFPA still owns `footbag.sport` and `footbag.us`, and whether to hold them defensively or let them lapse.
- [Q] **Disposition the legacy subdomains, one ruling per subdomain** — `sites.footbag.org` (the WordPress Worlds event sites), the Majordomo list host `list.footbag.org` (and whether a separate `lists.footbag.org` exists), and the `/reference/` MediaWiki: stay-on-legacy, retire, or snapshot-and-archive. A subdomain that stays live receives the platform's widened `.footbag.org` session cookie, so the cookie analysis re-verifies against the ruling. (MP §19 item 16.)

---

## Julie — IFPA governance

Julie is the IFPA secretary and supplies the governance decisions only she (with the Board) can make; Dave chases each one. The sensitive specifics (actual ballots and tallies) stay out of this public file. `docs/MIGRATION_PLAN.md` §20a (with §19 items 35-37) holds the full background; this section tracks the actionable status of what Julie owns.

- [Q] **Which legacy groups, committees, and lists are still alive,** and who runs, moderates, or would be affected by changing each, so the group and email-list inventory can be finalized. (MP §20a item 1, §19 item 35.)
- [Q] **Confirm the outbound mail-sender list**: every legitimate `@footbag.org` sender (Steve's sending host, any others) that the apex SPF record must authorize before the policy tightens. (MP §29.12 hand-applied record inventory.)
- [Q] **Disposition of the old IFPA vote records** (elections, issues, and the individually-attributable ballots): keep, archive, seal-and-encrypt, keep tallies only, or delete. A records-retention decision that is hers, not the operator's; the recommended path is encrypted-and-sealed and never published. (MP §20a item 2.)
- [Q] **How sanctioning actually works** (a deliberating committee or simple admin approval, and whether `sanctioning@footbag.org` is still read), which decides whether the platform's current simple-admin-approval design needs to become a committee workflow. (MP §20a item 3, §19 item 37.)
- [Q] **Where each group's conversation should live** (new platform, Google Groups, external chat, or retired); no in-app group feature is built until this is decided. (MP §20a item 4, §19 item 36.)
- [Q] **Whether kept groups need real moderators** with power over membership and posts, or whether informal admin and community order suffices. (MP §20a item 5.)
- [Q] **Who to notify** when a group or address is retired or archived, and how. (MP §20a item 6.)
- [Q] **Help run the IFPA Google Workspace** (groups, aliases, spam settings) once the kept groups and addresses are known. (MP §20a item 7.)
- [Q] **Ratify IFPA governance for platform-secret custody and administrative authority:** a System Administrator role with the Secretary's oversight; custody, separation of duties, and rotation for the operational secrets; who authorizes granting and revoking platform admin; bootstrap provisioning and total-admin-loss recovery; and audit and reporting to the Secretary. The technical mechanisms already exist (the admin lifecycle and secrets management in DESIGN_DECISIONS); this adds the IFPA authority layer, and per bylaws Article IV it also governs the Stripe-to-IFPA-bank authorization that Dave's Stripe activation depends on. (MP §20a; `GOVERNANCE.md`.)

---

## Dave — platform and project management

Dave owns the platform code and coordinates every open question to a decision. His own open questions and setup tasks come first, then the platform work, roughly in go-live order.

### Open questions and setup that Dave owns

- **[KANBAN] Run the project coordination.** Keep the action tracker and the shared stakeholder documents, drive every open question in this plan to a decision, write up user stories for the facts Steve and Julie supply, and route governance questions to Julie.

### Platform build and go-live items

Each item is tagged by category (see legend): `[DEVIATION]` an accepted substitute mechanism with a removal/unblock condition, `[BUG]` a verified defect, `[KANBAN]` unbuilt forward work, `[PRE-KANBAN]` to clear before the board launches and testers engage.

- **[DEVIATION] Internal QC tools must never reach production.** The operator-only QC tools under `src/internal-qc/` (reachable at `/internal/*` in development and staging) ship in the source but must not be reachable in production. Currently the web production image build (`docker/web/Dockerfile`) strips them out (it runs `rm -rf dist/internal-qc` and overwrites the route module `dist/routes/internalRoutes.js` with a null-router stub, `module.exports = { internalRouter: null }`, so app boot stays intact); the worker and image builds (`docker/worker/Dockerfile`, `docker/image/Dockerfile`) also run `rm -rf dist/internal-qc` and do not mount the `/internal` routes at all, so the QC tools are unreachable in those images too. They are removed for good when the QC subsystem is retired.
- **[DEVIATION] Retire the club-classification override CSV.** The admin work-queue actions that force-keep or force-junk a club against the live database are built, registered in `src/routes/adminRoutes.ts` and handled by `src/controllers/adminClubCleanupController.ts` over `src/services/clubCleanupService.ts`: `GET /admin/club-cleanup` (the queue), `POST /admin/club-cleanup/claim`, `POST /admin/club-cleanup/bulk-resolve`, `POST /admin/club-cleanup/bulk-delist-residue`, `POST /admin/club-cleanup/:clubId/resolve`, `POST /admin/club-cleanup/:clubId/contact-members`, `POST /admin/club-cleanup/:clubId/delist-residue`, `POST /admin/club-cleanup/candidates/:candidateId/promote`, and `POST /admin/club-cleanup/candidates/:candidateId/resolve`. Until the legacy pipeline stops rebuilding the database from CSVs on every reload, club-classification fixes are still hand-edited rows in `legacy_data/overrides/club_classification_overrides.csv` (James owns that procedure, below). Retire the CSV at the go-live switch to a persistent database, when the live-database actions become the sole path.
- **[BLOCKED] Activate real Stripe payments (needs Julie, the Board, and the IFPA bank account).** The live payment adapter is built (`src/adapters/paymentAdapter.ts` reads the API key lazily from Parameter Store), so going live is activation, not code. Blocked on the IFPA governance decision in Julie's section and, per bylaws Article IV, Board and Treasurer authorization to link the IFPA bank account to a Stripe account. The activation and professional end-to-end verification are the migration plan's live-payments gates (EX5: live keys and webhook secret in Parameter Store with one webhook delivery confirmed; EX8: the full go-live procedure). Sourcing `STRIPE_WEBHOOK_SECRET` from Parameter Store is part of that: today the operator pastes it into the server's env file by hand because no Parameter Store entry exists for it; add the secret to Parameter Store in both staging and production Terraform, grant the server permission to read it, have the deploy pull it from there, and update the DevOps guide.
- **[KANBAN] Build the apex redirector Terraform.** The ratified apex mechanism (MP §29.12): a $5/month always-on Lightsail instance with a static IPv4 that answers `https://footbag.org` with its own auto-renewed TLS certificate and 301-redirects every request to the same path on `https://www.footbag.org`, as its own Terraform under `terraform/production/`, with a smoke check (301 + `Location` + valid certificate). Done when `terraform plan` shows the redirector stack and the redirect smoke passes against the instance.
- **[KANBAN] Build the archive.footbag.org serving stack.** No archive resource exists in any Terraform tree while live code already redirects to the hostname (`src/controllers/legacyRedirectController.ts`, `src/controllers/clubController.ts`). Build in `terraform/production/`: the private archive S3 bucket behind Origin Access Control; the CloudFront public key + key group for signed cookies (private half in Secrets Manager or SSM SecureString scoped to the app runtime role); the archive CloudFront distribution (OAC origin, `trusted_key_groups` on the cache behavior, 1-year edge TTL, custom 403 redirect to the login page); and the `archive.footbag.org` ACM certificate in us-east-1. The serving contract and end-to-end auth test are MP §29.15. Done when `terraform plan` shows the stack and the end-to-end check (unauthenticated 403 → login redirect; authenticated 200) passes.
- **[KANBAN] Implement `scripts/deploy-migrate.sh`, the data-preserving deploy.** A stub today; the go-live model depends on it (MP §29.14, and the post-cutover fix-forward path assumes it). Implement the non-destructive code-plus-migration deploy against the existing live database (the working `deploy-code.sh`/`deploy-rebuild.sh` orchestration minus the DB rebuild, plus migration SQL application), and make the durability tests runnable against it on staging. Done when a staging deploy through it preserves curator content, email templates, and member rows with no seeder run.
- **[KANBAN] Harden lazily-resolved secrets and document production operations.** The live Stripe, Turnstile, and Safe Browsing adapters resolve their Parameter Store secret on first use by design, so a placeholder or wrong key boots clean and fails only under the first real request; optionally add a boot-time probe so a misconfigured secret fails at startup instead. (The KMS JWT-signing adapter is not in this set: it reads its KMS key ID from the `JWT_KMS_KEY_ID` env var at boot and calls KMS directly, so it has no Parameter Store secret to resolve or probe.) Separately, document in `docs/DEVOPS_GUIDE.md` the production-versus-staging cost deltas (the larger Lightsail bundle, longer log retention, dual-region replication, CloudFront) and the rotation runbook for the session and origin-verify secrets.
- **[KANBAN] Send the notification that waits on an unbuilt feature.** A notification named in a service header contract is not wired, because the action that fires it does not exist yet; it ships with its feature, not before.
  - **Event-organizer, participant, and sanctioning emails wait on the event-organizer feature.** `src/services/eventService.ts` documents these in its header contract; they arrive when that feature is built.
  - Done when each notification is enqueued, after its database transaction commits, by the action that triggers it and carries a firing test, at which point its clause here is deleted.
- **[KANBAN] Seed the remaining test personas as their features get built.** The test suite seeds a sample user for every account type whose feature exists. The still-missing ones are blocked on features that do not exist yet: event organizers, group owners and members, vote-eligibility lists, and a banned legacy member. Add each persona in the same change that builds its feature.
- **[KANBAN] Extend the accessibility scan beyond the public smoke set.** The axe WCAG 2.1 AA scan covers the high-traffic public pages. Remaining coverage: extend the axe scan to the important authenticated pages (dashboard, own profile and edit, member gallery, the admin surfaces), seeding a persona session as the existing end-to-end specs do. Done when the important authenticated pages carry an axe WCAG 2.1 AA scan and pass.
- **[KANBAN] Fix the site-wide colour-contrast WCAG 2.1 AA failures.** The axe scan reports `color-contrast` (serious) as the only WCAG 2.1 AA violation on every high-traffic public page, rooted in shared theme elements (the logo wordmark, the active primary-nav link, `.btn-primary`, the footer tagline), so it is a theme-palette change rather than per-page markup. The accessibility gate excludes the `color-contrast` rule today so it stays green on the structural checks; this item removes that exclusion once the palette meets the 4.5:1 (normal text) and 3:1 (large text) AA contrast ratios. Because it changes public visual design, agree the palette adjustments with the maintainer before editing CSS. Done when the theme palette passes the `color-contrast` rule and the exclusion is removed from `tests/e2e/accessibility.spec.ts`.
- **[KANBAN] Wire the remaining CI gates the testing standard specifies.** `docs/TESTING.md` §11.1 describes the CI-on-PR gate as including a per-PR dependency review (`actions/dependency-review-action` over the PR diff) and staging-safe security probes, plus a nightly/scheduled tier (mutation testing on the safety-critical short list, header-walk across the route table, dev-shortcut cutover audit). `.github/workflows/ci.yml` already runs dependency-audit, gitleaks, and CodeQL, but none of the testing standard's additions: there is no `actions/dependency-review-action` job (PR-diff scoped, distinct from the whole-tree `audit-ci --moderate`), no staging-safe probe job, and no `schedule:` trigger for the nightly tier. Add a job running `actions/dependency-review-action` and the staging-safe probes to `.github/workflows/ci.yml`, and add a separate scheduled workflow under `.github/workflows/` carrying a `schedule:` trigger for the nightly tier (reporting, non-blocking). Done when `.github/workflows/ci.yml` runs the dependency-review and staging-safe probe jobs on every PR and a scheduled workflow runs the nightly tier; delete this item then.
- **[KANBAN] Build the curator source-of-truth cutover tests.** At go-live the curator content source of truth switches from the `/curated/` JSON sidecars to the persistent production DB (the seeder stops running against production; admins author through the admin UI). Add special tests that pin this switch: curator `media_items`, galleries, and S3 bytes survive a data-preserving `scripts/deploy-migrate.sh` deploy with no seeder run; admin-UI create, edit, and delete is the sole authoring path and is durable without a seeder replay; and the seeder fails fast rather than orphan-deleting admin- and member-created rows if pointed at a persistent production DB. (Migration gate OR9, §29.14; DD §1.13.)
- **[KANBAN] Alert the remaining administrators to recruit when an administrator is lost.** New behavioral requirement supporting administrator continuity: when an administrator is lost, the remaining administrators receive a work-queue item and an email prompting them to recruit a new admin volunteer as soon as possible. This is a new behavioral surface, so it is specced in `docs/USER_STORIES.md` (extending `A_Manage_Admin_Role` or a new story) before coding. The death and resignation cases have clear triggers (a revoke through `/admin/admin-roles`, or a deceased mark on the member); the "stops working" case needs a defined inactivity signal (for example, no login within a configured window) plus a scheduled check to detect it, which is the open design question the spec settles. Done when the behavior is specced in USER_STORIES, built, and covered by a firing test; then delete this note. Address in a later session.
- **[KANBAN] Give operators an in-app way to monitor operational state, and stop the metric-feed log spam.** Today an operator can only watch system health (email-outbox backlog, scheduled-job success and failure, the dead-letter queue, webhook health, payment volumes, raised alarms) by reading the worker's raw structured-log stream, where high-frequency metric-feed lines can bury the signal. The designed views are specified but unbuilt: `A_View_System_Health` and `A_View_Audit_Logs` in `docs/USER_STORIES.md`, the operational-metrics cards on `A_View_Dashboard`, and the `src/internal-admin/**` admin tooling reserved in `docs/DESIGN_DECISIONS.md`. Build the operator-facing monitoring views so health is read in the app, not the log: outbox backlog and the dead-letter queue (the `email_outbox_paused` kill switch exists as a config key read in `communicationService`, with no admin view over it yet), scheduled-job run history with last-success age (the `system_job_runs` table already records each run's status, so this is a read view over existing data), and the alarm-acknowledge and audit surfaces those stories define (`A_View_Dashboard` already specifies the outbox-failure and unacknowledged-alarm summary, `A_Acknowledge_Alarm` the alarm-acknowledge view, and `audit_entries` is already written so `A_View_Audit_Logs` is a read view over it).
  - Developer ergonomics, same effort: make `./run_dev.sh` startup easy for a human to scan (a concise readiness banner, and/or a development-only human-readable log format), with a dev default `LOG_LEVEL` that hides the high-frequency `debug` lines.
  - Done when an admin can read outbox backlog, job-run health, the dead-letter queue, and recent alarms from an in-app view; and the dev startup output is human-scannable. Trace any view not already covered by `A_View_System_Health` or `A_View_Audit_Logs` to a `docs/USER_STORIES.md` spec before building. Address in a later session.
- **[KANBAN] Pin staging CloudFront TLS once it has a custom certificate.** Staging serves via the default `*.cloudfront.net` certificate, so its viewer TLS minimum is not pinned to `TLSv1.2_2021` as production is. When staging gets a custom domain and ACM certificate, set `minimum_protocol_version = TLSv1.2_2021` to match production.
- **[KANBAN] Resolve the last two staging/production Terraform parity items.** The two-tree parity audit is the migration plan's OR18 gate, and every divergence is addressed in HCL except two maintainer apply-time calls: (1) the staging `dr` bucket (with its versioning/encryption/public-access-block resources and the `dr` output in `terraform/staging/outputs.tf`) is marked a removal candidate but not yet removed, so either remove it or wire snapshot replication to it and record reset-tolerance; and (2) confirm the production first-admin bootstrap IAM parity (the production web-task role grants `ssm:GetParameter` and `ssm:DeleteParameter` on `/footbag/production/app/bootstrap/admin_token` and `kms:Decrypt` on the key that encrypts it). Done when both are resolved and `terraform plan` shows the intended state for both trees, satisfying OR18.

- **[KANBAN] Build and test the recurring-donation subscription handlers (pre-launch).** Recurring donations open at launch, so the subscription path is pre-launch, not deferred. Today `startDonation` and `cancelRecurringDonation` (`src/services/paymentService.ts`) throw `ServiceUnavailableError`, and the webhook dispatcher has no `invoice.payment_succeeded` / `invoice.payment_failed` / `customer.subscription.deleted` cases. Build the donation entry points and the subscription/invoice handlers to the state machine already specified in `docs/USER_STORIES.md` (`M_Donate`, `SYS_Process_Recurring_Donations`: active on `customer.subscription.created`, new payment row on `invoice.payment_succeeded`, `past_due` on `invoice.payment_failed`, `canceled` on `customer.subscription.deleted`), under the same professional-payments standard and production-only verification as one-time payments. Activation still depends on the IFPA bank/Stripe authorization tracked in the Stripe-activation item. Done when donations can be started and canceled, all four subscription/invoice events are handled idempotently, and firing tests cover each transition.
- **[KANBAN] Build the pre-launch payment-readiness pieces the live-payments gate requires.** The professional live-payments go-live gate (the live-payments row in the Migration Plan external-dependencies block) requires capabilities not yet built: a nightly reconciliation job comparing the Stripe ledger to the platform `payments` rows, writing `reconciliation_issues` rows for mismatches and missed webhooks, plus an admin view to resolve them (the `reconciliation_issues` table and config are seeded but have no producer or consumer; `docs/USER_STORIES.md` `SYS_Reconcile_Payments_Nightly`, `A_Reconcile_Payments`); and webhook-delivery-failure alerting (a CloudWatch metric filter + alarm in production and staging `cloudwatch.tf`, because recoverable webhook failures log at `warn`, below the error-only alarm, so a bad secret rotation would not alert until Stripe disabled the endpoint). Done when the reconciliation job and admin view and the webhook-failure alarm are built and tested, satisfying the live-payments gate.
- **[KANBAN] Make the member-tier ledger's current-tier resolution deterministic under a same-millisecond grant collision.** The `member_tier_current` view selects each member's latest `member_tier_grants` row by `(created_at, id)` — most recent `created_at`, with the larger `id` breaking a tie. Grant ids are UUIDv7 (`mtg_<uuidv7hex>`, minted by `newGrantId` in `src/services/membershipTieringService.ts`) whose within-millisecond suffix is random, so two tier grants written for one member in the same wall-clock millisecond order nondeterministically, and last-write-wins can pick the earlier grant. Production is unaffected: each tier grant is its own transaction and real tier changes for one member are seconds or more apart, so two never collide in a millisecond; the fragility only surfaces in a fast test driving several transitions back-to-back (guarded there by giving the member a single tier grant before the read). Harden the tiebreak so same-millisecond grants resolve in insertion order — a monotonic per-member sequence column, or a within-process-monotonic id — while preserving the cross-process id-minting property `newGrantId` documents. Done when two same-member grants written in one millisecond resolve to the later-inserted row deterministically and a test pins it.

### Gaps between what the site promises and what it does

Each item is a place where a shipped user story is not fully delivered. Delete it when the fix ships with a test. The name in parentheses is the user story it belongs to.

- **[KANBAN] Screen new passwords against a breached-password corpus.** Password validation enforces only length (8–128, no complexity), which matches the passphrase-friendly standard, but new passwords are not checked against known-compromised-password lists, which the same standard recommends. Add a breach check at registration and password change that rejects a known-compromised password with a clear message. This is new behavior, so spec the criterion into `V_Register_Account` / `M_Change_Password` first. Use the HaveIBeenPwned Pwned-Passwords k-anonymity range API (privacy-preserving: only a 5-character SHA-1 hash prefix leaves the server, never the password), with a short timeout and fail-open on outage (a breach check is defense-in-depth, not an availability gate); optionally add a small bundled top-N breached-password list as an offline floor so the most common passwords are still blocked during an API outage. Done when new and changed passwords are screened and a firing test covers a known-compromised rejection. (V_Register_Account, M_Change_Password)

- **[DEVIATION] Email content is hard-coded in code generators for now (accepted, to test current email scenarios).** Until the curated-JSON-then-database template pattern above is built, each email's subject and body come from a hard-coded generator in `src/services/emailContent.ts`; the email service's registry maps each `template_key` to its code generator plus its `pii_classification`, and that registry is the initial template set the current email scenarios test against. The generators and the registry carry `Current:` / `Target:` comments. Remove this deviation when the JSON sidecars seed `email_templates` and the email service renders from there.
- **[DEVIATION] The admin email-log page shows message metadata only, not the template body.** The per-message outbound-email log at `/admin/email-log` renders recipient, template, and delivery status, but not the message body. The intended design (the admin system-health story) shows the body as its underlying template with the data-merge fields left clearly unpopulated, so an admin can see what was sent without exposing the recipient's rendered personal data. Unblock: render the template body on the email-log page with its merge fields shown clearly unpopulated.
- **[KANBAN] Curate email templates as committed JSON pre-go-live, then cut over to the database at go-live, exactly like curated media.** Email-template content (`subject_template`, `body_template`, `pii_classification`, enabled flag) lives as committed JSON sidecar files pre-go-live, so templates are version-controlled, curated, and tested before launch; a seeder loads them into the existing `email_templates` table (the same shape as the curated-media `/curated/` sidecars seeded by `seed_fh_curator.py`). At go-live the source of truth switches to the persistent production database: the seeder stops running against production and admins author through the editor below, with the template rows surviving a data-preserving deploy. Capture this design in the canonical docs via doc-sync, a DESIGN_DECISIONS decision mirroring the curated-media source-of-truth decision and a MIGRATION_PLAN cutover gate mirroring the curator gate, and add cutover tests that pin the switch (templates survive a data-preserving deploy with no seeder run; admin-UI authoring is the sole post-go-live path; the seeder fails fast rather than orphan-deleting admin-edited rows). Done when the JSON sidecars seed `email_templates`, the cutover is doc-synced and tested, and the seeder is go-live-aware. (`email_templates` and its `pii_classification` column already exist in the schema with one template seeded; this item is the sidecars, seeder, and cutover, not the schema.)
- **[KANBAN] Give admins a simple email-template editor.** Building on the email service and the curated-JSON-then-database pattern above, let an admin edit an existing template's subject, body, enabled flag, and PII classification through one simple admin screen; the edited `email_templates` row is the live source the email service renders (pre-go-live it is seeded from the JSON sidecars; post-go-live it is the sole source). Each save writes an `audit_entries` row capturing the editing admin and the template changed. Keep it simple: edit existing templates only, no template creation and no arbitrary HTML. (SYS_Send_Email)
- **[KANBAN] Link the remaining freestyle pages to their galleries.** The media design's rule is that every freestyle media set is reachable as a gallery and linked from its page, through one consistent gallery UX (the grid tile, the tag-filter bar, the hero, and a "See all ... ->" link). The trick page already does this: `src/services/freestyleService.ts` shapes `referenceGalleryHref = /media/browse?context=<slug>`. The family browse view (the `?family=` view of the trick index), the freestyle landing page (route `/freestyle`, shaped by `freestyleService.getLandingPage()` in `src/services/freestyleService.ts` and rendered from `src/controllers/freestyleController.ts`), and `/freestyle/media` do not yet link into a `/media/browse?context=` gallery (the landing links only to `/freestyle/media`, which is itself not a gallery). Extend the same service-shaped gallery link to those surfaces so no freestyle media set dead-ends in a one-off render. (V_View_Gallery, V_View_Trick_Reference_Videos)
- **[KANBAN] Bring Net into the gallery model (Sideline optional).** The media hub's Net and Sideline cards are `href: null` (no gallery yet); the Net demo is fixed site content and the Sideline demos are static `.webm` chrome. Build a `#net` topic gallery (and, if wanted, `#sideline`) and wire the hub cards to it, so Net media is browseable through the same gallery/tag model as the rest of the collection. The Sideline `.webm` demos may remain static chrome by choice. (V_View_Gallery)

### Cross-track pickups (Dave writes the code; James advises on the freestyle and pipeline details)

These are freestyle or data-pipeline topics, but the actual change is in platform code, the test suite, or the `.claude` config, which is Dave's area. The skill items wait until James has audited the content.

- **[KANBAN] Tidy and relocate the freestyle skills.** Three freestyle-related skill files under `.claude/skills/` (`footbag-freestyle-dictionary`, `freestyle-dictionary-surface`, `freestyle-topology-governance`) carry dated history and machine-specific cross-references; clean each to a plain set of triggers and steps, and move the design rules they contain into the design docs. Move the `freestyle-dictionary-surface` skill out of the active skill folder, and fix its dead references: the cited `.claude/skills/club-leadership-surface/SKILL.md` skill and the `exploration/freestyle-dictionary-ux/` files (`EXPLORATION_CHARTER.md`, `FAMILY_PAGE_WHIRL_MOCK.md`, `TRICK_DETAIL_MOBIUS_MOCK.md`) no longer exist, and `footbag-freestyle-dictionary/SKILL.md` cites a non-existent `exploration/nonstandard-topology-audit-2026-05-24/AUDIT.md`; repoint or drop each. Do this after James audits the content; a few structural calls there are James's to make. Include in the rewrite the underscore-convention sweep of the kebab trick-slug examples in `footbag-freestyle-dictionary/SKILL.md` (the backtick-quoted slug tokens like `paradox-mirage` should read `paradox_mirage`), leaving non-slug concept tokens such as `tutorial-tier` and `same-side` unchanged; these examples live in the dated history this item relocates, so the sweep belongs in the rewrite rather than as a separate pass.
### Freestyle hashtag conventions and dictionary UX (docs and skills)

These pair with James's freestyle implementation items below; the design and config layers are Dave's.

- **[KANBAN] Record the difficulty-tag design once the PassBack compound tags are split.** After James splits `#passback_advanced` and `#passback_beginner` into a source tag plus a difficulty tag, record the design in USER_STORIES and DATA_GOVERNANCE: that `#advanced` and `#beginner` are their own concept and are never recombined with a source, the consistent rule (James sets it, anchored on the dictionary's ADD/difficulty signal) for which tricks carry them, that difficulty tags are their own tag kind, and the multi-tag named-gallery criteria that replace the compound tag. Record the constraint that the split preserves the visible two-gallery organization: the separate Beginner and Advanced PassBack galleries a visitor browses stay, behind the new multi-tag criteria or as an additive UI refinement, and the split never flattens the two galleries into one or drops the Beginner/Advanced grouping the visitor sees.

---

## James — data pipeline and freestyle

James owns the data pipeline (turning the old site's data into the new database) and all freestyle content, and he runs the onboarding QC pass below. This section carries the most detail. The QC pass and the cutover-critical pipeline work come first, then the freestyle work.

### Onboarding QC pass (platform code; moved from Dave's lane)

James runs the QC pass over the legacy member load and the onboarding surfaces. Start with the adversarial audit: paste the prompt file `JAMES_AUDIT_PROMPT.md` (repo root) into a fresh session and drive it to its `onboarding_review.md` report; the audit's questions come to James. The items below moved from Dave's lane with the pass; the audit's confirmed findings join them, and the prompt file is deleted when the pass completes.

- **[KANBAN] Enforce the homograph / confusable display-name constraint (Unicode UTS #39).** Script-restriction (reject mixed-script), confusable-skeleton collision against existing display names, and rejection of invisible / bidi-control / non-Allowed code points (NFC-normalized); store an indexed skeleton for the collision check. Enforce it wherever a display name is set — registration and profile edit alike (`validateRealName` / `validateDisplayNameSurname` today check only length, digits, word count, and surname match). Only the confusable-skeleton collision check needs an external Unicode confusables data table and a schema migration adding the indexed skeleton column; the mixed-script rejection, the invisible / bidi-control / non-Allowed codepoint rejection, and NFC normalization ship dependency-free using Node's Unicode property escapes. Ship those three first; treat skeleton-collision as a deferrable follow-on, and if wanted vendor the confusables data and compute the skeleton in-house rather than take a runtime dependency. (V_Register_Account, M_Edit_Profile)
- **[KANBAN] Analyze the bogus-club abuse vector opened by the create-club Active-Player bootstrap (needs analysis before building).** Because a never-AP Tier 0 member who creates their first club is granted the one-time Active Player period (and with it Tier 1 benefits), a member could create a throwaway or fictitious club solely to self-grant Active Player. Design a guard against this: an admin review of member-created clubs, and/or a viability assertion that a newly created club must attract at least one other joining member within some grace period or be flagged for review (with the bootstrap Active Player grant reconsidered if the club never gains a second member). This is dubious and needs analysis first — decide the review mechanism, the grace period, what happens to the Active Player grant and the member's tier benefits if the club stays a party of one, and how it composes with the existing periodic club-cleanup and club-viability-signal machinery rather than adding a parallel one. Follow-up. (M_Create_Club, M_Active_Player_Expiry, A_Periodic_Club_Cleanup)
- **[KANBAN] CL_Edit_Club edit scope.** Co-leaders can edit only description, external URL, and hashtag, not the club name, city, country, or logo the story grants; extend the edit surface, or narrow the story to the intended scope. Note when deciding: club name, city, and country are the cutover club-reconciliation keys, so extending edit to them interacts with the club-identity model and is not a purely additive UI change. (CL_Edit_Club)
- **[KANBAN] Close the catastrophic-surface test gaps on this surface.** Anti-enumeration equivalence + rate-limit tests for the anchor send-verification endpoint; timing-equivalence tests for password-reset and the claim lookup; concurrent-wizard-session race tests; a Case-C claimant profile-render test; and a behavioral append-only test that fires the `member_tier_grants` immutability trigger (the shared immutability helper is exercised on other ledgers but not this one); and a second-member re-claim of a purge-freed legacy row end-to-end (a legacy row whose prior claim was reverted or purged can be claimed by a different member). (M_Claim_Legacy_Account, M_Verify_Email, M_Complete_Onboarding_Wizard)
- **[KANBAN] Pin the club-confirmation wizard step's club ID with a test.** The stable club-ID generation is pinned by `tests/unit/stableClubId.test.ts` and the admin-promote action by its route test, but no test pins the literal club ID the onboarding wizard's club-confirmation step writes, so a regression in that write path would pass the suite. Add a route or integration test that drives the wizard club-confirmation step and asserts the exact club ID written. Done when that test exists and passes.

### Data pipeline

#### Legacy-site dump intake (the member data from Steve)

The one intake that reads the live legacy site's database export and lands it in the platform database, unified here so the whole task is visible in one place. **This intake is pre-kanban work and a key blocker: onboarding matching, legacy claims, and historical-person pages cannot be exercised against real data until it lands, so it is cleared before the board launches** (only the board / Tier-3 sub-item, which waits on external data, and the final post-freeze load, which waits on the cutover write-freeze, sit outside that deadline). Shared contract for every sub-item: the dump is the per-app MariaDB `mysqldump` set (`<app>/backups/latest.sql` in the webmaster's private repository), reached read-only through the git-ignored repo-root `footbag.org` symlink or a dump-root environment variable — never a path committed anywhere in this repo. Processing follows the established pipeline shape: extract to PII-bearing intermediate CSVs in the git-ignored `legacy_data/member_data_scripts/out/`, validate, dry-run load, then `--apply` load, with credential columns stripped at extraction and a header abort backstop in the loader. Completed claim state (`claimed_by_member_id`, `claimed_at`) is never disturbed by a re-import (covered by a test). The real-data load runs only on a maintainer machine as a cutover-migration step — never in CI, never against a persistent production database; a build with no dump present warns and skips, which is the correct state for local development and CI. Scale, measured against the delivered test dump: 33,665 member rows (25,655 with `MemberValid=1`), full date of birth on 20,607, primary email on 33,267 (`MemberEmail2`/`MemberEmail3` are empty across the whole dump), 520 Tier-1 and 318 Tier-2 statuses. The end state is a unified production database ready for onboarding matching, legacy claims, and historical-person pages: every dump account in `legacy_members`, every real dump-account-to-historical-person correspondence expressed as `historical_persons.legacy_member_id`, no duplicate historical persons, and the claim and auto-link surfaces reading all of it.

- **[BLOCKED] (5) Board / Tier-3 grant (needs Steve's committee table).** Granting Tier 3 to a member on the IFPA board at cutover: the two board columns on `legacy_members` (`legacy_was_board_at_cutover`, `legacy_board_underlying_paid_tier`) with their consistency CHECK, the board-to-`tier3` grant and `new_underlying_tier_status` derivation (including how a migrated Director's `tier3` relates to the governance ledger path `setGovernanceTier3`, `change_type 'governance_set'`), and the committee-table extractor with `BOARD_IFPA_TIER_CODES` kept empty until the codes are confirmed (do not guess). The derivation scaffold in `extract_legacy_members.py` is in place and inert; the committee table arrives in `groups/backups/latest.sql` (see Steve's section). The legacy admin PHP confirms `MemberIFPATier` is only 0/1/2 and does not encode a board tier, so that committee delivery is the sole unblock. The earlier idea of exercising the grant path now with a single known board-member fixture was found not executable — the board columns are not in the schema and the member record carries no board signal, so building the fixture would mean building the blocked scaffolding itself; the governance-path grant (`setGovernanceTier3`) is already covered by its own tests. Scope on its own once delivered.
- **[KANBAN] (6) Data remainder before the final production load.** Resolve the Hall-of-Fame and Big-Add-Posse honor worklist by linking each name against the authoritative public rosters (footbaghalloffame.net and bigaddposse.com, captured as snapshots in `legacy_data/inputs/`), then run the backfill (`extract_legacy_honors.py`) against the final dump — the read-only `report_legacy_member_honors.py` lists the ~37 Hall-of-Fame and the Big-Add-Posse names to link, or they miss their tier-2 grant; rerun the member-ID cross-check (`crosscheck_member_profile_ids.py`) against the frozen export; load the final post-freeze dump through the orchestrator, running the member extract with the confirmed go-live write-freeze date (the `--cutover-date` / `FOOTBAG_CUTOVER_DATE` input; Dave supplies it) so the Tier-1-annual flag derives against the real cutover moment — on the current test dump the flag is vacuously zero because every ever-paid Tier 1 is lifetime. Done when the final load passes the reconciliation gate and the data-review sign-off records it.
- Deferred, tracked in `docs/MIGRATION_PLAN.md` §28: the club cross-validation of the dump's authoritative `clubs` (602 rows) and `clubcontacts` (1,400 member-to-club links) tables against the mirror-derived club seed — a coordinated quality pass, not part of this intake.

#### Other pipeline work

Data-layer defects surfaced by the onboarding review; pre-kanban, because onboarding matching and the cutover load run on them.


- **[KANBAN] Re-verify the honor rosters before go-live.** The Hall-of-Fame and Big-Add-Posse flags are validated against captured roster snapshots. Shortly before go-live, re-run the read-only drift check (`diff_live_honor_rosters.py`) in case the public rosters changed, and refresh the snapshots if it reports real drift.
- **[DEVIATION] The club-classification override procedure (the CSV behind Dave's admin-cleanup item).** Before go-live, forcing a club to be kept or junked is done by hand-editing rows in `overrides/club_classification_overrides.csv`. The procedure: look up the club's key by name from the seed file (never guess it), fill in the name and reason so the row explains itself, re-run the club-classification build script and check the override count and the difference from the last run, then run QC. The loader deliberately fails loudly on an unknown category or a club key that no longer exists.

- **[PRE-KANBAN] Full audit and doc-sync of the post-refactor pipeline reality.** The legacy_data, freestyle, and net pipelines were refactored (the symbolic-grammar CSVs moved into `freestyle/`, moved-input reads were repointed, loaders were renumbered and some retired), so the prose that describes them has drifted from what the scripts now do. Do a full documentation-sync pass over everything that references the pipeline file layout and loader inventory: the root `CLAUDE.md` and any per-subtree `CLAUDE.md`, `README.md`, every `.claude/skills/*` and `.claude/rules/*` that names a `legacy_data`, `freestyle`, or net path, the pipeline script header comments, and the migration and data-model docs. Re-derive the actual current scripts and artifacts under `legacy_data/`, `freestyle/`, and the net loaders, then bring every reference into line with reality (correct loader numbers, current input and output paths, retired-loader removals). The blanket loader-invariant overstatements in `freestyle/CLAUDE.md` and `legacy_data/CLAUDE.md` are already corrected (and loader 08 now re-runs with scoped deletes, not a refusal); what remains for this item is the broad file-layout and loader-numbering drift across the other docs. Use the doc-sync skill and propose the doc edits for approval before writing.

- **[DEVIATION] The two person-id generators normalize names with local rules weaker than the canonical resolver, risking duplicate or mismatched historical persons (accents, and spaced/doubled hyphens).** Both id generators reimplement name normalization rather than using the canonical `legacy_data/pipeline/identity/alias_resolver.py:52-80` `normalize_name` (which applies NFKD accent-folding plus a defined punctuation/artifact strip), against the one-canonical-normalizer invariant: `legacy_data/clubs/scripts/01_build_club_person_universe.py:24-28` (`normalize_name`) and `legacy_data/persons/scripts/05_build_persons_master.py:30-31` (`norm_name`) both only lowercase, replace hyphens with spaces, and collapse whitespace, with no NFKD. Two consequences. (1) Accent-duplicate risk: neither id generator folds accents, while the canonical `AliasResolver.resolve` (used in `persons/05` at line 203) does, so accent variants of one name (for example `Bélanger` vs `Belanger`) hash to distinct `membership_only::` / `master_person::` ids and can surface as two historical persons the resolver would merge into one; whether this actually produces duplicates depends on the source data and must be checked against it. (2) Spaced-hyphen mismatch (verified): the two generators order whitespace-collapse and hyphen-replacement differently (`clubs/01` collapses after replacing, `persons/05` does not), so `"Jean - Pierre"` normalizes to `jean pierre` in `clubs/01` but `jean   pierre` in `persons/05`, and the load-time bridge `translate_membership_only_pid` in `legacy_data/event_results/scripts/09_load_enrichment_to_sqlite.py:116-164` (which keys on the normalized name) misses for that name, leaving the membership-only person's `historical_persons.historical_person_id` NULL. For ordinary names the two agree, so the bridge docstring's "consistent across both upstream scripts" holds in the common case and should be corrected to note these exceptions. Fix (data-and-freestyle maintainer's call: it changes some ids and needs a full pipeline rebuild): route name normalization in `clubs/01`, `persons/05`, and any other id generator through the canonical `alias_resolver.normalize_name`; correct the bridge docstring; and verify against the legacy data that an accent-variant pair collapses to one historical person and a spaced-hyphen name resolves to a non-NULL `historical_person_id`, with `pipeline/qc/run_qc.py` passing after the rebuild.
- **[DEVIATION · re-crawl only] Mirror-scraper robustness gaps that affect a future footbag.org re-crawl (the current mirror is already captured, so these are lower priority than the identity and QC deviations above).** Latent issues in `legacy_data/create_mirror_footbag_org.py` that matter only if the archival crawl is re-run: (a) session-expiry is detected by URL only (`'newauthorize' in final_url` at L2272) with no response-body check, so a member page returned as HTTP 200 with a login-form body would be saved as real content; (b) `_fetch_robots` (L339) collects every `Disallow:` line with no `User-agent:` scoping, ignores `Allow:` directives entirely, fetches robots over `https://` (L340) while the crawl is `http://` (`BASE_URL` L68), and uses the default requests User-Agent rather than the crawler's; (c) `PROGRESS_FILE` / `ROBOTS_CACHE_FILE` (L82-83) are bare relative names that resolve to the process CWD rather than the mirror directory, so a direct invocation from another directory restarts the crawl instead of resuming; (d) the four special-page handlers (~L2598, L2634, L2655, L2677) write with a plain non-atomic `open(...,'w')` after the URL is already marked visited (L2491), so a crash mid-write leaves a truncated page that resume will not refetch; (e) `save_content`'s SHA-256 content dedup (L2102-2107) silently drops a second URL whose HTML matches an earlier page, with no redirect stub or sitemap entry, losing distinct-but-identically-rendered pages. Done when: each item is fixed, or explicitly accepted in the script header with its reason.

- **[KANBAN] Remove the unused `contact_email` column from the committed club seed.** The `contact_email` column in `legacy_data/seed/clubs.csv` is empty in every row and is never populated or loaded into the database: `legacy_data/scripts/extract_clubs.py` writes it as `""` by design (club contact is leader-supplied, not carried from the mirror), and the cutover and enrichment loaders (`legacy_data/clubs/scripts/06_cutover_pre_populated_clubs.py`, `legacy_data/event_results/scripts/09_load_enrichment_to_sqlite.py`) intentionally never write it; the `has_contact_email` leader signal derived from it in `legacy_data/clubs/scripts/02_build_legacy_club_candidates.py` is therefore always false. Drop the column from `clubs.csv` and from the extractor, candidate-builder, and test references that carry it (`extract_clubs.py`, `02_build_legacy_club_candidates.py`, and the `contact_email` rows in `legacy_data/tests/test_loader_idempotency.py` and `test_clubs_url_verdict_stamping.py`), so the committed seed no longer carries a dead, email-shaped column. If a real club-contact email is ever wanted, it can be re-derived from the footbag.org dump. Done when `clubs.csv` and its producers and consumers no longer reference `contact_email`, the pipeline build runs clean, and the `legacy_data` tests pass. Deferred (mirror-blocked): a read-only audit confirmed `contact_email` is empty (312 rows, 0 non-empty) and unused (no `clubs` table column, no app or loader consumer; the only reference is the always-false `has_contact_email` signal in the candidate-builder). Clean removal needs `clubs.csv` regenerated from `extract_clubs.py`, but the local mirror's `clubs/show` tree holds only 1 of the 312 clubs, so a regeneration would lose 311 (the extractor's refuse-to-shrink guard blocks it). Do not hand-edit `clubs.csv`. Unblock when a complete club mirror is in the checkout; then the extractor edit plus a regen drops the column.


- **[KANBAN] Audit the legacy mirror for completeness against the legacy-site dump, build the targeted reconciliation checks, and run the final cutover-time mirror.** The static mirror produced by `legacy_data/create_mirror_footbag_org.py` (`legacy_data/mirror_footbag_org/`, ~27,500 rendered pages) becomes the archived `archive.footbag.org`, so it must capture all legacy footbag.org content worth preserving. A first-pass gap analysis against the legacy-site data-and-code dump the webmaster supplies is done; the dump is the legacy PHP engine plus SQL seed, not rendered content, so the comparison is content coverage, not a file diff. Remaining work:
  - Verify and close the four content risks the gap analysis surfaced: (a) the MediaWiki `reference/` tree renders only about twenty pages — confirm against the live database or the Internet Archive that it held no larger article set, and database-export or re-crawl it if it did; (b) triage the roughly 5,751 failed downloads in the crawl log and re-fetch the high-value misses (PDFs, videos, gallery originals), the largest concrete loss vector and invisible from the dump side; (c) decide capture-or-accept for the group mailing-list and discussion archives (only group pages and file attachments are captured today); (d) confirm the dropped non-English localized pages are an accepted loss, not an oversight. Record the pre-existing losses (the forum was already offline at crawl time; polls are thin) as accepted so they are not mistaken for crawl regressions.
  - Build the targeted reconciliation checks (a small script, not a full crawler-comparator): parse the crawl's failed-download log and classify misses by area and extension; reconcile the committed `*/backups/latest.sql` seed rows (faq, news, events, clubs) against rendered mirror pages so database rows that were never rendered are caught without the live database; and confirm each `v.footbag.org/media/...` reference resolved into a localized `/media/` file.
  - Run the final cutover-time mirror: it must be crawled from the frozen live site after the write-freeze, but the crawl takes several days, so it cannot finish inside a short cutover window — start it before the window opens, sequenced against the freeze, then audit the finished mirror against these checks before it serves as `archive.footbag.org`. Coordinate subdomain and special-content scope with the webmaster's legacy-feature disclosure (the `ifpa.footbag.org` and `list.footbag.org` hosts are mailing-list mail, not web; `v.footbag.org` media is already localized into the mirror).
  - Done when each of the four risks is closed or explicitly accepted, the reconciliation checks run clean or their misses are triaged, and the final post-freeze mirror is captured and audited complete.

### Freestyle

**Surface propagation rule (definition of done for freestyle slices; a standing rule, not a task — do not delete when items close).** Any freestyle update — a promotion, a doctrine change, a classifier change, or a content backfill — must propagate to every affected surface before the slice is considered complete. A change is not complete merely because the source row was edited. Affected surfaces, as applicable: canonical trick data; aliases / duplicate archive; Emerging Vocabulary; the observational universe; tracked names; ADD analysis; trick detail pages; browse / search; operator / modifier pages; set pages; family surfaces; media / related-trick projections; metrics / counts / copy; generated content files (`src/content/freestyleObservationalUniverse.ts`, `freestyleTrackedNames.ts`, and their peers); and tests / QC gates. For every slice, explicitly verify one of: (1) all affected surfaces were updated and regenerated, or (2) a surface is intentionally unchanged, with the reason documented. This applies to human and AI work alike.

**Current working order (curator-set) — the Editorial Release Phase.** Work is no longer executed as a numbered roadmap. Every slice must directly improve what a public visitor sees; internal cleanup, repository work, and pipeline documentation are intentionally deferred until editorial completion. Four phases run in order. Phase A, public encyclopedia polish, is editorial completion and the release bar; it runs as four ordered sub-phases plus a running usability pass — A1 foundational pages (Tier 1) — the four model pages Down, Swirling Whirl, Osis (made the model page, resolving its polarity contradiction), and Whirling Swirl, each teaching rather than merely describing (optimized for understanding, not completeness: teach first, reference second, documentation third), opening with the reader's question and the why-it-matters hook, answering the ten-question rubric, and leaving one memorable takeaway, as exemplars for every trick and family page; foundation-card connectivity and explanation-before-notation are A3 and a standing principle, not folded into A1), A2 history (rewrite the page; add BAP, Hall of Fame, FB World; add the timeline and references; link records and historical people), A3 encyclopedia connectivity (glossary, deep links, see-also, family/operator/movement/notation/history links), then A4 the public-honesty pass (remaining coming-soon, missing videos, broken links, records framing, badge cleanup) done last so it polishes a finished encyclopedia. A usability pass — read as an advancing intermediate freestyler, the encyclopedia's real audience — runs early and its findings feed A1 and A3. Each foundational and important page is finished only when it passes the four-audience review (a novice understands it, an advancing player learns from it, an expert trusts it, and an editor would recommend it — accessibility, education, authority, engagement, independently, the editorial standard replacing "technically correct"); the expert lens has two facets that both must pass, a doctrine-and-fact check against `freestyle/doctrine/*` and an editorial-authority review reading the page as a long-time freestyler for fairness, emphasis, and omission. The two release-bar gates before V1 is declared are the four-audience acceptance per page and the site-level Hour Test: browse the site for an uninterrupted hour as an interested freestyler and fix whatever makes you want to stop and edit, repeating until the hour runs clean. Phase B, dictionary completion — unknown notation, the no-Red blurry footbag.org subset, remaining deterministic authoring and aliases. Phase C, architecture — de-dup, authority modules, reference ownership, exploration consolidation. Phase D, repository — PRE-KANBAN, skills, pipeline docs, cleanup, backup pruning. The full phase definitions, the rubric, and the dependency reasoning are in `exploration/v1-completion-audit-2026-07-03/MASTER_ROADMAP.md` section 2a, which supersedes this summary.

- **[KANBAN] Author the missing freestyle-dictionary user stories (James drafts; Dave approves the canonical-doc edit).** The freestyle dictionary is the platform's largest deployed public surface — the mounted routes `/freestyle/tricks`, `/freestyle/tricks/:slug`, `/freestyle/search`, `/freestyle/sets`, `/freestyle/sets/:slug`, `/freestyle/glossary`, `/freestyle/operators`, `/freestyle/modifier/:slug`, `/freestyle/learn`, `/freestyle/observational`, `/freestyle/insights`, `/freestyle/notation-article`, `/freestyle/progression/*`, `/freestyle/competition`, `/freestyle/partnerships`, `/freestyle/leaders`, `/freestyle/history`, `/freestyle/about`, and `/freestyle/compositional-sets` in `src/routes/publicRoutes.ts` — but no `docs/USER_STORIES.md` story covers it: only `V_View_Tutorials` and `V_View_Trick_Reference_Videos` touch freestyle, and the deployed-surface rule (`.claude/rules/deployed-surface.md`) requires every deployed route to trace to a named story. Write the missing visitor stories in `docs/USER_STORIES.md` following the existing story pattern exactly: a `### V_...` header, an Access line, a `Story:` line in the form "As a visitor, I want to understand technical freestyle ... so that ...", then a `Success Criteria:` bullet list naming the concrete behaviors each page must satisfy. Scope the stories so each deployed dictionary surface (trick index and detail, sets, glossary, operators and modifiers, notation and insights, learning and progression, observational / emerging vocabulary) is covered by exactly one story or an explicitly shared story. James supplies the freestyle domain intent; the stories describe design intent, never implementation status. Editing `docs/USER_STORIES.md` is a canonical-doc change and needs Dave's explicit approval of the drafted text before it lands. Done when every mounted `/freestyle/*` route in `src/routes/publicRoutes.ts` traces to a named story in `docs/USER_STORIES.md` and the new stories carry success criteria in the existing format.
- **[KANBAN] Source a replacement video for the sole survivor trick (curator).** This trick's only curated video went private, so its page has no demo (the embed is hidden by the `#unavailable_embed` tag). The clip was originally attached under the structural name spinning symposium whirl; that duplicate row has since been retired and merged into sole-survivor, which kept the notation and aliases, so the replacement belongs on sole-survivor's page. A curator needs to find a live replacement clip to restore coverage. Note: the records loader (`10_load_freestyle_records_to_sqlite.py`) is additive (INSERT OR IGNORE, no DELETE), so edits to existing record rows only take effect on a fresh database build, not an in-place rebuild.
- **[KANBAN] Link the remaining unresolved world-record videos to their tricks (Red/curator input needed).** Of the 165 record trick names, about ten still do not resolve to any active trick or alias, so those records badge nowhere (the spelling and folk variants are wired, the mechanical residual is exhausted, and the same-side positional records were resolved under the ratified positional mapping). Two small buckets remain, each blocked on a human decision. **With Red (all included in the Wave 3 letter's rider list):** "Solestice" (an undefined "osis flapper" operator), "Double Dyno" and "Double Whip" ("double" is a structural prefix, and the "(rev)" reading is ambiguous), "Toe Spinning Toe" and "Blink" (degenerate structures with no exemplar), and "Stepping Ducking Blurry Whirl" (the folk name reads four operators at 6 ADD, the structured name two operators at 5). **With the curator:** five "(ss)" records whose same-side qualifier is ambiguous between two components — "Double Leg Over", "Eggbeater", "Fairy Double Leg Over", "Pigbeater", and "Smog" — plus "Pixie DSO (ss)", blocked until its base trick has operational notation; the per-row evidence is on the positional curator worklist at `exploration/positional-evidence-audit-2026-06-23/CURATOR_WORKLIST.md`. Separately, three "Unique N-Dex"/"Unique N-ADD" entries are consecutive-dex record categories, not tricks, and correctly badge nowhere.
- **[BLOCKED] Freestyle trick-promotion decisions (needs the curator and Red).** Everything independently derivable from the sources is promoted, and the curator's one-sitting micro-ruling session cleared the one-line-call queue (outcomes recorded at `exploration/curator-rulings-2026-07-02/RULINGS.md`); the live open-question list for what remains is `freestyle/doctrine/RED_QUEUE.md`. **With Red (the Wave 3 letter, see Blockers):** the blurry expansion predicate (when a blurry-named trick carries the paradox term, 64 rows plus ten deferred blurry-form aliases), the embedded-base labeling rule for down-family operator compounds (the one still-open half of the down question: Red has ruled the downs one family — a single structural decomposition with set/foot variants — which settles the grid itself; the three shipped base-label tension rows and any relabeling that depends on the frame stay deferred; evidence packet at `exploration/dod-ddd-evidence-pass-2026-07-02/RED_PACKET_A1_REVISED.md`), the structural definitions of blazing, flailing, slapping, and blistering (their ADDs are already evidenced), the cross-body rake base, the terraging chain rider, and the original rider list (repeated-operator scoring, jani-walker structure, the general osis-suffix rule, arctic, solestice, blink, motorfly). **With the curator, all that remains:** the POD-versus-Dimmier duplicate, which needs a video viewing to settle; the Kiwi source-internal contradiction; what Clipper Symposium Whirl is structurally; and the atomic-pickup alias target — "atomic pickup" is already omelette's alias carrying Red's own naming ruling (omelette = illusioning pickup, and illusioning later merged into atomic), while scrambled-eggbeater is the byte-identical structure, so rebinding it without an explicit call would silently overwrite Red-sourced naming. **Authoring-only remainder, no decisions needed:** operational notation for the nine promoted PassBack down-family folk tricks, and the five weaving stacks whose ducking mirror is not yet canonical (the fourteen whose ducking mirror was already canonical are promoted; each of the five authors when its ducking parent lands).
- **[KANBAN] De-epoch the inline labels remaining in the freestyle test files.** The comments rule requires test filenames, `describe`/`it` text, and file-header comments to state the long-term contract in plain words, with no sprint, slice, phase, wave, person-epoch, or dated labels. The filename-level group is done: the `promotion-slice{1..6}` cohort unit tests and the seven `freestyle.*` route tests whose names carried a slice / phase / date label were renamed to contract-stating names (`freestyleNotation.<structure>-cohort`, `freestyle.dictionary-glossary-cross-links`, `freestyle.dictionary-pedagogy`, `freestyle.glossary-structure-and-framing`, `freestyle.glossary-content-expansion`, `freestyle.browse-derived-families`, `freestyle.symbolic-notation-precedence`, `freestyle.dictionary-glossary-regression`), each with its header, `describe`, and `it` titles de-epoched. Remaining is the inline-only sweep: roughly 120 freestyle test files (about 23 unit and 100 integration) keep slice/wave/phase/date labels in body comments and `describe` text, for example `tests/unit/freestyleSymbolicEquivalences.test.ts` and `tests/unit/freestyleMovementSystems.test.ts`; this also covers the inline body comments left in the four just-renamed integration tests that still carry dated labels (`freestyle.dictionary-glossary-cross-links`, `freestyle.dictionary-glossary-regression`, `freestyle.glossary-content-expansion`, `freestyle.symbolic-notation-precedence`); the six `freestyleNotation.*-cohort` unit files are already clean. Leakage-prevention assertions that name the forbidden labels on purpose (a test asserting the rendered public HTML does not contain `Slice X` / `Wave-N`) are contract text and must stay. Some `Wave N` mentions refer to the rules-expert consultation rounds rather than a delivery sprint, so judge each. This item covers only the delivery-epoch labels (sprint, slice, phase, wave, person-epoch, dated) in the test filenames, `describe`/`it` text, and comments. It is kept separate from the freestyle reference-and-comment cleanup item below, which fixes a different defect in some of the same files, an embedded design-doc or section reference rather than a delivery label, and which is the home for the `src/content/freestyle*.ts` and `src/services/freestyleService.ts` source residue this note previously pointed at.
- **[KANBAN] Strip the embedded design-doc references from freestyle and curator comments, and repoint the removed-plan references (James).** A repository sync pass found design-doc and section references embedded directly in freestyle and curator comments, plus stale pointers to the removed `legacy_data/IMPLEMENTATION_PLAN.md` (consolidated into this single root plan). The standard for every edit in this item: strip the bare reference rather than relocate it; replace it with verified plain English that states the point, read and confirmed against the cited design source rather than guessed; where a pointer genuinely aids a source comment, name the target section by its durable title, never its number, which rots silently on renumber; and in test files strip the reference outright, because the test-comment rule permits no doc or section reference at all, not even a title. Three parts. (1) Eight freestyle test files embed doc or section references (`DD §`, `US §`, `glossary §`, `doctrine lock §`), about 25 in total, 18 of them in `tests/integration/freestyle.routes.test.ts` and one or two each in `freestyle.trick-kind-filter.routes.test.ts`, `freestyle.trick-compressed-from.routes.test.ts`, `freestyle.glossary-equivalence-topology.routes.test.ts`, `freestyle.dictionary-glossary-regression.routes.test.ts`, `freestyle.dictionary-glossary-cross-links.routes.test.ts`, `freestyle.avalanche-spike-hammer.routes.test.ts`, and `freestyle.add-analysis.routes.test.ts`; some `glossary §` / `doctrine lock §` hits point at an in-repo freestyle structure rather than a canonical doc, so verify each one's target before rewording. (2) The freestyle content and service source carries the same embedded-reference and planning-jargon residue in `src/content/freestyle*.ts` and `src/services/freestyleService.ts`; reword to verified plain English, and keep every `phase` / `stage` / `slice` that names a real pipeline stage rather than a delivery sprint, since blanket-stripping those would corrupt accurate pipeline-stage descriptions. (3) Three exploration scratch files still cite the removed `legacy_data/IMPLEMENTATION_PLAN.md` (`exploration/symbolic-grammar-2/SYMBOLIC_GRAMMAR_2_DESIGN.md`, `exploration/freestyle-notation-grammar/GRAMMAR_GLOSSARY_V3.md`, `exploration/freestyle-media-ingestion-2026-05-29/ARCHITECTURE.md`); repoint each to the root `IMPLEMENTATION_PLAN.md` by name or drop it as superseded scratch, and fold this into the `exploration/` go-live disposition pass if that lands first. The operator-facing pointer in `legacy_data/run_pipeline.sh` is already repointed to `runbooks/rebuild-identity-pipeline.md`, the accurate target for its missing-identity-lock-CSV error. A bare `DD §` locator sitting beside a complete self-contained explanation in freestyle or curator source is tolerated by the comments rule and is out of scope here. Done when the eight test files and the named source files carry no embedded doc or section reference, the three exploration files no longer cite the removed plan, and `npm run build` stays green.
- **[KANBAN] Decide the go-live disposition of the `exploration/` working corpus.** `exploration/` holds the freestyle working history (audit packets, decision packets, handoffs, curator notes) as roughly 1,000 tracked files across about 130 dated subfolders, committed at the repo root. Part of it is load-bearing: this plan and several `.claude/skills/*` cite exact files inside it (for example `exploration/doctrine-audit-2026-06-19/DECISION_PACKET.md` and `exploration/emerging-vocab-taxonomy-2026-06-20/UNDEFINED_OPERATOR_INVENTORY.md`), while most is superseded scratch. Before go-live there must be a plan for the directory: which packets are promoted into the canonical docs or the `freestyle/inputs/` and kept, which move into `exploration/_archive/`, and whether the corpus stays tracked at the repo root or moves out of the committed tree. Any relocation must repoint the citing references in this plan and in the skills in the same change so no path dangles.
- **[KANBAN] Trim the freestyle Claude Code skills: strip dated status and split the oversized one (James).** The freestyle-domain skill files carry dated implementation-status narrative that reloads into context on every invocation, which violates the comment / doc-governance no-status discipline and pushes one file past Anthropic's ~500-line `SKILL.md` ceiling. Fix each while preserving the domain invariants — strict layer separation, the source-provenance order (Red Husted, then curated dictionary, then footbag.org, then scraped), and the Red-wave governance rule kept as a timeless rule. (1) `.claude/skills/footbag-freestyle-dictionary/SKILL.md` (858 lines): move the dated "Foundational doctrine" status journal — the 2026-05-15 through 2026-06-06 curator-negotiation narrative, the superseded "supersedes the notes below" snapshots left in place, the Phase 1-10 delivery table, and the "Modifier Reference currently disabled" status — into `IMPLEMENTATION_PLAN.md` and/or a memory topic file; then, if the file still exceeds ~500 lines, move the durable layer procedure (Trick Dictionary / Modifier / Alias / Glossary / Sequence / Media / Navigation / Results sections) into a `REFERENCE.md` in the skill folder with `SKILL.md` as the router. A `REFERENCE.md` is a plain reference file, not a skill: no frontmatter, no command, never in the slash menu. Keep the Red-wave rule as a timeless rule; drop its dated "as of 2026-05-15" snapshot. (2) `.claude/skills/freestyle-dictionary-surface/SKILL.md`: strip the dated "2026-05-07 sandbox exploration" framing — the "Currently in / NOT in scope" section, the repeated dated provenance, and the closing "Status — Exploration-derived. Not production-shipped" section — keeping the timeless patterns; and fix the three dangling references to the deleted `.claude/skills/club-leadership-surface/SKILL.md` (remove or repoint them; that skill was deleted). (3) `.claude/skills/freestyle-topology-governance/SKILL.md`: strip the dated "Red-wave governance caution — as of 2026-05-15" snapshot and the bare "Slice D" forward-reference label, keeping the timeless doctrine. (4) `.claude/skills/footbag-curated-media/SKILL.md`: strip the three dated incident / reclassification notes (the 2026-05-06 whitelist-addition narrative, the 2026-05-10 reclassification note, the 2026-05-31 mid-run-failure narrative), keeping the worked examples as timeless. Done when no freestyle skill body carries a dated status marker or a superseded snapshot, `footbag-freestyle-dictionary/SKILL.md` is under ~500 lines (split into a plain `REFERENCE.md` if needed), and no reference to `club-leadership-surface` remains. The split follows the same plain-`REFERENCE.md` pattern already applied to the `bug-hunt` skill (a lean `SKILL.md` overview plus sibling reference files that hold the moved detail verbatim). The harness self-check `scripts/ci/assert_claude_harness.sh` currently allowlists these two findings as freestyle-maintainer-pending exceptions (its two `FREESTYLE_PENDING_OVERSIZE` and `FREESTYLE_PENDING_MISSING_REF` entries near the top of that script): completing this item includes deleting those two allowlist entries, so the self-check then enforces the ~500-line ceiling on `footbag-freestyle-dictionary` and confirms the `club-leadership-surface` reference is gone.
- **[KANBAN] Member Tips follow-ups: remap the unresolved recovered tips and mine the legacy text into the coaching fields.** The recovered footbag.org Member Tips are live on trick detail; the grouped review artifact `freestyle/reports/member_tips_unmatched.json` (gitignored) lists every non-published tip by bucket and slug so a curator can remap without re-running discovery. Remaining work: (a) author or promote the unresolved freestyle compounds (barraging-paradox-mirage, stepping-paradox-symposium-whirl, stepping-down-double-down, clipper-set-illusion, pixie-same-side-butterfly), after which their tips remap automatically; (b) resolve pogo (a frontier operator) and the atomic-double-over-down-versus-fusion identity question before remapping those two; (c) net tips wait for net technique pages; (d) highest-leverage: mine the recovered legacy descriptions (`freestyle/inputs/footbag_org_moves_metadata.ndjson`) and Member Tips to fill `execution_summary` / `learning_notes` / `prerequisite_notes` on the most-read trick pages (target the ~200 most-read pages, not all tricks), rather than just replacing `description`; (e) the v2 candidates (author attribution behind the public-individual-names governance decision, per-trick records surfacing, description reconciliation) stay deferred. Re-running loader `freestyle/loaders/27_load_trick_tips.py` requires a fresh DB build (it is wired into `run_freestyle.sh`).
- **[KANBAN] Four tricks are deferred for Technique Notes because no honest source exists (not skipped work).** Phase B populated the coaching fields (`execution_summary` / `learning_notes` / `prerequisite_notes`) across the most-read trick set, but left these four blank on purpose, each with a specific unblock condition: `atomic_reverse_guay` — no instructional source; the available evidence is only the ADD / decomposition. `paradox_whirling_swirl` — structural identity is known, but there is no trustworthy execution description. `quantum_guay` and `quantum_legover` — the quantum modifier's physical mechanics are not documented well enough to coach; this is tied to the open "Quantum and Miraging: one set or two?" question in the Deferred / parked section. Unblock each by supplying a trustworthy execution source (a legacy footbag.org description, a recovered Member Tip, or curator prose), or for the quantum pair by settling the quantum modifier's mechanics; then author the three fields. Done when each of the four carries source-grounded Technique Notes, or is confirmed to have no authorable source and removed from this list.
- **[KANBAN] Tag the BAP Individual Shred Collection clips by trick and set (human viewing pass).** The 72 curated player videos in the era-grouped BAP galleries are full multi-trick shred routines with no per-video trick data, so trick and set tags cannot be derived from metadata and must not be inferred; a curator watches each clip and applies the per-trick/set tags. The collection's remaining membership gaps (3 HOLD cases, 7 members with no known video, 1 rejected source) are intentional, not defects, and need no action.

### Freestyle hashtags and dictionary UX

- **[DEVIATION] Count-bearing and quantifier tokens render as operators in the symbolic notation.** In `src/services/semanticNotationRendering.ts` the count / quantifier tokens (double, triple, surging, high) are grouped with operators and render identically, instead of carrying a rendering that distinguishes their count / quantifier semantics. Unblock: give these tokens their own rendering path so the symbolic notation renders count / quantifier semantics distinctly from operators.
- **[DEVIATION] The PassBack galleries use compound difficulty tags instead of separate concept tags.** `#passback_advanced` and `#passback_beginner` intentionally fold a source and a difficulty into one tag to mark the two named galleries, while the intended design keeps separate `#passback` plus `#advanced` or `#beginner` tags. Unblock: split them, define and apply a consistent rule (anchored on the dictionary's ADD/difficulty signal) for which tricks are advanced or beginner, update the tag labels and tag classification so the difficulty tags are their own kind rather than defaulting to trick, and rebuild the two PassBack named galleries on the multi-tag criteria so their contents are preserved. Hard constraint: the visible two-gallery organization (the separate Beginner and Advanced PassBack galleries a visitor browses) stays. The tag split is a metadata change behind those same two galleries, or an additive UI refinement; it must not flatten the two galleries into one, drop the Beginner/Advanced grouping, or otherwise reduce the organization the visitor sees.

---

## Go-live MVP scope (Dave decides this last, after the questions above)

- [Q] **Decide what ships in version 1 and what waits.** This is the final scoping decision, and it cannot be made until every open question above is answered, because those answers determine what is even possible for launch. So it sits last in the plan.

---

## Blockers

Things we are waiting on from outside the immediate work.

- **The legacy member data, from Steve.** The final source for the member table; processed by the "Legacy-site dump intake" block in James's section. The test dump has arrived as a full per-app dump (members and admins, the IFPA payments and governance tables, clubs, events, gallery, news, and more, catalogued in `docs/MIGRATION_PLAN.md` §28) and holds 33,665 member accounts (25,655 valid); the committee/board table (`ifpa_committees`, `ifpa_committee_members`) is the one piece still to come (see Steve's section). Still open: the final dump taken after the site is frozen, and agreement that the member IDs line up. On a maintainer machine with the dump present, `./run_dev.sh --all-data` now applies the full member load locally; a build with no dump (any other developer, CI) keeps the mirror-derived pre-seed (~2,505 `legacy_members` rows with names only — no emails, no dates of birth). Several features wait on the real data: linking club-only members to accounts, claiming a legacy account at registration, re-seeding name variants, and showing the public member list (the public member directory is not yet built; the membership hub is at `/ifpa`).
- **The freestyle rules wording, from the IFPA.** The official wording for the Routine, Circle, Sick 3, and Shred 30 formats. It re-enables the rules buttons that were removed from the freestyle page.
- **The remaining Wave 3 answers from rules expert Red Husted.** Five doctrine questions were sent 2026-07-02 after an audit removed everything answerable from the written record. His first follow-up has landed and is integrated: the downs are one family, a single structural decomposition with set/foot variants. Still open: the blurry expansion predicate (the largest gate, 64 rows), the embedded-base labeling frame for down-family compounds, four operator structural definitions, the cross-body rake, the terraging chain, and the rider list. The terraging chain rider still needs send confirmation: the committed audit refers to an email draft that was never committed, so nothing in the repository proves that rider actually reached Red — confirm it was sent (or send it) before treating it as pending with him.
- **The data-review sign-off.** A confirmation that the legacy data is complete and the member list has been reviewed, recorded as an audit entry with James as the actor. Legacy-data pages must not go to production without it.

---

## Release gates

The checks that must pass before the data and platform are considered ready. The authoritative list of go-live gates is the go-live blocker index in `docs/MIGRATION_PLAN.md` — the single source of truth for what blocks cutover. The checks below are the data-and-platform QC and test checks this plan tracks day to day; each is owned by a pipeline QC script or a standing test, not a separate cutover gate.

- Data integrity: QC passes with zero hard failures; no unexpected drops in row counts versus the last locked version; no new spikes of missing person IDs on participants.
- Workbook: the event count matches the canonical event list; no empty year sheets; world-championship events are labelled.
- Identity: no duplicate canonical persons; no aliases leaking into the persons list.
- Platform database: the event count matches the canonical list; sample event pages load; player pages resolve with no orphaned IDs.
- Club cleanup: the stable club-ID generation is pinned by `tests/unit/stableClubId.test.ts` and the admin-promote action by its route test.

---

## Deferred / parked

Real work, but nothing is being done on it now.

- **Retire the inline preview card (consolidate on `GET /dev/outbox`).** The dev-only outbox route is built: `GET /dev/outbox` lists the full captured buffer newest-first, merging the web process's captured mail with the worker process's over the internal-secret IPC channel (the outbox drain runs in the worker, so its captures are otherwise unreachable from the web process). Industry practice surfaces captured dev/staging mail through such a separate inbox route (Mailpit / MailHog / letter_opener_web), never injected into the triggering response. Password-reset and legacy-claim links are already read from `/dev/outbox`; the inline card is still rendered on registration check-email (scoped to the session's own address). Remaining work: retire that card and read its link from `/dev/outbox` too.
- **Ballot retention/cleanup.** The soft-deleted-records cleanup job covers members and payments; it deliberately preserves ballots and never auto-deletes them, because destroying IFPA vote records is an IFPA governance decision rather than an operator action. Ballot retention/cleanup activates when the voting feature ships and the IFPA defines a destruction policy. (SYS_Cleanup_Soft_Deleted_Records)
- **There is no in-app way to edit the trick dictionary.** The freestyle trick data is read-only while the site runs; only the pre-launch pipeline writes it. Once the live site is taking member writes, that rebuild-from-pipeline approach cannot run against production, so the dictionary cannot grow until there is an admin editing screen, probably modelled on the existing curator-media admin tools. A freestyle-curation mechanism following the same source-of-truth cutover pattern as curated media and the email templates (content curated before launch, then a switch to admin-UI authoring against the live DB at go-live, with no pipeline rebuild against production) is planned, so freestyle content can be curated post-go-live without a pipeline rebuild. The same read-only limitation applies to records, consecutive-kicks records, and events.
- **Glossary version 4 multi-layer rollout.** Waiting on curator triage and a static-page rollout.
- **Symbolic-grammar UI rollout.** Phase 1 waiting on approval.
- **Local MP4 trick clips.** Need a way to ingest video files that are not embeds.
- **Movement Systems completeness audit.** Inventory every first-class movement concept (Flying, Alpine, Zulu, Weaving, Tapping, and the like), decide whether each has a natural browse surface, then make one coherent taxonomy update instead of adding isolated categories. Gated behind the current freestyle doctrine work. Prompted by the Alternative Surfaces cleanup, which dropped Flying because it is a body-movement concept, not a terminal surface: Flying carries no modifier links today and rides no movement axis. Flying now has a dedicated Movement Neighborhood (the topology view) as the canonical browse surface for Flying/airborne body-component tricks; it is populated from operational notation (the FLYING / DOUBLE KICK / DOUBLE KNEE body tokens), not by name matching. Flying remains a platform-tracked movement concept, not a teaching-page concept. Its peers (Alpine and the like) still surface only via By Family and By ADD. Do not add a one-off Flying/airborne modifier or axis ahead of this audit; the neighborhood is a topology grouping, not a new modifier or movement-system axis.
- **Quantum and Miraging: one set or two? (curator / Red question).** Both are inward op-side counterparts of atomic (atomic is the outward-dex peer; the data records `REV(0) miraging = atomic`). They are currently treated as distinct first-class sets: Quantum is a terminating toe set (`TOE > OP IN [DEX] >` op-side), Miraging omits the terminal and generalizes the entry (`SET > OP IN [DEX] >`), and the platform records different compound families for each (Quantum to frantic; Miraging to drifter and double-leg-over). Their bare inward-dex skeletons are close enough that a future ruling may merge them the way Illusioning folded into Atomic. The Quantum and Miraging set detail pages cross-link each other noting the similarity. Until ruled, keep them distinct.
- **Set Encyclopedia educational migration (resumed for settled sets).** The per-set educational teaching page is built and proven: `src/services/symbolicSetEducation.ts` holds the frozen set template (What it is, Why it exists, How it launches, JOB notation, Where it appears, How it composes, Progression, Representative tricks, Common confusions, Related concepts, Launch notes), `src/views/freestyle/set-detail.hbs` forks to render that teaching layer above the structural reference sections on a migrated set and suppresses the thin Formula and Movement sections it absorbs, and `src/controllers/freestyleController.ts` 301-redirects `/freestyle/modifier/<set>` to `/freestyle/sets/<set>` for any set that has an authored page. Stepping is the reference implementation (`/freestyle/sets/stepping`), and seven further settled, non-ambiguous sets are now authored on the same template: Pixie, Fairy, Whirling, Swirling, Floating, Surfing, and Warping. Authoring proceeds set by set as each is settled; this item tracks the sets still queued and the sets held by the gate. Classification rule applied throughout: a concept is a set or an operator by its compositional role (does it launch a trick or modify an existing one), not by the mechanic it embeds. Before any set page is authored it must pass the platform-canonical gate: the concept has a `src/content/freestyleCanonicalSets.ts` entry whose `source` is not `holden-only` and is not a frequency or purely historical artifact. Still queued (pass the gate, not yet authored): Atomic, Miraging, Quantum, Barraging (Furious), Nuclear. Held by the gate (do not author a set page; keep as historical or component cross-references): Sailing, Shooting, and Railing (their set readings are `holden-only`), and Rooted and Splicing (set-type modifiers with no canonical-set entry). Zulu and Weaving's example sections are populated: their canonical single-modifier base layer is promoted (24 compounds across twelve ducking bases, each mirroring the matching ducking compound's ADD and decomposition with a non-scoring annotation for the distinguishing launch detail), so both set pages auto-derive twelve example tricks from the modifier links. Fourteen multi-operator weaving stacks whose ducking mirror was already canonical are now promoted under the same mirror rule; the remaining Zulu/Weaving frontier is intentionally held: the five stacks whose ducking mirror is not yet canonical, folk names, and positional variants. Done-condition: every queued set has a page on the template, and the held concepts are documented as cross-references rather than pages.
- **Merging the two canonical result sets.** Combine the pre-1997 and post-1997 sets and retire the older one.
- **Version stamps in the data outputs.** Add the build version, build date, and identity-lock version to the workbook and canonical files.
- **A data-notes sheet in the workbook.** Explaining excluded events, sources, and what "unknown" means.
- **Score-text free-text descriptors.** Extracting the multilingual "sick"/"routine" free-text descriptors from raw result text; sparse and low-value, and would mean changing the core text parser.
- **Stricter workbook event accounting.** A check that the year sheets and the excluded-events list exactly account for all events; not currently computable because the year sheets list event names rather than keys and about 9 sparse events appear on both.
- **Surface the override flag in the data.** Expose an "overridden" flag in the canonical data and the database itself (the workbook already lists overridden events).
