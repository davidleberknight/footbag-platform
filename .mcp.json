{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": [
        "-y",
        "@playwright/mcp@0.0.80",
        "--headless",
        "--no-sandbox",
        "--browser",
        "chromium",
        "--config",
        ".claude/playwright/config.json"
      ]
    }
  }
}
