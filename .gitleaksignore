# Verified false positives already published in git history. The working tree
# no longer contains these strings; the entries exist because a full-history
# scan still reaches the commit that introduced them, and history is not
# rewritten. Each line is one exact finding: commit, file, rule, line. A
# fingerprint cannot mask a recurrence in new code, which a regex allowlist
# would; anything the scanner reports outside this list is a real finding.
#
# A prose comment describing address-keyed bounce and complaint state. The
# generic-api-key rule read the label-then-phrase shape of that sentence as a
# key being assigned a high-entropy value. No credential was ever present.
017c323b905e0d0dba62eec8e0502767b9cbc0e1:src/db/db.ts:generic-api-key:5310
#
# The entry above was once explained by quoting the flagged phrase verbatim, so
# the same rule fired a second time on this file's own comment. Explanations
# here paraphrase instead of quoting, to keep the allowlist from seeding new
# findings.
e7eb26a66945672e8c60d22667eb9b3375b59d70:.gitleaksignore:generic-api-key:9
#
# A media fixture helper. Converting a hand-rolled insert to a factory call moved
# two object-store paths from SQL column names into object properties, so a field
# whose name ends in the credential-ish word was assigned a string carrying a
# generated id, and the rule read the randomness as a key value. The paths are
# synthetic and no credential was present. The working tree no longer has the
# shape: both values are built into locals named for what they are, a path, and
# assigned from those.
bdb92337e2ec72ed8aedbfa36a6db67da24d76fd:tests/integration/media.routes.test.ts:generic-api-key:67
