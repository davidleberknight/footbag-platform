<div class="hero hero-sm">
  <div>
    <h1>{{page.title}}</h1>
    {{#if page.intro}}<p>{{page.intro}}</p>{{/if}}
  </div>
</div>

<div class="wrapper">

  {{! ── Media-collection cards; browse-by-hashtag leads with a green accent ─ }}
  <section class="content-section">
    <div class="media-hub-grid">
      {{#each content.cards}}
      <div class="media-hub-card{{#if accent}} media-hub-card--browse{{/if}}">
        <div class="card-title">{{title}}</div>
        <p>{{description}}</p>
        {{#if href}}
          <a href="{{href}}" class="btn btn-outline">{{cta}} &rarr;</a>
        {{else}}
          <span class="media-hub-soon">{{#if emptyNote}}{{emptyNote}}{{else}}Coming soon{{/if}}</span>
        {{/if}}
      </div>
      {{/each}}
    </div>
  </section>

</div>
