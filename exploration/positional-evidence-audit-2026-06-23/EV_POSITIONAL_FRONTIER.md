# Emerging Vocabulary — positional frontier (2026-06-23)

Tracking view of the positional record-name frontier. This is the resolution
ledger; it is NOT the rendered `/freestyle/observational` page. The 8 resolved
slugs are not present in the observational universe (verified 0/8), so the
rendered EV page needs no regeneration — these were record-link resolutions, not
universe frontier entries.

## Held buckets (sub-statuses)

The positional frontier uses these held sub-statuses instead of one flat
"unresolved":

- **Distinct variant candidate** — multi-component, unique target, new config;
  needs a curator author-or-alias decision.
- **Ambiguous multi-component** — the qualifier has >=2 candidate targets; needs a
  target decision before any identity is assigned.
- **No notation** — base has no operational notation; blocked until authored.
- **Awaiting Curated Equivalence** (NEW) — a fixed/redundant-looking
  multi-component configuration that the resolver may NOT autonomously collapse to
  the base; held until a curator asserts the equivalence. (Currently 0 among the
  14; a held category for future multi-component fixed-looking cases.)

## Current positional outcome

Resolved:
- 7 curated positional aliases (single-component or curator-approved equivalence)
- 1 collision alias — `Fairy Legover (ss)` -> `Double Orbit` (structural equivalence)

Held:
- 7 distinct variant candidates
- 5 ambiguous target cases
- 1 no-notation case (`Pixie DSO (ss)`)
- 0 awaiting curated equivalence

## Final EV summary — positional frontier

```
Resolved by curated alias ............. 7
Resolved by collision equivalence ..... 1
Distinct variant candidates ........... 7
Ambiguous multi-component ............. 5
No notation ........................... 1
```

Total 21 affected positional record names: 8 resolved, 13 held.
