# Foundational Notation-Backfill Mini-Wave (2026-05-23)

Implements the safest subset of the 38-row Wave Alpha notation-backfill
checklist as canonical-DB corrections. **CSV authored + applied to local
DB via the existing loader; full test suite green (4,122 passing).**

## What was implemented

15 doctrine-quiet copy-as-is canonical-bracket rows from
`exploration/promotion-cohorts/wave_alpha_notation_backfill_2026-05-23.csv`.
Filter cascade:

- `frontier_tier = 1_safest`
- `recommended_action = backfill_canonical_op_notation`
- `doctrine_divergence_tier = none` (zero ADD-claim spread across sources)
- `curator_action = copy-as-is` (no convention translation needed)
- `best_notation_source = fborg` (footbag.org curator-archived; canonical-
  bracket form already authored upstream)
- ADD-math sanity check passes (`[BRACKET]` token count == official ADD)

Filter input: 38 rows. Filter output: 16 rows. Excluded: 1 row (see below).
Mini-wave size: **15**.

## Excluded from the mini-wave

| Slug | Reason |
|---|---|
| `stepping-ducking-paradox-blender` | ADD-math mismatch — official `adds=7` but fborg notation has only 6 bracket tokens (`[DEX], [BOD], [DEX], [BOD], [XBD], [DEL]`). Curator review required before backfill. |

The other 22 rows in the 38-row checklist were not in scope for this
mini-wave because they need convention translation (fm-parens →
canonical-brackets) or have small doctrine divergence. They remain in
`wave_alpha_notation_backfill_2026-05-23.csv` for the next pass.

## The 15 backfill rows

| Slug | Official ADD | Notation (canonical-bracket form, copy-as-is from fborg) |
|---|---:|---|
| `fairy` | 2 | `TOE > SAME OUT [DEX] > OP TOE [DEL]` |
| `paradox-barrage` | 4 | `CLIP > SAME IN [PDX] [DEX] > SAME IN [DEX] > OP TOE [DEL]` |
| `paradox-double-leg-over` | 4 | `CLIP > SAME IN [PDX] [DEX] > OP OUT [DEX] > SAME TOE [DEL]` |
| `paradox-high-plains-drifter` | 5 | `CLIP > SAME IN [PDX] [DEX] > SAME IN [DEX] > SAME CLIP [XBD] [DEL]` |
| `paradox-symposium-mirage` | 4 | `CLIP (plant) > (no plant while) SAME IN [PDX] [BOD] [DEX] > OP TOE [DEL]` |
| `paradox-symposium-whirl` | 5 | `CLIP (plant) > (no plant while) SAME IN [BOD] [PDX] [DEX] > OP CLIP [XBD] [DEL]` |
| `paradox-whirl` | 4 | `CLIP > SAME IN [PDX] [DEX] > OP CLIP [XBD] [DEL]` |
| `ducking-butterfly` | 4 | `SET > (DUCK) [BOD] > SAME/OP OUT [DEX] > OP CLIP [XBD] [DEL]` |
| `ducking-osis` | 4 | `SET > DUCK [BOD] > SAME or OP OSIS [BOD] [XBD] [DEL]` |
| `spinning-osis` | 4 | `CLIP > (back) SPIN [BOD] > (back) SPIN [BOD] > OP CLIP [XBD] [DEL]` |
| `spinning-paradox-blender` | 6 | `CLIP > (back) SPIN [BOD] > OP IN [PDX] [DEX] > (back) SPIN [BOD] > SAME CLIP [XBD] [DEL]` |
| `spinning-paradox-illusion` | 4 | `CLIP > (back) SPIN [BOD] > OP OUT [PDX] [DEX] > OP TOE [DEL]` |
| `spinning-paradox-mirage` | 4 | `CLIP > (back) SPIN [BOD] > OP IN [PDX] [DEX] > OP TOE [DEL]` |
| `spinning-paradox-whirl` | 5 | `CLIP > (back) SPIN [BOD] > OP IN [PDX] [DEX] > OP CLIP [XBD] [DEL]` |
| `symposium-whirl` | 4 | `SET > (no plant while) OP IN [BOD] [DEX] > OP CLIP [XBD] [DEL]` |

ADD-math verification (per the dual-convention rule, each `[TOKEN]` = +1):
all 15 rows have `token_count == official_add`.

## What changed in the repo

`legacy_data/inputs/curated/tricks/red_corrections_2026_04_20.csv`

30 rows appended (15 candidates × 2 fields):

- 15 `operational_notation` rows with the canonical-bracket notation
- 15 `operational_notation_source` rows with provenance citation:
  *"Source: footbag.org /newmoves curator-archived (canonical-bracket
  form). Cross-source confirmation across ≥ 4 independent corpora
  (canonical, FootbagMoves, footbag.org, Stanford shorthand)."*

Each row's `source_note` (curator-visible audit trail) tags the change
as *Wave-Alpha foundational backfill mini-wave 2026-05-23*.

Pre-load conflict check: confirmed zero prior `operational_notation`
entries for these 15 slugs in the corrections CSV. Clean additions.

## Verification performed

1. **CSV append.** 30 rows appended to
   `legacy_data/inputs/curated/tricks/red_corrections_2026_04_20.csv`.
   Logical row count moved from 247 → 271 (15 candidates × 2 fields).
   Pre-load conflict check: zero prior `operational_notation` entries
   for these 15 slugs in the corrections CSV.
2. **Loader applied.**
   `python3 legacy_data/event_results/scripts/19_load_red_additions.py`
   ran cleanly. 265 corrections applied total (out of 271 — 6 skips
   were pre-existing for slugs not in the DB, none from this mini-wave).
3. **DB state verified.** All 15 mini-wave slugs now carry the new
   operational_notation values (sqlite verified).
4. **Tests.** `npm test` → 4,122 tests passing across 230 test files;
   no regressions.
5. **Build.** `npm run build` → clean TypeScript compile.

The parser-population script (`scripts/parse_freestyle_notation.py
--apply`) does NOT need to re-run for this slice — the parser reads
`notation` (Jobs notation), not `operational_notation`, and this
slice only touched the latter.

## What's still pending

- **Re-rebuild parity.** A future `scripts/reset-local-db.sh` run
  (David-owned) will re-apply these corrections automatically as
  part of the standard pipeline — no extra step required, they're
  in the curated source CSV.
- **Production parity.** The change is in the curated source-of-truth
  CSV, so it propagates to any environment that rebuilds the DB from
  the curated inputs.
- **Doc-sync.** Per the doc-sync skill, this is a curator-paced data
  correction (no design intent shift), so no canonical-doc updates
  required. The `exploration/promotion-cohorts/` artifacts are the
  audit trail.

## Next slice (separate)

Wave Alpha promotions — 30 new-promotion candidates across 7 coherent
families (illusion, pickup, eggbeater, legover, whirl, mirage, osis).
Materially different from this mini-wave:

- New canonical rows via `red_additions_*.csv` (not corrections)
- Each candidate needs curator-decidable canonical-vs-compositional
  judgment per the project's named-identity-persistence rule
- Family-grouped review for batch coherence

See `WAVE_ALPHA_2026-05-23.md` for the candidate list and
`wave_alpha_promotions_2026-05-23.csv` for the row data.

## What this slice does NOT do

- ❌ No UI changes
- ❌ No schema migrations
- ❌ No new canonical rows (this mini-wave is corrections-only)
- ❌ No DB rebuild triggered
- ❌ No doctrine adjudication
- ❌ No automatic notation translation
- ❌ Did not touch the 22 non-foundational rows in the broader
  backfill checklist (deferred to a follow-on wave once the curator
  signs off on this one)
