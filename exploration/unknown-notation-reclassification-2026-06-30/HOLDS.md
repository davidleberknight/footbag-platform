# Notation-backfill holds — one missing fact each (2026-07-02)

Companion to `README.md` (the Ready-18 list) after the backfill slice: six rows authored in
the slice, eleven held, and **floatation authored the same day** (see Resolved below) —
ten holds remain. Each row names the SINGLE fact that would allow immediate authoring.
Classes: RED = Red doctrine · CUR = curator decision · SRC = existing-source contradiction ·
EXE = missing canonical exemplar · PAR = parser limitation (applies to none of the rows).
P = estimated probability the row completes without waiting for Red.

## Resolved

- `floatation` (6) — **AUTHORED 2026-07-02.** The missing fact (the floating set's event
  chain) was on the authored Floating Set Encyclopedia page all along: the Quantum Symposium
  Quantum stack, +3. Notation shipped as quantum prefix + no-plant symposium-merged quantum
  + the butterfly body with parity mirrored from canonical quantum_butterfly; bracket count
  6 = ADD, parser computes it exactly, landing/route/add-analysis tests green.

## Held (10)

| Row (ADD) | The one missing fact | Class | P(no Red) |
|---|---|---|---|
| `sole_survivor` (5) | Keep or merge: it is structurally `spinning_symposium_whirl` (separately canonical, same 5). One curator call; the survivor takes the other as alias | CUR | 0.95 |
| `gyro_diving_clipper` (3) | Which is right: the promotion-era asserted 3, or the bracket-derivable 4 (gyro [BOD] + dive [BOD] + [XBD] + [DEL])? The assertion was a curator Tier-1 sweep, not a Red ruling, so the curator owns the correction either way | SRC | 0.9 |
| `witchdoctor` (5) | Is it the FAR form? 5 works only as atomic symposium far mirage carrying [XDEX] (the sumo/atom-smasher pattern); footbag.org reads the near form at 4. PassBack has no witchdoctor row, so the fact is not documentary — one curator identification (or video) settles it | CUR | 0.8 |
| `liquifier` (6) | The splicing set's two-event chain. Registry says +2 and the row's own description says "gyro, reving"; no splicing exemplar exists anywhere. A one-line curator definition (which two events, which sides) makes it mechanical | EXE | 0.7 |
| `terrage` (4) | What carries the fourth bracket. Three-way contradiction: registry terraging = +3, the shipped terraging-clipper notations embody a two-dex chain, footbag.org's terrage JOB has 3 brackets, Red ruled 4 ADD. The ADD is fixed (Red authority); the missing fact is only the chain's shape, which the curator may be able to settle from PassBack's "(midtime) Toe near Barrage" reading | SRC | 0.5 |
| `terraging_illusion` (5) | Same fact as terrage — the chain's third scored event. Authors mechanically the moment terrage settles | SRC | 0.5 |
| `terraging_legover` (5) | Same fact as terrage | SRC | 0.5 |
| `terraging_mirage` (5) | Same fact as terrage | SRC | 0.5 |
| `bill_ted_s_excellent_adventure` (5) | Whether "flailing X" = the documented flail body (symposium illusion) stacked as a prefix. The decomposition already closes (1+1+3 = 5); the prefix reading is the flailing confirm/deny hypothesis in the Red Wave 3 letter. A curator could accept it conservatively, but it is already in front of Red | RED | 0.3 |
| `paradox_blur` (5) | Whether a repeated operator scores: asserted 5 = paradox on blur = paradox twice (the same-operator-twice rider in the Red letter); footbag.org reads single-paradox 4. Demoting a shipped asserted ADD without Red would invert the source-priority order | RED | 0.15 |

## Reading

- **Expected completions without Red: ~6 of the 10 held.** Three are single curator calls
  (sole_survivor, gyro_diving_clipper, witchdoctor), one needs a one-line set definition
  (liquifier), and the four terraging rows rise or fall together on one chain question that
  the curator has even odds of settling from the PassBack reading.
- **Only two rows genuinely wait on Red** (paradox_blur, bill-and-ted), and both are already
  inside the sent Wave 3 letter — no new ask required.
- **No row is parser-limited**: the parser handles every candidate chain; the gaps are facts,
  not syntax.
- Fastest batch order if the curator sits down once: sole_survivor merge → gyro_diving_clipper
  ADD → witchdoctor far-form → liquifier splicing chain → terraging chain. That single
  sitting clears 8 of the 10.
