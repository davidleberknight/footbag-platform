# Frontier Remnant — hand-off after the clean-batch tranche

## Curator rulings — three items moved from blocked to AUTHORABLE

These three were on the "undefined operator/base" blocker list. Curator gave
mechanical definitions; all three are now authorable under the standard
publication review (ADD still derives from bracket count per candidate).

- **whirling** = "uptime whirl". A real operator definition (uptime whirl event),
  no longer under-attested. The earlier miraging-collision worry is moot. Author
  whirling-X off the uptime-whirl chassis. (~61 frontier slugs in scope.)
- **guay** = BASE trick: `pickup -> inside stall` (terminates in the clipper/inside
  stall atom). Not an operator. Author guay as a canonical base; X-guay stack
  operators on it. (~44 frontier slugs in scope.)
- **rake** = BASE trick: `scoop -> toe stall` (terminates in the toe-stall atom).
  Rake is the reverse of pendulum — capture that as a glossary equivalence note.
  Author rake as a canonical base. (~33 frontier slugs in scope.)

Still blocked (genuine undefined tokens / doctrine): blazing token, terraging
token, atomic-rotational Q3, plus DOD-policy / blurry-transitivity presentation
clusters and Group-3 low-volume tokens (zulu, motion, torquescrew, bent).

---


State at hand-off: canonical first-class **607**, frontier **1,640** unique
non-canonical, clean Tier-1 **45** (+ blazing 8 parked). All seven fully-clean
homogeneous operator+base batches are done (stepping/spinning/diving/fairy/
barraging/symposium/swirling). What remains is risk-flagged — do NOT bulk-promote;
each needs the specific gate below cleared first.

Source data: `BURNDOWN_UNIQUE.md` (full per-slug), `CLEAN_TIER1.md` (Tier-1 detail).

## The risk-flagged batches

### blazing + base (8) — BLOCKED on a Red/curator token ruling
`blazing` is a confirmed +1 body modifier but has **no defined movement/notation
token** (no `blazing-X` precedent, no operator-reference entry, DB says only
"+1 body modifier"). Authoring notation would mean fabricating the operator.
- **Gate:** one-sentence ruling — *what is blazing mechanically, and what is its
  body-event token (`___ [BOD]`)?* Unblocks all 8 at once.
- Candidates: blazing-{butterfly, drifter, illusion, legover, mirage,
  paradox-whirl, symposium-mirage, torque}.

### atomic + base (10) — RED dependency (rotational-Q3) + alias scrub
- **Non-rotational bases** (butterfly, osis, legover, pickup): atomic = +1, clean
  and promotable now after the alias scrub.
- **Rotational bases** (mirage, whirl, torque, blender, swirl, drifter): atomic =
  +2 via the hidden X-Dex carry — **gated on Red Q3** (atomic-rotational doctrine).
- **Alias scrub first:** many atomic-X are canonical folk names already —
  atomic-mirage=atom-smasher, atomic-legover=eggbeater, atomic-pickup=omelette
  (Q1-conflicted), atomic-double-leg-over=predator, atomic-symposium-mirage=
  witchdoctor, atomic-ducking-whirl=ego. Those are Tier-4 duplicates, not promotions.

### whirling + base — RESOLVED (now authorable)
Curator defined whirling = "uptime whirl". See the rulings block at the top of
this file. Standalone whirling+atom now derives off the uptime-whirl chassis; the
miraging-collision worry is moot.

### pixie + base (10) — folk-alias scrub required
Pixie structural forms are heavily folk-named: pixie-mirage=smear,
pixie-butterfly=dimwalk, pixie-ducking-butterfly=phoenix, pixie-ducking-mirage=
assassin, pixie-diving-near-butterfly=darkwalk, pixie-illusion=sailing(set).
- **Gate:** per-candidate scrub against the folk-overlap registry + alias table.
  The genuinely-distinct pixie-X are promotable (pixie token is well-defined,
  `TOE > SAME IN [DEX]`); the aliased ones reclassify to Tier-4.

## Small clean remnants (likely promotable, verify token first)
paradox (5), terraging (3, token `+3` confirmed), tapping (2, `TOE > OP OUT [DEX] >>`
confirmed), miraging (2, `SET > OP IN [DEX] >` confirmed). These are small but the
tokens are attested — run the standard review and they should pass.

## Recommended order for next session
1. **pixie alias scrub** → promote the non-aliased pixie-X (no Red needed).
2. **atomic non-rotational** (after alias scrub) → promote the +1 non-rotational set.
3. **Small remnants** (tapping / miraging / terraging / paradox).
4. **whirling** → after confirming the token.
5. **Hold for Red:** blazing token, atomic-rotational Q3.

## Open Red/curator questions blocking the tail
- **blazing** movement/token (blocks 8).
- **atomic-rotational Q3** doctrine (blocks ~6 atomic-rotational candidates).
- (Resolved this session: spyro = plant-before-dex gyro move; warping = 2-dex
  3-ADD set, 2nd dex symposium; rooted = support-foot set; floating = quantum
  symposium quantum = 3.)
