# Notation-backfill audit — 59 active canonicals lacking op_notation

Classified by how the notation can be sourced. Per-trick data: `notation_backfill.csv`.
DOD/DDD impact detail: `dod_impact.csv`.

| Group | Count | Disposition |
|---|---|---|
| **mechanically derivable (high confidence)** | **13** | backfill now, one pass, no Red |
| derivable but requires curator review | 32 | HOLD |
| blocked on operator definition | 0 | — |
| blocked on doctrine | 14 | HOLD |

(`refraction` is a 3-ADD dex base atom needing direct notation authoring, not operator-blocked — counted in curator-review.)

## Mechanically derivable (13) — clean exemplar'd operator on a notated base

Notation = mirror the operator's existing sibling exemplar onto the already-notated base;
the hard gate is bracket-count == ADD.

diving-eclipse, diving-toe-stall, gyro-diving-clipper, spinning-ducking-drifter,
spinning-miraging-symposium-torque, spinning-symposium-whirling-swirl, spinning-tomahawk,
stepping-ducking-symposium-eggbeater, symposium-blur, symposium-mobius, symposium-tomahawk,
whirling-pickup, whirling-rake.

**One-pass estimate: all 13, zero Red.** (A handful of the 32 curator-review folk names have
settled decompositions and could join later, but each needs a one-line operator confirmation
first, so they are not high-confidence today.)

## Held — curator-review (32)
Folk names whose operator is not in the slug (blizzard, fury, genesis, rage, warp, witchdoctor,
floatation, liquifier, margaritaville, clown-face, bedwetter, big-apple-sauce, …) and cascade
cases whose base is itself un-notated (hop-over, jani-walker, plasma, sailing, spyro, surging, terrage).

## Held — doctrine-blocked (14)
paradox (6), fairy (3), blurry (2), atomic (1), barraging (1), DOD/DDD (1).

## Highest-leverage findings
- **Non-Red workstream:** the **13 mechanical backfills** — pure execution, ships now.
- **Red doctrine question:** **DOD/DDD policy** — affects 23 frontier items (corrected from an
  inflated 30; the bare token "double" over-matched double-orbit/double-pickup). Resolving it makes
  ~8 immediately promotable and corrects a systematic +2-to-+4 ADD undercount (PassBack claims 2-3
  vs derived 4-6, since the Double Down base is 4 ADD). Detail in `dod_impact.csv`.
