# Freestyle dictionary frontier audit

Read-only. Generated by `audit.py` from the live DB reconciled with current CSV
inputs (`canon = DB active ∪ slugify(active red_additions)`), so the not-yet-
reloaded promotions count. Nothing promoted, nothing sent to Red.

## 1. Published / resolved counts (with change explanations)

| Metric | Now | Source / change since last reports |
|---|---|---|
| Active canonical tricks | **723** | DB 712 + **11 pending** (committed in CSVs, DB not yet reloaded): the 7 new canonicals (double-illusion, double-whirl, triple-swirl, triple-mirage, atomic-blender, atomic-double-leg-over, atomic-ducking-torque) + 4 fborg rows (dragon, probe, miraging-kick, illusioning-kick). |
| Aliases | **258** (current CSV source) | DB `freestyle_trick_aliases` still reads **166**; the +92 delta is the recent wiring not yet reloaded: 63 PassBack folk-name aliases, the 7 new-canonical folk aliases (delusion, whirr, …), and the foundational aliases (dso, clipper-kick). |
| First-class tricks | **621** (proxy) | Convergence-gate proxy = active tricks whose op_notation bracket-count == adds. The render-time `isFirstClass` (pilot list + convergence rule) last reported ~651; this proxy is conservative and DB-stale by the 11 pending. |
| Media coverage | **135** tricks | Distinct trick tags carrying media (`media_tags`). |
| PassBack orphan residual | **69** ✓ | Confirmed by the authoritative mapper (from 187). |

> Note on the stale DB: a clean number requires `reset-local-db.sh`, which the testing rules forbid on a workstation holding the real `legacy_data/` dump. The CSVs are authoritative; the 11 pending are real promotions awaiting a reload in a safe environment.

## 2. Promotion-ready frontier — **3** (fastest safe promotions)

No zero-edit promotions remain (the clean Tier-1 runway is exhausted). The 3
below have a single clean reading + a PB ADD claim and need only op_notation
authoring + a bracket-count==ADD check. Detail in `promotion_ready_now.csv`.

| Trick | Reading | PB ADD | First action |
|---|---|---|---|
| Pandora's Box | Gyro Pickup | 1 | verify gyro-pickup isn't already canonical (→ alias) else author+promote |
| Green Eggs and Ham | Stepping Ducking Swivel | 2 | author op_notation; verify ADD |
| Anonymous | Spinning Miraging Symposium Miraging Refraction | 3 | author; the long reading vs ADD-3 must be reconciled before promoting |

## 3. Needs authoring — **5** (`needs_authoring.csv`)

Structure known, but ADD/formula not pin-down-able yet: Irish Cream (2 competing
readings, ADD 2), Tobius (2 readings, ADD 1) → resolve the reading, then they move
to promotion-ready. Monster, Rotor, Wauxspin → no ADD claim / thin structure.

## 4. Doctrine-blocked — **41** (`doctrine_blocked_current.csv`)

Recomputed from current readings + residual undefined-operators (grouped by first-
matching blocker keyword; a few entries match two and land in the first).

| Blocker group | Count | Representative | Exact question for Red |
|---|---|---|---|
| **DOD/DDD/dragon** | 10 | Dimmier, Dimmiest, Cold Fusion, Spanishfly | Is a "Double Down" component (DOD/DDD) a promotable base, and at what ADD? This is the single highest-leverage unblock. |
| **motion/twisting/frantic/undefined** | 11 | Blink (twisting), E-Walk (frantic), Locomotion (motion) | Define the folk operators twisting / motion / frantic (ties to the residual packet's high-leverage 3). |
| **other (recurring undefined ops)** | 18 | Jackknife (alpine), Kilo, Lawn Moir (slicing), Moby Dick (mobiusscrew) | Define alpine / kilo / slicing / mobiusscrew / torquescrew, or confirm not-tricks. |
| **blur/furious** | 1 | Blurrier | (also DOD-tagged) |
| **shooting** | 1 | Shooting Star | (also DOD-tagged) define shooting. |

## 5. Unknown dex / unknown ADD — **65** (`unknown_add_or_dex.csv`)

- **59 active canonicals with no op_notation → unknown dex.** Source missing but
  derivable (most are mechanical JOB-backfill candidates). The 10 null-ADD rows
  were operators (gyro, spinning, symposium, …) and are excluded — ADD is N/A for
  operators, not a gap.
- **6 observational entries with null proposed ADD** — needs Red where doctrine-
  blocked, else derivable.

## 6. Orphan video fallout — **1** (`orphan_video_residual.csv`)

The 5 PassBack demo records were reattached earlier; one remains: a demo on slug
`da-da-curve` (Anssi Sundberg). Proposed: Red-needed (no obvious canonical).

## 7. Emerging vocabulary re-audit — 46 entries (`emerging_vocab_classified.csv`)

| Classification | Count | Note |
|---|---|---|
| already-canonical-or-alias | **18** | Recent orphan-reduction aliasing made these redundant. Per the layer-separation forever-rule they should be **pruned** from `freestyleObservationalTricks.ts`. (Big Orange, GDLO, Ghost, POD, Slapdown, Whirlwalk, …) |
| doctrine/undefined-operator | 20 | Feeds bucket 4. |
| promotion-candidate | 3 | Bucket 2. |
| needs-authoring | 5 | Bucket 3. |
| source-only/historical | 1 | Red-needed. |

## Prioritized action plan (next 1–2 days, all safe, no Red)

1. **Prune the 18 redundant observational entries** — pure layer-separation cleanup the recent aliasing created; no promotion, no Red, removes stale Emerging-Vocabulary rows. Highest value-per-risk.
2. **Promote the ~2–3 clean candidates** (Pandora's Box, Green Eggs and Ham; reconcile Anonymous): for each, first check whether the reading's target is already canonical (→ wire an alias instead), else author op_notation + verify bracket==ADD + promote.
3. **Resolve the 2 ADD-claim authoring rows** (Irish Cream, Tobius) by picking one reading → they become promotion-ready.
4. **Reload the DB in a safe environment** to surface the 11 committed-but-unloaded promotions (or accept the CSVs as authoritative).
5. **Hold the 41 doctrine-blocked for Red** (do not send yet). When you do, lead with the **DOD/DDD policy** question — it unblocks 10 directly and is upstream of several "other" entries — then the folk-operator definitions (twisting / motion / frantic), which also clear residual-packet items.

Net: the frontier is now **authoring- and doctrine-bound, not discovery-bound**. The safe near-term yield is small (~3 promotions + 1 cleanup sweep); the real leverage is the DOD/DDD ruling and the folk-operator definitions.
