# Dragon / Refraction identification — evidence note

Date: 2026-07-02. Evidence pass only: no canonical data changed by this note.

## Bottom line

The curator identification question "what are the folk-named bases Dragon and Refraction?"
(tracked in `IMPLEMENTATION_PLAN.md` under the freestyle trick-promotion decisions item, and
originally in `exploration/doctrine-audit-2026-06-19/DECISION_PACKET.md`) is **stale for both
names: both are already canonical, active, and expert_reviewed** in `freestyle_tricks`. The
remaining open identification questions from that packet are Clipper Symposium Whirl and
Nuclear Osis vs Aeon Flux only.

## Dragon — resolved, sources convergent

- Canonical row: `dragon`, 2 ADD, `SET > SAME OUT [XBD] [DEL]` (bracket count 2 = ADD).
- FB.org showmove/21: "Dragon", 2 ADD, notation residue `[xbd] [del]` (no prose).
- PassBack (`exploration/passback-intake/passback_trick_sources.csv`): technical name
  **"Xbd Outside Stall"** — a cross-body outside delay, exactly the canonical notation.

Three sources agree on structure and ADD. Nothing left to decide.

## Refraction — resolved, one registered divergence

- Canonical row: `refraction`, 3 ADD, `SET > OP INSIDE [DEL] > OP OUT [DEX] > OP CLIP [XBD]`
  (bracket count 3 = ADD).
- FB.org showmove/65: 3 ADD, JOB matching the canonical notation, plus prose: inside delay,
  then wrap the bag while completing a 360-degree spin toward the support leg, clipper catch.
- PassBack divergence: PB's technical name for Refraction is **"Butterfy near Osis"** (sic),
  dex count 1, already flagged `-2` vs the canonical ADD in the intake CSV. The FB.org
  prose+JOB reading is the stronger source (full description vs a bare tech-name string).
  Scoring is unaffected; the PB reading belongs in the cross-source divergence register, not
  in canon.

## Side-finding: the 7-ADD refraction stack has a folk identity

PassBack row: folk name **"Anonymous"** = technical name "Spinning far Miraging Symp.
Miraging Refraction" — the same row the observational universe carries as
`spinning-miraging-symposium-miraging-refraction` (derived 7 ADD; PB dex count says 3).
Its base (refraction) is canonical, but the stack contains **miraging twice**, which is
doctrine-blocked under the same-operator-twice zero-precedent rule, and the PB-vs-derived
ADD divergence is unresolved. It stays blocked; recorded here so the folk identity is not
lost when the ruling lands.
