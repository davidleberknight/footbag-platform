# Promotion-Unlock Audit (2026-06-20)

Data-grounded re-classification of the held freestyle frontier against settled doctrine
(`DOCTRINE_AUDIT.md`, this folder). Inputs: the June packet's
`exploration/doctrine-audit-2026-06-19/PROMOTION_INVENTORY.csv` (52 clean-band candidates) and the
64 `freestyle_tricks.is_active = 0` emerging-vocabulary rows in the live DB.

## Clean-band frontier (52 candidates)

| Class | Count |
|---|---|
| Already promoted (live) | 32 |
| **Ready now** (0 doctrine block) | **9** |
| Needs curator (insertion convention) | 7 |
| Needs identification | 4 |
| Still needs Red | 0 |

**Ready-now 9:** `nuclear-osis`*, `paradox-miraging-symposium-eggbeater`,
`paradox-miraging-symposium-mirage`, `pixie-miraging-symposium-miraging-legover`,
`sailing-gyro-pickup`, `swirling-gyro-inside-stall`, `symposium-whirling-swirling-mirage`,
`whirling-symposium-mirage`, `whirling-symposium-swirl`.
*(`nuclear-osis` carries an open identification flag — see below — so it is ready-pending-ID.)*

**Needs curator (7):** `railing-{butterfly, illusion, legover, mirage}`, `railing-ducking-legover`
(Railing = Rooted Sailing set), `surfing-{osis, pickup}` (Surfing = Fairy Symposium Swirling set) —
the new June set-operators need an insertion-convention call, not Red.

**Needs identification (4):** `miraging-dragon`, `swirling-dragon` (Dragon deferred by Red),
`ducking-clipper-symposium-whirl` (clipper-symposium-whirl base), `spinning-miraging-symposium-
miraging-refraction` (Refraction = dex per Red, structure unconfirmed).

## Held emerging-vocabulary band (64 `is_active=0`) by doctrine cluster

| Cluster | Count | Status |
|---|---|---|
| Pogo | ~13 | settled (+0 set) → ready now |
| Atomic / Quantum | ~9 | settled doctrine; ready pending the +1-migration greenlight |
| Blurry | ~4 | settled (Stepping[+Paradox]) → ready now |
| Other settled operators (furious/shooting/sailing/barraging/backside) | ~5 | ready now |
| Terraging | 2 | notation authoring pending |
| Other (operator-definition rows + governance items) | ~31 | case-by-case |

## Estimates

1. **Promotable accepting settled rulings:** ~9 clean-band + ~25 held-band settled-cluster compounds
   ≈ **35 ready-now**, plus the DOD band (~25, verification-required, not Red). Roughly **35–55**
   with no further Red.
2. **Largest-unlock clusters:** DOD (~25) · Pogo (~13) · Atomic/X-Dex migration (~9–20) · Blurry (~4–7).
3. **"Open" packet items actually blocking today:** only **Weaving (A2)**. A3 Pogo, A4 Blurry, A5
   Atomic/X-Dex are settled; A1 DOD/DDD is mostly settled (verification, not Red).

## Held solely on now-settled doctrine (releasable)

- **Pogo family** — held on "+0 vs +1"; settled +0.
- **Blurry family** — held on "+2 rotational operator"; settled explicit Stepping[+Paradox].
- **Atomic-rotational band** — held on "+1 vs +2"; settled atomic +1, X-Dex on the following far dex.

## Bottleneck

Doctrine → execution. The constraint is authoring/verification throughput, not Red consultation.
Only Weaving (and the identification/SS/Clipper-anomaly items) genuinely await Red.
