# Entry-Topology Audit — Data-Fix / Doctrine-Conflict Candidates

Read-only. Curator review list.

## 1. `modifier_type` does not match entry set-role

The registry's `modifier_type` column cannot drive the entry taxonomy. Entry set-role and the stored type disagree for:

| modifier | DB modifier_type | entry set-role |
|---|---|---|
| symposium | body | No-Plant / Timing |
| paradox | body | No-Plant / Timing |
| blurry | set | No-Plant / Timing |
| furious | set | No-Plant / Timing |
| stepping | body | Set Ecosystem |

## 2. Artifact / suspicious entry-surface tokens (formula openings)

| opening | count | note |
|---|---|---|
| artifact/none | 8 | non-canonical set opening; review notation |
| inside-set | 1 | non-canonical set opening; review notation |

## 3. No-formula tricks (entry surface unknown)

178 of 651 active tricks carry no operational notation, so their entry surface cannot be derived. They are absent from the surface counts above.

## 4. Doctrine note — set ecosystems leave no opening token (NOT a data error)

pixie / fairy / stepping / atomic / quantum / nuclear carry no dedicated opening token; the formula spells them as a bare toe/clip set plus a dex. This is expected: a set ecosystem IS the set pattern, not an inserted operator. The position test confirmed operator position within the formula does not determine topological role, so absence of an opening token is not grounds to dissolve a set ecosystem.

