# HOLD Packet (8)

Read-only. No promotions, no data writes. Each entry has a hard blocker that no review step resolves - it needs a base-notation definition, a structural ruling, or a grammar ratification. The exact blocking condition and what would unblock it are stated per entry.

## Base primitive has no notation (3)
The decomposition's base is a set/spin primitive that has no `operational_notation` in the trick table, so there is nothing to compose the modifier onto.
| candidate | ADD | exact blocker | what unblocks |
|---|---|---|---|
| sailing-clipper | 2 | base `sailing` has no notation - it is a +2 set spine, not a canonical trick row | define the sailing set-primitive notation (it lives in `freestyleCanonicalSets.ts`, not `freestyle_tricks`); link or publish it |
| spyro-gyro | 2 | base `spyro` has no notation | define the spyro primitive, or ratify spyro == gyro-equivalent so it inherits gyro's notation |
| Spyro Illusion | (3) | same: `spyro` base no notation | same as spyro-gyro |

## Multi->> structural base (1)
| candidate | ADD | exact blocker | what unblocks |
|---|---|---|---|
| toe-mobius | 5 | base `mobius` carries two `>>` (multi->> structural); it is in the held tomahawk/mobius cohort, not a clean single-collapse base | a structural ruling on the mobius multi->> notation (the held family-taxonomy item) |

## Un-ratified count-prefix ADD grammar (4)
Tier-2 folk whose name uses a `double`/`triple`/`double-dex` count prefix. The structure is recognizable, but the count-prefix ADD grammar (how much ADD a `double`/`triple` adds, and whether it doubles the base dex or adds a fixed amount) is not ratified, so the ADD cannot be asserted.
| candidate | exact blocker | what unblocks |
|---|---|---|
| Double Drifter | count-prefix ADD grammar unratified | a ruling on double/triple count-prefix ADD (does `double X` = 2x the X dex, or +1 dex?) |
| Double Bubba | same | same |
| double-dex Butterfly | same (double-dex variant) | a ruling on double-dex / triple-dex ADD accounting |
| triple-dex Legover | same (triple-dex variant) | same |

## Summary
- 3 blocked on **base-primitive notation** (sailing/spyro sets) - a definition/linking task, possibly a quick curator confirm.
- 1 blocked on a **held structural ruling** (mobius multi->>).
- 4 blocked on **un-ratified count-prefix ADD grammar** (double/triple/double-dex).

None of the 8 is promotable now. The 3 set-primitive cases are the closest to unblocking (the sailing/spyro notations may already exist outside the trick table); the count-prefix 4 and the mobius 1 need an actual ruling. These are good candidates for the next Red consultation alongside the C-operator clusters.
