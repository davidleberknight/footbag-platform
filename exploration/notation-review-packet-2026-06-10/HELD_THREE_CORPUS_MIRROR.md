# Corpus-mirroring report — the 3 held deep stacks

Purpose: prove `big-apple-sauce`, `swirlwind`, `surging-ducking-paradox-torque` are
**exemplar-derived** (each = an existing multi-operator canonical skeleton + one documented,
independently-attested transformation) rather than convention-created. **Nothing written.** These
three stay HELD pending your sign-off on this report.

Method: for each held row, (1) name the nearest single existing canonical that already carries
most of the chain, (2) state the single transformation that turns it into the target, (3) cite the
existing canonical(s) that independently attest that exact transformation (same operator, same
`[PDX]`/`[BOD][DEX]` placement, same direction parity), (4) flag any residual step with no single-row
precedent. Bracket-count == ADD is verified for all three.

All exemplar strings below are quoted verbatim from the live `freestyle_tricks.operational_notation`.

---

## 1. big-apple-sauce (8) — spinning > paradox > miraging > symposium > torque

**Target (proposed):**
`SET > (back) SPIN [BOD] > OP IN [DEX] > (no plant while) OP IN [PDX] [BOD] [DEX] > (back) SPIN [BOD] > OP CLIP [XBD] [DEL]`

**Nearest existing canonical — `spinning-miraging-symposium-torque` (7):**
`SET > (back) SPIN [BOD] > OP IN [DEX] > (no plant while) OP IN [DEX] [BOD] > (back) SPIN [BOD] > OP CLIP [XBD] [DEL]`

**Transformation:** add `[PDX]` to the existing symposium no-plant event (+1 ADD), i.e.
`(no plant while) OP IN [DEX] [BOD]` → `(no plant while) OP IN [PDX] [BOD] [DEX]`.

**Attestation of that exact transformation** (paradox merging onto a symposium no-plant
`[BOD][DEX]` event, `[PDX]` first, direction unchanged):
- `spinning-paradox-symposium-whirl` (6): `… (back) SPIN [BOD] > (no plant while) OP IN [PDX] [BOD] [DEX] > OP CLIP [XBD] [DEL]`
- `ducking-paradox-symposium-mirage` (5): `… (no plant while) OP IN [PDX] [BOD] [DEX] > OP TOE [DEL]`
- `ducking-paradox-symposium-whirl` (6): `… (no plant while) OP IN [PDX] [BOD] [DEX] > OP CLIP [XBD] [DEL]`

Each shows the same `OP IN [PDX] [BOD] [DEX]` merged event with spinning/ducking preceding — exactly
the form big-apple-sauce inherits.

**Tail + entry attestation:** the `(back) SPIN [BOD] > OP CLIP [XBD] [DEL]` torque close and the
`SET > (back) SPIN [BOD] > OP IN [DEX]` spinning-then-miraging opening are both quoted verbatim from
`spinning-miraging-symposium-torque` and `symposium-torque` (`SET > (no plant while) … > (back) SPIN [BOD] > OP CLIP [XBD] [DEL]`).

**Residual judgment call (honest):** no single existing row carries *both* the miraging dex-injection
*and* paradox-on-the-symposium-event simultaneously — that co-occurrence is the only step without a
one-row precedent. It is the sum of two independently-attested operations (miraging injection from
`spinning-miraging-symposium-torque`; paradox merge from `spinning-paradox-symposium-whirl`), not a
new rule. Also applies the C2 canonical token order `[PDX][BOD][DEX]`, normalizing the nearest
exemplar's drifted `[DEX][BOD]`.

**ADD:** BOD + DEX + (PDX+BOD+DEX) + BOD + (XBD+DEL) = 1+1+3+1+2 = **8** ✓

---

## 2. swirlwind (7) — spinning > paradox > symposium > whirling-swirl

**Target (proposed):**
`CLIP > (back) SPIN [BOD] > (no plant while) OP IN [PDX] [BOD] [DEX] > OP BACK SWIRL [DEX] > SAME CLIP [XBD] [DEL]`

**Nearest existing canonical — `spinning-symposium-whirling-swirl` (6):**
`CLIP > (back) SPIN [BOD] > (no plant while) OP IN [BOD] [DEX] > OP BACK SWIRL [DEX] > SAME CLIP [XBD] [DEL]`

**Transformation:** add `[PDX]` to the existing symposium no-plant event (+1 ADD), i.e.
`(no plant while) OP IN [BOD] [DEX]` → `(no plant while) OP IN [PDX] [BOD] [DEX]`.

**Attestation:** identical to big-apple-sauce above — `spinning-paradox-symposium-whirl` /
`ducking-paradox-symposium-whirl` both ship the `(no plant while) OP IN [PDX] [BOD] [DEX]` merged
event with a leading spin. The whirling-swirl tail `OP BACK SWIRL [DEX] > SAME CLIP [XBD] [DEL]` is
quoted verbatim from `whirling-swirl` (4) and `spinning-whirling-swirl` (5).

**Residual judgment call:** none of note. This is the cleanest of the three: nearest skeleton differs
from target by exactly one well-attested operation, and the resulting string is a direct splice of
two verbatim corpus fragments (the `spinning-paradox-symposium-*` merged event + the
`spinning-symposium-whirling-swirl` swirl tail).

**ADD:** BOD + (PDX+BOD+DEX) + DEX + (XBD+DEL) = 1+3+1+2 = **7** ✓

---

## 3. surging-ducking-paradox-torque (8) — surging (=spinning+stepping) > ducking > paradox > torque

**Target (proposed):**
`CLIP > (back) SPIN [BOD] > OP IN [DEX] > DUCK [BOD] > OP IN [PDX] [DEX] > (back) SPIN [BOD] > OP CLIP [XBD] [DEL]`

**Nearest existing canonical — `ducking-paradox-torque` (6):**
`CLIP > DUCK [BOD] > OP IN [PDX] [DEX] > (back) SPIN [BOD] > OP CLIP [XBD] [DEL]`

**Transformation:** prepend the `surging` prefix = spinning `(back) SPIN [BOD]` + stepping
`OP IN [DEX]` (+2 ADD).

**Attestation:**
- The `surging` prefix itself = the standalone form ratified this session: `(back) SPIN [BOD] > OP IN [DEX]` (spinning + stepping).
- Spin-leading-then-ducking-then-paradox-torque-class chain is attested by
  `spinning-ducking-paradox-blender` (7): `CLIP > (back) SPIN [BOD] > DUCK [BOD] > OP IN [PDX] [DEX] > (back) SPIN [BOD] > SAME CLIP [XBD] [DEL]` — same `(back) SPIN [BOD] … DUCK [BOD] > OP IN [PDX] [DEX]` ordering and the same paradox-on-base-dex placement (C4).
- The `OP IN [PDX] [DEX] > (back) SPIN [BOD] > OP CLIP [XBD] [DEL]` paradox-torque close is verbatim from `ducking-paradox-torque` and `spinning-paradox-torque`.

**Residual judgment call (honest):** there is no existing canonical that stacks spinning **and**
stepping **and** ducking, so the *insertion point of stepping's* `OP IN [DEX]` is the one
unattested choice. I placed it adjacent to the spin (keeping the named `surging` unit contiguous:
`(back) SPIN [BOD] > OP IN [DEX]`), then `DUCK [BOD]`. The alternative — interleaving stepping after
the duck — would also satisfy bracket-count. This is the single parity decision to confirm; it does
not affect ADD.

**ADD:** BOD + DEX + BOD + (PDX+DEX) + BOD + (XBD+DEL) = 1+1+1+2+1+2 = **8** ✓

---

## Summary

| held row | nearest canonical (verbatim skeleton) | single transform | transform attested by | residual call |
|---|---|---|---|---|
| big-apple-sauce (8) | spinning-miraging-symposium-torque (7) | +[PDX] on sympo event | spinning-paradox-symposium-whirl; ducking-paradox-symposium-{mirage,whirl} | miraging-inject + paradox-merge co-occur (sum of two attested ops) |
| swirlwind (7) | spinning-symposium-whirling-swirl (6) | +[PDX] on sympo event | spinning-paradox-symposium-whirl; ducking-paradox-symposium-whirl | none of note (cleanest) |
| surging-ducking-paradox-torque (8) | ducking-paradox-torque (6) | +surging prefix (spin+step) | spinning-ducking-paradox-blender; ratified surging standalone | stepping-dex insertion point within surging prefix |

**Conclusion:** all three are exemplar-derived. Each target equals a verbatim corpus skeleton plus
one transformation that is itself quoted from existing canonicals. The two genuinely novel residuals
— big-apple-sauce's miraging+paradox co-occurrence, and surging-ducking-paradox-torque's stepping
insertion point — are *composition* choices (which exemplar fragments combine, and in what order),
not new grammar, and neither changes ADD. swirlwind has no material residual. These are the specific
points to ratify before backfill.
