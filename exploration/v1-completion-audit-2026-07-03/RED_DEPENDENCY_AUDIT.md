# Red-dependency audit — challenging every open question (V1.0 pass)

Audit date 2026-07-03. Method: for each entry in `freestyle/doctrine/RED_QUEUE.md`,
(a) restate the remaining question, (b) verify the affected-row counts against the
current universe and Emerging Vocabulary states (measured from the generated
`freestyleObservationalUniverse.ts`, stamped 2026-07-03, and the live database),
(c) attempt the removal under the standing burden-of-proof-on-keeping principle,
and (d) state the fallback if Red never replies, drawn from the three governed
outcomes in `freestyle/doctrine/README.md`: park in Emerging Vocabulary, publish
with divergence notes, or curator adjudication.

Measured gate states used throughout (971 EV intake rows): doctrine-blocked 82
(blurry_expansion 64, crossbody_rake_base 12, repeated_operator 4, terraging_chain
2); governance 23 (all down_family_verification); undefined_operator 67 across 16
tokens; identification 13; parser 108; folk 98; authoring 134; ready 1; alias 445.

**Headline: every V1-critical fallback already exists.** No entry below blocks a
V1.0 release; the queue is a promotion-throughput dependency, not a release
dependency. Two entries carry maintenance defects (a count drift and a state
inconsistency) fixed by editing the queue, not by waiting for Red.

---

## With Red

### 1. Blurry expansion predicate — KEEP, but split the batch out of the gate

- **Question**: is the paradox term in blurry-named tricks physical content
  footbag.org records and PassBack elides, or a notation convention?
- **Rows verified**: the queue says ~43 at audit count; the current universe gates
  **64** rows on `blurry_expansion` (plus the ten deferred blurry-form aliases and
  5 active canonical blurry rows, all flagged `approximate` in ADD status). Update
  the queue's count or date it.
- **Removal attempt**: fails. The reduction audit already mined the written record;
  the residue is a genuine source contradiction (fb.org JOBs carry `[PDX]` where
  PassBack's stepping-only technical names omit it; Ripwalk the only agreement
  case). No third source exists to break the tie; frequency is not authority.
- **However**: the queue itself concedes the rows that have their own footbag.org
  JOBs author under the codified source-priority order **regardless of the
  answer**. That sub-batch is not Red-blocked and is already queued in the curator
  working order ("the batch of blurry rows that carry footbag.org notation").
  Action: count that sub-batch, author it, and shrink the gate to the rows with no
  fb.org JOB.
- **Fallback**: authored sub-batch publishes with a divergence note where PB
  disagrees; the remainder parks in EV under `blurry_expansion`, which is the
  governed correct outcome.

### 2. Embedded-base labeling frame (down-family compounds) — KEEP, explicitly non-blocking

- **Question**: which coordinate frame names the embedded base of an operator
  compound (entry-relative after stripping operator contacts, with or without the
  evidenced body-spin inversion).
- **Rows verified**: 3 shipped base-label tension rows (shooting_star,
  tapping_double_over_down, venom — detail table in `DOWN_FAMILY.md`) plus **23**
  EV rows in `governance`/`down_family_verification`. No ADD rides on it anywhere.
- **Removal attempt**: close. The spin-inversion convention has two independent
  exhibits (Scorpion's Tail, Bullwhip) and zero counter-exhibits — by the
  derivation standard used for alpine (nine pairs) this is thin but not empty. A
  curator COULD adjudicate it as an evidence-derived convention with a recorded
  proof and an amendable chain. The queue keeps it with Red because it is one line
  of doctrine that settles a class (well-formed by the governance principle), and
  the letter is already sent. Keep, but it is labeling-only.
- **Fallback**: curator adjudication on the two-exhibit evidence (recorded in
  `OPERATOR_DERIVATIONS.md` style), or indefinitely ship as-is: the three rows
  stay as shipped with the tension table already public doctrine. Either way V1
  ships.

### 3. Operator structural definitions (blazing, flailing, slapping, blistering) — KEEP, halve it

- **Question**: what each movement physically IS (ADDs already evidenced/proven).
- **Rows verified**: blazing — the queue says nine backfills;
  `OPERATOR_DERIVATIONS.md` proves +1 over **eight** canonical compounds
  (reconcile nine-vs-eight when editing the queue). Slapping — 9 EV rows gated
  `undefined_operator/slapping` plus alias-layer folk pairs. Flailing — no
  dedicated EV gate; the affected rows sit in the alias layer (29 flail-name
  rows). Blistering — no EV gate found; affected rows not independently
  verifiable this run.
- **Removal attempt**: partially succeeds. Flailing and slapping are already
  downgraded to single confirm/deny hypotheses with the bare structures documented
  (FootbagMoves defines bare Slap and Flail = Symposium Illusion); the derivation
  boundary is explicit — no compound notation exists to cross-check, so a
  DERIVATION cannot close them, but a curator could accept the prefix-form
  hypothesis as an evidence-derived ruling with a recorded residual risk, exactly
  as alpine accepted a one-in-nine diving variance. That is a legitimate
  adjudication the curator may choose rather than wait.
- **Fallback**: the Jani-Walker precedent (active on ADD authority, structure
  deferred) publishes blazing's nine compounds now with structure-deferred notes;
  slapping/flailing rows park in EV or activate ADD-only the same way; blistering
  stays parked.

### 4. Cross-body rake base and the crossbody register — KEEP

- **Question**: what the xbd-rake base is structurally.
- **Rows verified**: **12** EV rows gated `crossbody_rake_base` now; Snowflake is
  the named immediate need; the queue's "~47-slug parser-section drain follows"
  is plausible against the 108 parser-gated rows (25 rake-named rows visible in
  the universe) but not exactly re-derivable this run — date or re-derive the 47.
- **Removal attempt**: fails. No source carries a structural notation for the
  xbd-rake base; rake compounds cannot author without it, and inventing a chassis
  is exactly what the governance principle forbids (the Monster/Rotor/Wauxspin
  hold is the precedent).
- **Fallback**: park all 12 (and Snowflake's promotion) in EV under the existing
  gate. Nothing published is wrong meanwhile.

### 5. Terraging chain rider — KEEP, with an administrative action item

- **Question**: reconciling the registry's +3, the shipped two-dex clipper
  notations, footbag.org's 3-bracket terrage, and Red's ruled 4.
- **Rows verified**: **2** EV rows gated `terraging_chain`; 4 rows were ridden to
  Red by the curator session ruling 6 (the other two are presumably in the alias/
  parser layers — the queue and the session record disagree on 2 versus 4;
  reconcile when editing).
- **Removal attempt**: fails by construction — four mutually inconsistent
  authorities; only the author of the ruled 4 can break the tie. This is the one
  entry where independent evidence CANNOT even in principle resolve it, because
  the contradiction includes Red's own prior statement.
- **Administrative defect**: the committed audit cites an email draft never
  committed; nothing proves the rider reached Red. **Confirm it was sent, or send
  it** — until then this entry is not actually pending with him, and the queue
  says so itself.
- **Fallback**: publish under the registry's +3 with a divergence note recording
  the four-way tension (the Witchdoctor near/far divergence note is the shipped
  pattern).

### 6. The rider list — KEEP the list, retire two entries into other queues

Verified row-by-row:

- **Same-operator-twice scoring**: queue says three rows; the universe gates
  **4** (`repeated_operator`). Update the count. Keep — the standing 0-precedent
  doctrine block is deliberate; fallback is EV parking (already in effect).
- **Jani-walker structure**: 1 canonical row active on ADD authority + Sidewinder
  + the Fairy Gyro Janiwalker parser row. Keep; fallback is the already-shipped
  ADD-without-structure state, which is stable indefinitely.
- **General osis-suffix rule**: keep — it is a class-settling question (the
  governance ideal); fallback is the per-row curator path it would accelerate.
- **Arctic**: **state inconsistency found.** The queue calls arctic undefined,
  but the universe has Arctic Butterfly and Arctic Whirl in `authoring` state
  (and an "Arctic (frigidosis Pixie)" alias row suggesting a recorded reading).
  Either arctic gained a usable reading (then narrow the rider to confirming it)
  or the two rows are misclassified (then re-gate them). Reconcile the data and
  the queue; do not leave them disagreeing.
- **Solestice**: zero universe rows — it is a RECORD-name question (one record
  badge), not a dictionary gate. Keep on the rider only as a records item; it is
  already tracked in the IP records item. Consider removing here to avoid double
  tracking (one live queue per case).
- **Blink / twisting**: 1 folk row (Butterfly Symp. Blink) + 6 EV rows
  (twisting 3, twisted 3). Keep; degenerate structures, no exemplar; fallback EV.
- **Motorfly**: zero universe rows; a pure memory check with no data hanging on
  it. Keep as a one-line rider (costless in a letter already sent), but it can
  never block anything.
- **Double Dyno / Double Whip**: records-only (structural-prefix ambiguity).
  Same double-tracking note as Solestice.
- **Stepping Ducking Blurry Whirl**: records-only (6-versus-5 ADD reading).
  Fallback: the record badges nowhere until ruled — honest and already the case.
- **Slicing**: conditionally self-resolving — Slicing Butterfly / Slicing far
  Butterfly sit in `authoring` from FootbagMoves notation; the rider fires only
  if that authoring attempt fails. Correctly framed; keep.

## With the curator

All four are genuinely human-owned; no removal succeeds and none blocks V1:

- **POD versus Dimmier** — requires a video viewing (13 identification rows
  include POD). Not resolvable from the written record by the curator's own
  ruling. Fallback: both names stay visibly unmerged in EV.
- **Kiwi** — source-internal contradiction in PassBack's own row versus the
  archive alias reading (Tapping Paradon). Unrulable from the record. Fallback:
  EV.
- **Clipper Symposium Whirl** — structure unknown; no notation in any source.
  Fallback: EV.
- **Atomic-pickup alias target** — a naming-authority decision between Red's pt6
  naming (omelette) and byte-identical structure (scrambled-eggbeater); the
  half-applied session ruling correctly deferred rather than silently overwrite
  Red-sourced naming. Fallback: the alias stays on omelette (status quo preserves
  the Red provenance — arguably the default the supersession method implies; a
  one-line curator confirmation would close it).

## Positional atom-level foundations (carried in POSITIONAL_IDENTITY.md)

Butterfly default side, whether `[XBD]` inherently encodes OP, the osis
spin-to-catch relationship, and the ambiguous multi-component same-side records:
all verified as genuinely open (the foundations audit isolated them after
settling the four resolvable atoms). Fallback for every one: the resolver's hold
states (`NEEDS_CURATED_EQUIVALENCE`, `AMBIGUOUS_MULTI_COMPONENT`) keep the names
parked without wrong output. Non-blocking.

## Queue edits recommended (maintenance, not doctrine)

1. Refresh or date the counts: blurry ~43 → 64; same-operator-twice 3 → 4;
   terraging 4-versus-2 reconciliation; blazing nine-versus-eight.
2. Reconcile the arctic state inconsistency (queue says undefined; two rows say
   authoring).
3. De-duplicate the records-only riders (Solestice, Double Dyno/Whip, Stepping
   Ducking Blurry Whirl) so each lives in exactly one queue — recommend the IP
   records item as the single home, with the Red letter as the delivery vehicle.
4. Record the terraging send-confirmation as an action item with an owner, since
   the queue itself flags that nothing proves the rider was sent.
5. Split the blurry gate: rows with their own fb.org JOBs move to the authoring
   queue (they author under source priority regardless of Red's answer).
