# PassBack candidate extraction outputs

Staging-only, read-only. Generated by `../build_passback_candidates.py` from `exploration/passback-intake/passback_trick_sources.csv` (282 rows = passback-dicrionary.txt 240 + pb-dict2.txt 42) + read-only `database/footbag.db`. No DB writes, no promotion.

**Red Wave 2 gate:** the unresolved-trick queue (02) is the promotion backlog and stays blocked until the grammar questions rule. Trick/family linkage (01/06) are media-tag candidates that could proceed before the gate, but still need the taxonomy module (ARCHITECTURE section 6) and Dave-track coordination.

## Counts

- 01 trick linkage candidates: 136
- 02 unresolved trick queue (Red Wave 2 gated): 146
- 03 combo decomposition candidates (rows): 300
- 04 operator extraction candidates: 200
- 05 progression candidates (structural prerequisite proxy): 98
- 06 family linkage candidates: 136
- 07 tutorial series grouping (media items): 3

## Matcher-recovery (Bucket 6 fix)

42 rows the intake marked `new_candidate` actually resolve to an existing active slug on DB re-check (alias / canonical / slug). These must NOT be treated as new tricks. Recovered:

- Assassin -> assassin (via recovered_slug)
- Atom Bomb -> atom-bomb (via recovered_slug)
- Bedwetter -> bedwetter (via recovered_slug)
- Big Apple -> big-apple (via recovered_slug)
- Bladerunner -> bladerunner (via recovered_slug)
- Blaze -> blaze (via recovered_slug)
- Bling Blang -> bling-blang (via recovered_slug)
- Blizzard -> blizzard (via recovered_slug)
- Blurrage -> blurrage (via recovered_slug)
- Chainsaw Massacre -> chainsaw-massacre (via recovered_slug)
- Colossus -> colossus (via recovered_slug)
- Darkwalk -> darkwalk (via recovered_slug)
- Dolomite -> dolomite (via recovered_slug)
- Ego -> ego (via recovered_slug)
- Flurricane -> flurricane (via recovered_slug)
- Flux -> flux (via recovered_slug)
- Gangsta Party -> gangsta-party (via recovered_slug)
- Gary Coleman -> gary-coleman (via recovered_slug)
- Goliath -> goliath (via recovered_slug)
- Maelstrom -> maelstrom (via recovered_slug)
- Mantis -> mantis (via recovered_slug)
- Morpheus -> morpheus (via recovered_slug)
- Nova -> nova (via recovered_slug)
- Orbit -> orbit (via recovered_slug)
- Pandemonium -> pandemonium (via recovered_slug)
- Predator -> predator (via recovered_slug)
- Rake -> rake (via recovered_slug)
- Reactor -> reactor (via recovered_slug)
- Ripped Warrior -> ripped-warrior (via recovered_slug)
- Schmoe -> schmoe (via recovered_slug)
- Scorpion's Tail -> scorpions-tail (via recovered_slug)
- Tapdown -> tapdown (via recovered_slug)
- Trixie -> trixie (via recovered_slug)
- Twirl -> twirl (via recovered_slug)
- Wrap -> wrap (via recovered_slug)
- Bladerunner -> bladerunner (via recovered_slug)
- Double Pickup -> double-pickup (via recovered_slug)
- Fairy Eggbeater -> fairy-eggbeater (via recovered_slug)
- Fairy Legover -> fairy-legover (via recovered_slug)
- Orbit -> orbit (via recovered_slug)
- Paradon Swirl -> paradon-swirl (via recovered_slug)
- Triple ATW -> triple-around-the-world (via recovered_alias)

## Unregistered operator candidates surfaced (curator decision needed)

baragging, eggbeating, flailing, illusioning, phasing, slicing, twisting, warping, whipping

Surging is auto-decomposed to spinning + stepping per the dictionary-skill rule; it is never emitted as a standalone operator.
