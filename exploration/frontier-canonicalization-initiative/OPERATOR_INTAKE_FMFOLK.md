# FM/folk operator-intake triage (read-only)

Triage of the unregistered operator-shaped tokens surfaced by `build_fm_folk_ge7_triage.py` (the
FM/folk ≥7 cohort). **Read-only; no operator registration.** Registering a modifier writes
`freestyle_trick_modifiers` + affects ADD math — a curator (and for head-path/topology, Red) decision.
Frequency is evidence, not authority: a single FM occurrence never registers an operator. Same
discipline as `../freestyle-media-ingestion-2026-05-29/candidates/OPERATOR_TRIAGE.md` (which triaged the
earlier PassBack-corpus tokens: illusioning / twisting / flailing / etc.).

## Disposition

| Token | uses | evidence | disposition |
|---|---:|---|---|
| **zulu** | 1 | `odula` "Blurry Zulu Blender"; `zulu` is the unregistered 4th member of the head-path family (`ducking`/`diving`/`weaving` ARE registered) | **CANDIDATE — register (curator/Red).** A real, known movement, not folk noise; completes the head-path family. Strongest candidate. |
| **railing** | 3 | `dorshanatrix` "Railing Symposium Mirage", `flying-fish` "Railing Ducking Mirage", `rail-warrior` "Railing Ducking Butterfly"; no canonical `rail` trick | **CANDIDATE — defer.** Recurs 3× as a leading operator; FM-only. Curator: is "rail/railing" a known move or a variant? |
| **flailing** | 2 | `your-sister`, `bill-ted...`; gerund of `flail` (active base) | **DEFER.** Gerund-of-a-base operator; rides the `illusioning` "is a base-gerund a registrable operator?" Red question. (Already deferred in the prior triage.) |
| floating | 1 | `floatation` "Floating Butterfly" | **DEFER/REJECT** — single-source folk/descriptive. |
| surfing | 1 | `big-papa-smurf` "Surfing ss Blender" | **DEFER/REJECT** — single-source folk. |
| splicing | 1 | `liquifier` "Splicing ss Blender" | **DEFER/REJECT** — single-source folk. |
| bent | 1 | `kyttyra` "...Whirling Bent Symposium..." | **DEFER/REJECT** — unclear descriptive, single-source. |
| warping | 1 | `warp` "Warping Mirage" | **REJECT** — self-referential; `warp` is a trick, not a modifier. |
| motion | 2 | `atomotion` "Atomic Motion", `syberian-express` "...Far Motion" | **REJECT** — a noun in folk names, not an operator. |
| nova | 1 | `oh-wheely` "Nuclear Nova" | **REJECT** — `nova` is a base trick (legover family, 4 ADD), not an operator. |
| double | 1 | `nebula` "Furious Double Legover" | **REJECT** — fragment of the base name `double-leg-over`. |
| star | 1 | `shooting-star` "Shooting Star" | **REJECT** — folk-name noun (shooting is the registered operator; "star" is the base/noun). |
| os | 1 | `your-mom` "Atomic os Symposium..." | **REJECT/CLARIFY** — abbreviation/typo (likely `osis`), not an operator. |
| xbd | 1 | `sasquatch` "...Whirling XBD Rake" | **REJECT** — the cross-body notation token `[XBD]` leaking into a name, not an operator. |
| x-body | 1 | `stepping-ps-whirling-x-body-rake` | **REJECT** — same as `xbd` (cross-body token). |
| p.s | 1 | `stepping-ps-whirling-x-body-rake` "Stepping P.S. Whirling..." | **REJECT/NORMALIZE** — `P.S.` = Paradox-Symposium abbreviation (slug-normalization), not an operator. |
| torquescrew | 1 | `moirius` "Spyro Symposium Torquescrew" | **REJECT** — base-name artifact (no such operator). |

## Summary

- **Register-candidates (curator / Red):** `zulu` (head-path family completion — strongest), `railing`
  (3 uses, assess "rail" first), `flailing` (rides the illusioning gerund-of-base Red question).
- **Single-source folk — defer/reject:** floating, surfing, splicing, bent (frequency != authority).
- **Reject — parse artifacts / abbreviations / structural tokens, NOT operators:** warping, motion,
  nova, double, star, os, xbd, x-body, p.s, torquescrew.

## Recommended next actions (none registered here)

1. **`zulu`** — surface to curator/Red as a head-path-family modifier (parallel to registered
   `weaving`/`ducking`/`diving`); this is topology/family governance, so it belongs with the head-path
   doctrine, not an ad-hoc registration.
2. **`railing`** — curator check whether "rail/railing" is a recognized movement before any registration.
3. **`flailing`** — bundle with the `illusioning` Red Wave 2 "is a base-gerund a registrable operator?"
   question (one ruling settles both).
4. The reject cohort needs no further action; the abbreviation/token cases (os, p.s, xbd, x-body) are
   notation/normalization artifacts the parser/normalizer should absorb, not registry candidates.

Governance: read-only; no `freestyle_trick_modifiers` write. Registration is curator-governed; head-path
additions (zulu) are Red/topology-governed.
