# Frontier Residue Packet — structural-reconciliation questions

**Date:** 2026-06-02
**Reviewer:** Red Husted (curator items also flagged)
**Scope:** the small set of genuinely-open questions left after the pt13/pt14 waves stabilized the operator arithmetic.

---

## Organizing principle: arithmetic validity ≠ structural certainty

Every item below has **valid arithmetic** — the ADD math closes, and where sources diverge on the number, the divergence resolves under the existing outlier rules. What is **uncertain is the structure**: which operators compose the trick, which base it lands on, whether a token is a real operator at all, and which of several readings is canonical.

This packet asks **only structural / governance questions**, never arithmetic ones. A trick can be a clean 7 ADD and still not be publishable, because the publication contract also requires a *reconciled, non-fabricated structure* and *honest incompleteness*. Holding such a trick is the framework working, not a gap. "Defer" remains a fine answer anywhere.

---

## Q1 — Down-family per-row disposition + Shooting Star reconciliation

**Background (settled):** your pt14 Q6 ruling endorsed Option A from the earlier down-family packet — the down-family tricks are standalone and "down" corresponds to a real dexterity. The two bases (`double-over-down` = 4, `down-double-down` = 4) are canonical; the modifier cascade validates arithmetically (e.g. spinning + down-double-down = 5).

What was explicitly deferred (§E of that packet): **for the no-records family members, does each become its own canonical row, or an alias on its structural decomposition?** Only Scorpion's Tail carries a record.

**Q1a — default disposition.** Confirm the heuristic: a down-family compound earns its own canonical row only if it has a competition record (Scorpion's Tail); the no-records members (Shooting Star, Pixie Double Over Down, Blurrier, Down Double-Down Swirl, …) live as **aliases on their structural decomposition**, not as standalone rows.

- **A.** Yes — records → own row; no-records → alias on the decomposition.
- **B.** All cascade members get their own row.
- **C.** Per-row, case by case.

**Q1b — Shooting Star structural reconciliation (the real blocker).** The ADD is agreed at 7 (shooting +3 set, prefixing a 4-ADD base; PassBack's 4 is the lone outlier and is non-authoritative). The **structure is not** — the corpus gives four incompatible readings of the same name:

| Reading | Base | Notation / note |
|---|---|---|
| Shooting **Double Over Down** | double-over-down (4) | `CLIP > OP IN [DEX] > OP OUT [PDX] [DEX] > OP OUT [DEX] > SAME OUT [DEX] > OP CLIP [XBD] [DEL]` |
| Shooting **Torque** | torque (4) | same slug, alt-name "Shooting Torque" in fborg-7add.txt |
| (BOD / RAKE form) | — | `CLIP > OP IN [DEX] > (no plant while) OP IN [PDX] [BOD] [DEX] > OP X-BODY RAKE [XBD] [DEL] [DEX]` |
| PassBack | "Shooting far Double Down" / "Pogo Nuclear far Double Down" | flagged "controversial about cleanliness" |

Mechanistically, two things need pinning:
- **Which base does `shooting` prefix here** — double-over-down or torque? (Both are 4-ADD, so the number can't disambiguate.)
- **Where does the second, `[PDX]`-bearing dex live** — is it part of `shooting`'s own +3 structure, or part of the base? This determines whether `shooting` is a clean prefix operator or carries an internal paradox.

Until the base + the `[PDX]` placement are fixed, Shooting Star stays held (arithmetic-valid, structurally-uncertain).

---

## Q2 — Undefined operator tokens (define / alias / reject)

The frontier surfaced ~20 candidate names that won't parse because they lead with a token that is not a registered operator. Each token below appears in real source names; we cannot promote anything using them until each token is dispositioned. **For each: is it (A) a real operator — give the mechanic (uptime/downtime, does it insert a dex, ADD weight); (B) an alias of an existing operator — name which; or (C) a folk/junk token to reject?**

| Token | Appears in | First-pass guess |
|---|---|---|
| `railing` | dorshanatrix, flying-fish (+1) | ? possibly a rail/edge surface, not a set/body operator |
| `flailing` | your-sister, bill-ted's-excellent-adventure | ? relation to the trick `flail` (= symposium illusion) |
| `floating` | floatation | ? airborne/no-plant marker |
| `surfing` | big-papa-smurf | ? |
| `splicing` | liquifier | ? |
| `warping` | warp | ? |

These are listed for disposition, not advocacy — rejecting any or all is a clean answer, and is the default under the fewer-rows-bias principle if a token has no clear mechanic.

---

## Q3 — Blurry redundant-paradox cases

**Background (settled):** pt14 deprecated `blurry` as a productive operator — it is the legacy way of writing a stepping-paradox trick, and "stepping paradox" is now the accurate form.

The open mechanistic case: a few names carry **both** "Blurry" **and** an explicit "Paradox" in the same name. Example:

- **blurry-chainsaw-massacre** = "Blurry Illusioning Paradox Symposium Eggbeater"

When "Blurry" co-occurs with an explicit "Paradox," is the blurry:
- **A. Redundant** — it *is* the stepping-paradox already named by the explicit Paradox; drop "blurry," read as stepping-paradox once (no double-count).
- **B. A separate layer** — blurry contributes its own stepping-paradox on top of the explicit Paradox, so the name carries two paradox-bearing elements.
- **C. Per-name** — depends on the specific construction.

(Same question applies to `odula` and `sasquatch` if either is confirmed to carry an explicit Paradox alongside a "blurry" reading.)

---

## Reply

Quick `A / B / C / defer` on each is plenty. Anything not ready, defer — every item here is a structural-certainty question, and holding is correct until the structure is reconciled.

Thanks, Red.
