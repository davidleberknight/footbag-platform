# Red — three small ontology questions (omelette / illusioning / atom-smasher SE)

A tight follow-up packet on three loose ends that surfaced during the
2026-05-26 trick-dictionary stabilization pass. ADD totals are not in
dispute on any of them — these are structural-reading / ontology
questions only. Each is independent; please answer in any order.

---

## Q1 — omelette has two competing decompositions on record

Two prior rulings both give **omelette = 3 ADD**, but they disagree on the
structural reading:

- **pt2 (+ followup-2026-04, locked)** — `omelette ≡ atomic illusion`.
  Encoded in the platform's symbolic-equivalence chain and the
  resolved-formula registry (`operator: atomic, base: illusion,
  derivation: atomic(+1 non-rotational) + illusion(2)`).
- **pt6 (2026-05-04)** — `omelette = Illusioning Pickup, 3 ADD`. Encoded
  in the DB row's `base_trick = pickup` + intended `modifier_links =
  illusioning`, and the alias `illusioning pickup` is recorded on the row.

The platform currently surfaces the pt2 reading on the omelette page
("atomic illusion"); the pt6 reading is preserved as an alias but its
modifier never wired up (see Q2). The trick has no JOB notation and no
operational notation, because the two readings would produce different
JOBs and we don't want to publish one without your call.

**Question:** which reading is canonical for publication?

- **A.** pt2 wins — `omelette ≡ atomic illusion`. Then JOB candidate
  `OMELETTE` (folk-name compound mechanical rule), and op-notation
  sibling-derived from atom-smasher's atomic-dex pattern.
- **B.** pt6 wins — `omelette = Illusioning Pickup`. Then `illusioning`
  needs to be a real modifier (see Q2), and JOB/op-notation reflect that
  composition.
- **C.** Both readings co-exist as legitimate alternate readings (the
  Doctrine-C "blurry / stepping" pattern you ruled on 2026-05-21).
  Then both surface on the page; pt2 reading carries the JOB.
- **D.** Something else — write a fifth reading or hold.

---

## Q2 — is `illusioning` a real modifier, and if so, what's its weight?

pt6's `Illusioning Pickup` reading introduced `illusioning` as a new
operator. It was never wired as a registered modifier on the platform,
which is why the pt6 reading doesn't structurally compose today (the
modifier-link silently no-ops at load time). Three options:

- **A.** Yes — `illusioning` is a real body modifier with weight **+1**
  (parallel to spinning / ducking / paradox). Then `illusioning pickup`
  = +1 + pickup(2) = 3 ADD ✓, and it could appear on other compounds.
- **B.** Yes — but with different weight or class (please specify).
- **C.** No — `illusioning` was a one-off naming label for omelette
  only, never intended as a productive operator. Retire the name; pt6
  reading goes away with it; pt2 becomes canonical by default.
- **D.** Hold — defer until you've audited where else "illusioning"
  appears in the corpus.

Answer A or B would also affect any other tricks whose name contains
"illusioning"; we'll surface that list as part of operationalizing
your ruling. There are no other published canonical tricks using it
today.

---

## Q3 — atom-smasher symbolic-equivalence chain (curator-confirm pending)

The platform records `atom-smasher ≡ atomic mirage` in the
symbolic-equivalence chain with a `curatorConfirmPending` flag — meaning
this reading is plausible but not yet locked. Today's notation backfill
gave atom-smasher an explicit JOB:

> `TOE > OP OUT [DEX] > OP IN [DEX] [XDEX] > OP TOE [DEL]` — 4 ADD

The notation encodes the pt1+pt2 hidden X-Dex carry — atom-smasher = 4
ADD via atomic(+1) + mirage(2) + X-Dex(+1). The SE chain reading
`atomic mirage` only accounts for **3** of the **4** ADD on paper; the
X-Dex is the hidden +1.

**Question:** how should we phrase the SE chain reading so it stays
honest?

- **A.** Confirm the existing reading as-is — `atom-smasher ≡ atomic
  mirage` — and clear the pending flag. The X-Dex is treated as a
  hidden mechanical detail not surfaced in the reading.
- **B.** Update the reading to `atomic + x-dex mirage` (or similar
  explicit form). Then clear the pending flag.
- **C.** Withdraw the SE chain entry entirely — atom-smasher is a folk
  name with no clean compositional decomposition; let it stand on its
  own JOB notation.
- **D.** Hold — needs more investigation.

---

## What we'll do with your answers

- **Q1** → unblocks omelette JOB + op-notation publication.
- **Q2** → either registers `illusioning` as a real modifier or retires
  the name. Either way it stops being an unfinished thread.
- **Q3** → clears one of ~12 `curatorConfirmPending: true` flags in
  the symbolic-equivalence chain; future "compressed from" pedagogical
  surfaces become unlockable for atom-smasher.

No urgency on any of these — they're tidy-loose-ends questions, not
blockers on anything in flight.

— end —
